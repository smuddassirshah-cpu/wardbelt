# Build state

Current stage: 0 complete; stages 1-4 in progress
Last updated: 2026-09-08
Mode: autonomous one-shot (see CLAUDE.md)

## Stages
| # | Stage | Status | Verifier | Merged |
|---|-------|--------|----------|--------|
| 0 | Scaffold | merged | orchestrator (stage 0 has no verifier per CLAUDE.md) | 2026-09-08 |
| 1 | Domain core | merged | PASS 2026-09-08 (first round) | 2026-09-08 |
| 2 | Store | merged | 1 FAIL then PASS 2026-09-08 | 2026-09-08 |
| 3 | Scheduler | merged | 1 FAIL then PASS 2026-09-08 | 2026-09-08 |
| 4 | Visual system and components | in progress | - | - |
| 5 | Integration: App wiring and flows | pending | - | - |
| 6 | Shift stats, summary, settings, transfer | pending | - | - |
| 7 | Hardening and evidence | pending | - | - |
| 8 | README, DECISIONS, pre-push gate, deploy | pending | - | - |

Status values: pending / in progress / awaiting verification / verified / merged.
Verifier column records PASS with date, or the fail count before PASS.

## Stage reports
(One section per stage, written by the implementing subagent: built, DoD evidence, deviations, open questions.)

### Stage 0: Scaffold (orchestrator, 2026-09-08)
Built: Vite 7 + Preact 10 + TS 5.9 strict (exactOptionalPropertyTypes, noUncheckedIndexedAccess); ESLint 10 flat config with typescript-eslint strictTypeChecked and security selectors (no dangerouslySetInnerHTML/innerHTML/eval, no em-dash, no-empty, no-floating-promises, no-console, no TODO in code); Prettier; Vitest 4 + jsdom + fake-indexeddb with 100% thresholds on src/domain; Playwright 1.63 Pixel 5 profile against `vite preview` of the production build; vite-plugin-pwa injectManifest with `src/sw.ts` (Workbox precache, navigation fallback, SKIP_WAITING message, notificationclick focus/open); `index.html` with the section 7 CSP; icons generated by `scripts/icons.mjs` (pure Node PNG writer); `ci.yml` (lint, typecheck, unit+coverage, build, e2e) and `pages.yml` (workflow_run on CI success for main, VITE_BASE_PATH=/wardbelt/); MIT LICENSE; `.gitignore`; `tests/fixtures/synthetic.ts`. Shared contract for stages 1-4: `src/domain/types.ts` (Patient, Task, Event, Settings, State, Action union, TransferFile) and `src/domain/template.ts` (19 steps, codes, phases).

DoD evidence (actual runs, 2026-09-08):
- `npm run lint`: eslint 0 problems; prettier "All matched files use Prettier code style!"
- `npm run typecheck`: both tsconfig projects clean.
- `npm test`: 1 file, 4 tests passed; coverage src/domain 100% statements/branches/functions/lines (17/17, 0/0, 3/3, 17/17).
- `npm run build`: dist/index.html 1.27 kB, assets/index 10.92 kB (4.61 kB gzip), sw.js precache 10 entries; no inline script in dist/index.html (CSP compatible).
- `npm run test:e2e`: 2 passed (heading visible; manifest.webmanifest and sw.js served).
- `actionlint`: no findings on ci.yml and pages.yml.
- `npm audit`: 0 vulnerabilities. `gitleaks detect`: no leaks.

Deviations: see Decisions (dependency versions and the extra dev packages).
Open questions: none.

### Stage 1: Domain core (implementing subagent, 2026-09-08, commit 930b150 on stage/1-domain)
Built:
All under /Users/misbahshah/code/wardbelt-wt/stage-1-domain (branch stage/1-domain), pure TypeScript, no I/O, no Date.now():
- src/domain/time.ts: toMs / toIso / addMinutes, MINUTE_MS, DAY_MS; the only ISO-to-ms conversion point (UTC Z form).
- src/domain/recovery.ts: scheduleChecks(tasks, returnedAt) sets dueAt on check_1..4 at exactly +15/30/45/60 min in ms via CHECK_OFFSETS_MIN; clearChecks removes them; other tasks keep object identity.
- src/domain/patient.ts: createPatient, currentTask (min-order todo), isBeltComplete, completeTask/skipTask (todo only, any order, doneAt on both; in_theatre completion sets theatreReturnAt + schedules checks; discharge completion discharges), addCustomTask (insert after / at end, phase inherited or PRE_OP when first, O(n) renumber, duplicate id no-op), setNote (task note with delete-on-empty, or patient notes), dischargePatient, revertTask. Same reference on every no-op; property order kept stable for byte-for-byte undo.
- src/domain/urgency.ts: DUE_SOON_MS, nextDue, overdueTasks, urgencyOf (ranks 0-3), urgencyKey + compareUrgency (strict total order: rank, dueAt, intake, createdAt, id), sortByUrgency (keys once per patient, one sort).
- src/domain/stats.ts: shiftBounds (04:00 local to next 04:00 local via local Date constructors), shiftStats (ShiftStats per section 6; two linear passes over events, undone events excluded, UNDO never counted, 3-minute grace, streak across mixed events, median with even-count averaging, O(k log k) sort justified in a comment).
- src/domain/reducer.ts: initialState, reduce (exhaustive switch with a never-typed runtime fallback), activePatients, dischargedPatients; every action per the stage brief; derived event ids `${eventId}:1`.
- tests/unit/domain/{helpers,time.test,recovery.test,patient.test,urgency.test,stats.test,reducer.test,properties.test}.ts: xorshift32 PRNG, deepFreeze purity guard, unit tests for every branch, and the four property suites (a)-(d) with 33 seeded cases.

DoD evidence (actual runs):
Full gate `npm run lint && npm run typecheck && npm test && npm run build` exit 0 (log at scratchpad/gate.log):
- lint: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!"
- typecheck: tsconfig.json and tsconfig.sw.json both clean.
- npm test (vitest run --coverage): 9 files, 125 tests passed, 2.1 s. Coverage summary: Statements 100% (548/548), Branches 100% (406/406), Functions 100% (108/108), Lines 100% (532/532). Per file (lines/branches): patient.ts 64/64, 55/55; recovery.ts 9/9, 6/6; reducer.ts 120/120, 98/98; stats.ts 56/56, 32/32; urgency.ts 38/38, 35/35; time.ts 5/5; template.ts, types.ts, validate.ts unchanged at 100%. Existing validate and scaffold tests still pass (24 of the 125).
- Property suites all green: (a) 12 seeds x 150-300 random actions incl. invalid targets, deep-frozen states, invariants: exactly one current task or complete belt, orders 0..n-1, task ids unique, status/dischargedAt/discharge-task consistency, event ids unique; (b) 8 seeds x 3 snapshot states x every patient, N random complete/skip then N undos, JSON.stringify equality and event count; (c) 5 seeds x 40 random return times with odd milliseconds, both via completeTask(in_theatre) and SET_THEATRE_RETURN, delta exactly 15/30/45/60 x 60000 ms; (d) 8 seeds, two shuffles plus original sort identical, comparator antisymmetric, transitive, and never 0 for distinct keys.
- Unit tests cover: 3-minute grace at exactly +3:00 (on time) and +3:00.001 (late); undone events excluded and UNDO itself never counted; streak across mixed and undone events; median odd (100), even (65), single, none; shift boundary 03:59:59 vs 04:00:00 local and year rollover; urgency ranks incl. exact due time and exact DUE_SOON_MS edges and every tiebreak level; every reducer branch incl. malformed timestamps and an unknown action type.
- npm run build: dist/index.html 1.27 kB (0.57 kB gzip), dist/assets/index 10.92 kB (4.61 kB gzip), sw.mjs 17.07 kB (5.73 kB gzip), precache 10 entries. Domain code is not yet imported by main.tsx so the bundle is unchanged from stage 0.
- e2e not run (stages 1-3 must not, per the common brief).

Deviations: 1. revertTask clears theatreReturnAt and check dueAts only when the reverted in_theatre task was done, not when it was skipped (brief said "if key is in_theatre"): a skip never set them, so reverting a skip must not remove a separately recorded theatre return; this keeps undo byte-for-byte in that case. 2. UNDO of a discharge completion appends two UNDO events (`eventId` naming the TASK_COMPLETED and `${eventId}:1` naming the derived DISCHARGED), so stats' "excluding undone events" rule also nullifies the discharge count; the brief said "append UNDO { undoOf }" (singular). 3. Actions carrying an unparseable `at`, `returnedAt` or ADD_TASK `dueAt` return the same state instead of throwing (toISOString would throw on NaN); not in the brief, added for the malformed-input case. 4. UNDO still appends its UNDO event when the target's task is already unfinished (e.g. after an IMPORT replaced the record) so undo cannot get stuck on the same event; the brief's "invalid transition returns same state" was read as applying to complete/skip. Everything else follows PLAN.md and the stage brief exactly.

Open questions: 1. Undo after a manual SET_THEATRE_RETURN: if the nurse records a theatre return manually while in_theatre is still unfinished, then completes in_theatre (which overwrites theatreReturnAt per the brief), an UNDO removes theatreReturnAt entirely rather than restoring the manual value; event history would be needed to restore it. Byte-for-byte undo therefore holds for every state except that one ordering; property (b) excludes SET_THEATRE_RETURN from snapshot generation for that reason. Stage 5 may want the UI to prefer COMPLETE_TASK(in_theatre) over SET_THEATRE_RETURN for the normal flow. 2. A DISCHARGE action (as opposed to completing the discharge cell) leaves no TASK_COMPLETED event, so a following UNDO reverts the previous completion rather than the discharge, exactly as PLAN.md section 6 defines undo ("last completion/skip"); stage 5 should decide whether the discharge button offers undo. 3. shiftBounds is tested with local Date constructors on the build machine's zone; a DST-transition day cannot be forced in-test without changing process TZ, which the tests deliberately avoid.

Verifier (independent, 2026-09-08): PASS on first round. Gate re-run green; 66 probe tests across 7 time zones (UTC, New York, Auckland, Kolkata, Chatham, London, Santiago) all pass; probes covered identity on 25 no-op and malformed reducer cases, byte-for-byte revert for every template task, DST and leap-day check scheduling, 200 custom tasks, unicode notes, urgency total order. Notes for later stages: UNDO of a completed discharge appends two UNDO events (eventId and eventId:1) so the derived DISCHARGED event is nullified too; the DISCHARGE action emits only DISCHARGED and cannot be undone by UNDO, so the stage 5 UI should route the discharge button through COMPLETE_TASK on the discharge task; reduce() is only defined for well-typed Action objects.

### Stage 2: Store (implementing subagent, 2026-09-08, commits 88f0d64 and 865a587 on stage/2-store)
Built:
Branch stage/2-store, worktree /Users/misbahshah/code/wardbelt-wt/stage-2-store.
- src/store/db.ts: DB_NAME 'wardbelt', DB_VERSION 1, typed idb DBSchema (patients keyPath id + index status; events keyPath id + index at; settings keyPath key, rows {key, value}); MIGRATIONS map of versioned steps and `upgrade(db, old, new)` applying old+1..new incrementally (throws on a missing step, which aborts and rolls back the open); `openDatabase(factory, name, version)` issues the request on an injected IDBFactory and uses idb's `wrap` for the promise API; `blocked` rejects at once with BlockedError (no retry loop) and a late success closes the stray connection; `versionchange` closes the connection so another tab's upgrade is never blocked.
- src/store/repo.ts: exports StorageNotice, LoadResult, Repo, RepoOptions, DEFAULT_RETRY_DELAY_MS, `openRepo` (never rejects) exactly per the brief. One RepoImpl over two backends (IdbBackend, MemoryBackend). Writes are serialised through one promise chain; each write tries once, waits retryDelayMs via the injectable `wait`, retries once; a second failure emits write_failed and queues the write; the queue is flushed in submission order when the next write is submitted, then `recovered` is emitted. `load()` validates every row with validatePatientRecord / validateEvent / validateSettings, skips and counts corrupt rows, keeps their raw values, sorts events by (at, id), assembles settings from rows with defaults for missing fields, emits `corrupt` when count > 0. `unavailable` is replayed to late subscribers. `close()` closes the db.
- src/store/transfer.ts: buildExport (copies records), serialiseExport (pretty JSON), parseImport (UTF-8 byte cap then validateImport), exportRawStore (JSON envelope with the raw corrupt rows).
- tests/unit/store/helpers.ts: per-test fake IDBFactory, request-like EventTarget factories for blocked/error/success, a factory that throws, `failNextPuts` (fails the next N object-store puts through fake-indexeddb's real request/transaction error path, or sync throw), notice/wait harness, raw open/put/getAll helpers for seeding corrupt rows.
- tests/unit/store/db.test.ts (11), repo.test.ts (28), transfer.test.ts (29): schema and index creation, reopen keeps records, real blocked (held v1 connection vs v2 open), late-success close, request error, VersionError, versionchange self-close, migration loop and abort rollback; round trip through reopen, event ordering, overwrite, deletePatient scope, clearAll, settings row per field and row removal, writes after close surface; fallback for blocked / error / VersionError / sync throw (Error and non-Error values) / indexedDB undefined / migration abort with exact notices, replay on subscribe and every method working in memory; corrupt patient, event (including an event with no `at`), settings rows; retry-once success, fail-twice queue + flush + recovered with same-record ordering, backlog across repeated failures, sync-throw path, concurrent write serialisation, default 250 ms real timer, listener error propagation; 21 malformed import variants (each rejected whole, store unchanged), export/parse deep-equal round trip, non-aliasing, unknown-field drop, file exactly at the byte cap, exportRawStore.

DoD evidence (actual runs, after fixes):
All run in /Users/misbahshah/code/wardbelt-wt/stage-2-store after the fix (no e2e, as instructed):
- `npm run lint`: eslint 0 problems (max-warnings 0); prettier "All matched files use Prettier code style!"
- `npm run typecheck`: `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.sw.json --noEmit` clean, no output.
- `npm test` (vitest run --coverage): Test Files 5 passed (5); Tests 96 passed (96) (was 91; 5 new regression tests); no unhandled errors. Coverage all files: statements 100% (454/454), branches 98.85% (258/261), functions 100% (101/101), lines 100% (443/443); src/domain thresholds (100%) satisfied. src/store: 100% statements/functions/lines, 96.29% branches; the only uncovered branches are pre-existing and noted by the verifier (db.ts:70 `newVersion ?? version`, repo.ts:259 equal-id sort tie); the new `inTransaction` is fully covered on both the abort and the already-aborted path.
- `npm run build`: dist/index.html 1.27 kB (gzip 0.57), dist/assets/index-C5IxCUWh.js 10.92 kB (gzip 4.61), dist/sw.mjs 17.07 kB (gzip 5.73), precache 10 entries (16.69 KiB), built without errors.
- Regression check: with src/store/repo.ts stashed to the pre-fix version, the same test file gives 5 failed | 27 passed, the five failures being exactly the new tests; restored, 32/32 pass.
- Verifier's original probe (sync throw in cursor loop): before the fix load() returned 0 patients and 4 events plus 2 Vitest "Unhandled Rejection: AbortError" errors; after the fix the equivalent test asserts 1 patient and 4 events intact, `write_failed` with reason containing "QuotaExceededError: quota", one wait of 5 ms, then `recovered` and 0 events after the flush, with zero unhandled rejections.
- `git status --porcelain` empty after commit and push.

Deviations: 1. Events are read with getAll() on the events store and sorted by (at, id) in the repo rather than read through the `at` index. Reason: an index cannot see rows whose `at` is not a valid key, so index-only reads would make exactly those corrupt rows invisible to the corrupt count and rawCorrupt; the sort reproduces index order at O(n log n). The index still exists per the binding schema and is tested.
2. Repo write methods resolve (never reject) after a double failure; the failure surfaces as the write_failed notice and the write stays queued. The brief's interface says Promise<void> and section 8 says the action is already applied in memory, so a rejection would give stage 5 nothing extra to do. A listener that throws does propagate to the caller of the write.
3. `load()` propagates a read failure (rejects) rather than switching modes; section 8 lists no boot-read failure mode, and silently flipping `mode` after open would be incoherent. Stage 5 should catch it and show the storage banner.
Everything else matches PLAN.md sections 5, 7, 8 and the stage brief.
Fix round: Fixed both verifier failures plus one related latent defect the verifier's own probe exposed, all in /Users/misbahshah/code/wardbelt-wt/stage-2-store/src/store/repo.ts.

1. Partial commit on a synchronous throw mid-transaction (verifier finding 1). Added `inTransaction(tx, body)`: runs the body, and on any failure aborts the transaction when `tx.error === null` (a failed request has already aborted itself and set `error`; a throw from anywhere else leaves the transaction idle and about to auto-commit half-applied), observes `tx.done`, and rethrows the body's error. `deletePatient`, `putSettings`, `clear` and `readAll` now run through it, so the patient delete, the settings clear and the first store clear are all rolled back when a later step fails, and the write is queued for retry exactly as before.

2. Unhandled rejection of idb's eagerly created `tx.done` (found while reproducing finding 1: the verifier's probe produced two "Unhandled Rejection: AbortError" errors in Vitest). `inTransaction` always observes `done`, and requests inside one transaction are now awaited one at a time (at most six per transaction) because idb v8 does not attach a handler to request promises: a request left pending when a sibling threw synchronously would reject unobserved on abort.

3. `__proto__` settings row leaking through the prototype chain (verifier finding 2). `load()` now copies only keys in `KNOWN_SETTINGS_KEYS` (a Set built from `SETTINGS_KEYS`) into the assembled record; unknown keys, `__proto__` and `constructor` included, are dropped rather than assigned, matching PLAN.md section 7 "unknown fields dropped" and the existing `futureField` behaviour (not counted as corrupt).

Regression tests (tests/unit/store/repo.test.ts, new describe 'multi-request transactions are atomic' plus one case under 'load with corrupt rows'): deletePatient rolled back entirely on a synchronous cursor-delete throw and on a cursor-delete request error (both then flushed on the next write with `recovered`); saveSettings keeps previous rows when a put throws after the clear; clearAll leaves every store intact when a clear fails, then applies on flush; `__proto__`/`constructor` settings rows dropped with no prototype leak, corrupt 0. Each transaction test also asserts no unhandled rejection via a new `unhandledRejectionsDuring` helper. tests/unit/store/helpers.ts: extracted `erroringRequest` (genuine fake-indexeddb request that errors and aborts), added `failNextClears`, `failNextCursorDeletes`, `unhandledRejectionsDuring`. Verified all five new tests fail against the previous repo.ts (git stash) and pass with the fix. Removed the verifier's untracked scratch tests/unit/store/probe.test.ts (never committed; it used console.log and would fail lint).

Open questions: 1. Domain defect outside my ownership (src/domain/validate.ts, stage 0/1): `isTaskKey` uses `u in TEMPLATE_BY_KEY`, and TEMPLATE_BY_KEY is a plain object with Object.prototype, so inherited names pass as task keys. Verified in-session: validatePatientRecord accepts a task with key 'constructor' and validateEvent accepts taskKey '__proto__'. Fix is `Object.hasOwn(TEMPLATE_BY_KEY, u)`; the import boundary then rejects such records. My __proto__ import variants target other fields, so they pass regardless.
2. Stage 5 wiring notes: subscribe before load() to receive the corrupt notice; catch load() rejection into the storage banner; the repo never rejects writes on storage failure, but a throwing notice listener does propagate to the write's caller.
3. The typescript-eslint stylistic rule non-nullable-type-assertion-style asks for `!` while no-non-null-assertion forbids it; tests use a guarded accessor instead. Not blocking, but future stages will hit the same pair. The verifier's notes (a) to (h) describe implementer decisions rather than failures and are unchanged by this fix: load() reads events from the store and sorts by (at, id) rather than using the `at` index so rows with an invalid `at` are still counted as corrupt; corrupt settings count as one logical record; queued writes flush ahead of the newly submitted write; write methods resolve rather than reject on storage failure (notice is the channel); load() rejects on a read transaction failure (stage 5 must catch this at boot); openRepo has no timeout if the factory never fires an event; appendEvent uses put so a duplicate event id overwrites; exportRawStore uses JSON.stringify (throws on BigInt, flattens Map). Stage 2 report is returned here rather than written to STATE.md, per the common brief.

Verifier (independent, 2026-09-08): round 1 FAIL with 2 findings (minor: deletePatient commits a partial delete when the cursor loop throws synchronously (patient removed, its events orphaned); minor: A settings row keyed `__proto__` injects fields into loaded settings via the prototype chain instead of being ignored or counted corrupt). Round 2 PASS after fixes. Probes: DoD gate from scratch on 865a587: npm run lint (eslint 0 problems, prettier clean, exit 0), npm run typecheck (both projects clean), npm test (5 files, 96 tests passed; src/store 100% stmts/lines/funcs, 96.3% branches; domain thresholds met), npm run build (dist + sw.js, 10 precache entries). No unhandled rejections reported. API shape: type-level bidirectional equality (Equals<A,B>) of StorageNotice, LoadResult, Repo, RepoOptions, openRepo, buildExport, serialiseExport, parseImport, exportRawStore against the brief's declarations; tsc clean. PASS. Schema: raw factory inspection after openRepo shows version 1, stores patients(id, index status), events(id, index at), settings(key, no indexes); reopen keeps records; two connections at v1 both read/write without blocking. PASS. Retry once: put fails once (genuine fake-indexeddb request error) then succeeds: wait called exactly once with the injected retryDelayMs (123), no notice, data present. PASS. Genuine internals-free failure: put diverted to add so a real ConstraintError fires; twice -> write_failed containing 'ConstraintError', old record intact, next write flushes and emits recovered. PASS. Quota path on every write method (savePatient, appendEvent, saveSettings via put and via clear, clearAll, deletePatient): each yields exactly one {kind:'write_failed', reason} with 'QuotaExceededError', one wait, zero unhandled rejections, and rollback of the multi-request transaction. PASS. Head-of-line queue: W1 and W2 both fail twic
Verifier notes: Decisions the implementer made where the brief was silent or where it deviated with reasoning (none change observable behaviour against the DoD):
1. Queue flush order: when a backlog exists, the backlog is attempted first and the new write only after it, so a still-failing head write also queues the new write (with a second write_failed whose reason describes the head write). PLAN.md section 8 reads more like 'attempt the new write; on success flush the queue'. The implementer's ordering guarantees a retried write never lands after a newer write to the same record, and nothing is lost either way. Stage 5 should expect possibly repeated write_failed notices while storage is broken, and a single recovered when it drains.
2. Events are read from the object store and sorted by (at, id) in O(n log n) rather than via the at index, because rows whose at is not a valid key are invisible to the index and are exactly the corrupt rows that must be counted. Order matches index order for valid rows.
3. Settings are treated as one logical record: any bad row (or any row without a string key) makes the whole record fall back to DEFAULT_SETTINGS, counts as 1 corrupt, and pushes every settings row (valid ones included) into rawCorrupt. So corrupt can be less than rawCorrupt.length; the UI should display count and offer rawCorrupt without assuming equality.
4. load() rejects (rather than resolving with a notice) when the read transaction fails or the connection is closed (InvalidStateError). S

### Stage 3: Scheduler (implementing subagent, 2026-09-08, commits 92a3d9a and 275f6ca on stage/3-scheduler)
Built:
/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler on branch stage/3-scheduler:
- src/scheduler/clock.ts: `Clock` interface, `realClock` (Date.now, window.setTimeout/clearTimeout), `createFakeClock(startMs)` with `advance` (fires due callbacks in (due, arm-order) sequence, sets now to each due time before calling, handles timers armed during the advance), `set` (jump either way, fires nothing), `pending`. Sorted-insert array, O(log n) search per arm; negative/NaN delays clamp to 0; a throwing callback propagates and leaves the rest armed.
- src/scheduler/timers.ts: `DueTask`, `Notifier`, `TimersOptions`, `Timers`, `DEFAULT_MAX_DELAY_MS`, `createTimers`. One timer at a time (`arm` always clears first, so re-entrant `reschedule` from a dispatch stays single); sweep dispatches TICK, collects newly due todo tasks of active patients not yet announced at their current dueAt (Map taskId -> dueAt, rebuilt each sweep so a task drops out when done/skipped/removed or its dueAt moves), dispatches one DUE each (most overdue first), one `notifier.due` when non-empty; delay = clamp(earliest unannounced due - now, 0, max) or max when none; `onVisible` sweeps immediately then re-arms if running; `start`/`stop` idempotent; fire re-arms in `finally`; unparseable dueAt skipped; only ids, names and labels retained.
- src/scheduler/notify.ts: `PermissionState`, `NotifyDeps`, `NotifierApi`, `createNotifier`, `browserNotifyDeps`, plus `dueTitle`, `dueBody`, `DUE_TAG`, `DUE_VIBRATION`, `DUE_BODY_LINES`. `state()` reads the live permission ('unsupported' without a Notification API); `requestPermission` prompts only from 'default' and routes a rejecting prompt to onError; `due` vibrates [200,100,200] then, when granted, shows `${n} check(s) due` with up to 5 `${patientName}: ${label}` lines then `and N more`, tag 'wardbelt-due', renotify true; missing registration or rejecting showNotification/getRegistration route plain-English reasons to onError (default no-op). `browserNotifyDeps` feature-detects navigator.vibrate, navigator.serviceWorker.getRegistration and Notification without throwing.
- tests/unit/scheduler/clock.test.ts (11), timers.test.ts (25), notify.test.ts (19): fake clock only, no vi.useFakeTimers, async notifier paths awaited via promises resolved by the fakes.

DoD evidence (actual runs, after fixes):
All run in the worktree on 2026-09-08 after the fix:
- `npm run lint`: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!".
- `npm run typecheck`: tsc -p tsconfig.json and tsc -p tsconfig.sw.json both clean.
- `npm test` (vitest run --coverage): 5 files, 82 tests passed (scheduler: 58 across clock/notify/timers, up from 55). Coverage summary 100% statements (405/405), 100% branches (269/269), 100% functions (80/80), 100% lines (396/396). Per file: scheduler/clock.ts, notify.ts, timers.ts each 100/100/100/100; src/domain threshold met at 100%.
- `npm run build`: dist/index.html 1.27 kB (gzip 0.57), dist/assets/index 10.92 kB (gzip 4.61 kB), dist/sw.mjs 17.07 kB (gzip 5.73 kB), PWA precache 10 entries.
- Regression check: the three new timers tests run against the previous timers.ts (git show HEAD~1) fail (3 failed | 25 passed); against the fix all 28 pass.
- Verifier's real-timer trap reproduced: a temporary setup file replacing window.setTimeout and globalThis.setTimeout with a function throwing 'REAL TIMER USED', merged into vitest.config via a temporary config in the worktree; tests/unit/scheduler: 3 files, 58 passed, 0 failed. Temp files deleted before commit; `git status --porcelain` empty after push.
- e2e not run (stages 1 to 3 must not, per the common brief).

Deviations: None from PLAN.md. One from the brief's literal wording: the brief says delay = clamp(nextDue - now, 0, max) with nextDue the earliest dueAt over all todo tasks; taken literally an overdue task that has already been announced but is still todo (the nurse has not done the check yet) yields delay 0 forever and a busy re-arm loop. The delay therefore uses the earliest dueAt among tasks not yet announced at their current dueAt, while `nextDueAt()` keeps the literal meaning (earliest dueAt over todo tasks of active patients, overdue included). Tested ("announces a task added with a past dueAt on the next reschedule without a busy loop").
Fix round: Fixes after verification, branch stage/3-scheduler, commit 275f6ca (pushed).

/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler/src/scheduler/timers.ts (major fix): `notified` is now rebuilt from the current board on every arm as well as every sweep. A new `prune()` runs inside `arm()`: it walks the timed outstanding tasks once, keeps only entries whose task is still todo on an active patient with the same dueAt, and returns the earliest unannounced dueAt used for the delay (the same O(tasks) walk arm already did). A task completed then undone, or whose dueAt moves away and back, between two `reschedule()` calls with no tick in between is therefore re-announced on the next sweep. The three task walks (sweep, prune, nextDueAt) share one generator `timedTodo()` that yields active-patient todo tasks with a parseable dueAt, replacing the previous duplicated loops. Exported shapes unchanged. Decision-notes header updated to describe the new behaviour.

/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler/tests/unit/scheduler/timers.test.ts: three regression tests (all fail on the previous timers.ts, pass on the fix): complete then undo within a single tick window re-notifies (the verifier's exact repro, asserting two DUE actions at +30 s and +33 s and two batches, then no third); dueAt moved to D2 then back to D1 within a single tick window re-notifies at D1; an announced overdue task completed then undone is announced fresh on the next onVisible sweep.

/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler/tests/unit/scheduler/clock.test.ts (minor fix): the realClock test no longer arms a real window.setTimeout; it spies on `window.setTimeout` / `window.clearTimeout` (target typed as plain `Window` so the DOM signature returning number is used rather than the Node overload) with inert mock implementations and asserts delegation of the callback, delay and id. vitest.config `restoreMocks: true` restores them after the test.

Open questions: For stage 5 wiring: call timers.start() then timers.onVisible() at boot (either order works), timers.reschedule() after every state change, timers.onVisible() on visibilitychange to visible, and timers.stop() in the read-only second tab; notifier.due is fire-and-forget, so pass onError from browserNotifyDeps() consumers through to the banner. For stage 6: the denied/unsupported explanation copy is Settings' to write from state(); no helper was added. Verifier note: the scheduler is not yet imported by main.tsx, so the bundle size is unchanged by this stage. Carried over from the verifier's notes, unchanged by this fix: stage 5 must not call onVisible() on the read-only second tab (BroadcastChannel lock) or that tab will dispatch DUE and vibrate in parallel with the primary tab; stage 5 should leave maxDelayMs at the default (a non-positive or NaN value would arm zero delays in a loop).

Verifier (independent, 2026-09-08): round 1 FAIL with 2 findings (major: Notified set is pruned only at sweep time, so a task that leaves todo and returns before the next tick is never re-notified (complete then undo within a tick); minor: realClock test arms a real window.setTimeout in a unit test). Round 2 PASS after fixes. Probes: Gate from scratch on 275f6ca: npm run lint (eslint + prettier), npm run typecheck (both tsconfig projects), npm test (82 tests, coverage 100/100/100/100 incl. src/scheduler), npm run build: all exit 0; e2e not run per instruction RE-PROBE prior failure 1: complete then undo between two reschedule() calls with no tick between -> 2 DUE, 2 batches, no further re-notify over 10 min, single timer (PASS, fixed by prune() in arm()) RE-PROBE prior failure 1 variants: dueAt moved D1->D2->D1 with no tick; skipped->todo; patient discharged->active; patient deleted->re-added, all within one tick window: each re-notifies exactly once (PASS) RE-PROBE prior failure 2: scheduler suite plus all probes run with globalThis/window setTimeout, setInterval, setImmediate, requestAnimationFrame, requestIdleCallback replaced by throwing stubs: 129 tests pass, no trip; positive control confirmed the trap fires on realClock.setTimeout (PASS, clock.test.ts now uses vi.spyOn mocks) API shape: type-level Equal<> assertions under tsc for Clock, FakeClock, DueTask, Notifier, TimersOptions, Timers, PermissionState, NotifyDeps, NotifierApi, createFakeClock, createTimers, createNotifier, browserNotifyDeps, realClock against the brief's declarations: all identical; negative control produced TS2344 so the assertions are live (PASS) Timers: task due exactly at start fires once at delay 0 then 60 s cadence, no busy loop (PASS) Timers: due at now+cap fires at cap; due at now+cap+1 fires at cap+1 after the 60 s TICK
Verifier notes: Both prior failures are fixed: arm() now calls prune(), which rebuilds the notified map from the live board on every reschedule() as well as every sweep, so complete/undo, skip/undo, dueAt move-and-back, discharge/re-activate and delete/re-add within a single tick window all re-notify exactly once; and the realClock test now delegates through vi.spyOn mocks instead of arming a real window timer. Decisions for the STATE.md log where the brief or PLAN.md was silent or literally unworkable: (1) the armed delay is computed from the earliest UNANNOUNCED todo task rather than the brief's literal clamp over all todo tasks, because the literal formula arms a 0 ms delay forever once an announced overdue check is left outstanding; nextDueAt() keeps the plain section 8 meaning (earliest outstanding due including announced ones). (2) realClock uses window.setTimeout rather than globalThis.setTimeout (identical in the browser; needed because @types/node makes globalThis.setTimeout return NodeJS.Timeout); realClock therefore requires a window and is not usable in the service worker, which nothing needs. (3) Newly due tasks are dispatched and listed most-overdue first (k log k over the newly due subset only, justified in the file header). (4) onVisible() runs the sweep and dispatches TICK/DUE and notifies even when stopped; only the re-arm is gated on running. Stage 5 must not call onVisible on the read-only second tab or it will vibrate and dispatch DUE in parallel with the primary tab. (5

## Decisions
Running log. One line each: date, decision, reason, PLAN.md deviation? y/n.
- 2026-09-08, TypeScript 5.9.3 rather than 7.x, typescript-eslint 8.70 peer range is <6.1 and TS 6 changed tsconfig defaults, n.
- 2026-09-08, Vite 7.3.6 and Vitest 4.1.11 rather than 8.x/5.x, vitest 5.0.0 was five days old at scaffold time and the 4.x line is the mature one, n.
- 2026-09-08, jsdom 29.1.1 rather than 30, jsdom 30 needs node >=22.22 and the build machine runs 22.16, n.
- 2026-09-08, added dev deps beyond section 10: @vitest/coverage-v8 4.1.11 (Vitest's own provider, needed for the stage 1 coverage gate), @types/node 22.20.1 (process.env in configs), workbox-precaching and workbox-routing 7.4.1 (Workbox is named in section 4; pinned explicitly instead of relying on hoisting from workbox-build). All maintained, released within the last month, MIT, y.
- 2026-09-08, eslint-plugin-preact omitted (last release 2022, v0.1.0, unmaintained); section 7 rules enforced with no-restricted-syntax selectors in eslint.config.js, n (plan allows the maintained equivalent).
- 2026-09-08, ESLint 10 no longer ships @eslint/js so the core rule set is listed explicitly in eslint.config.js, n.
- 2026-09-08, no @preact/preset-vite; JSX via esbuild automatic runtime with jsxImportSource preact, n.
- 2026-09-08, template has 19 steps: section 6 enumerates 19 keys and 19 codes although section 1 says "17-step"; the enumerated list is binding, y (documented discrepancy).
- 2026-09-08, Action union extended beyond the section 6 list with SET_NOTE, SET_SETTINGS, PURGE_DISCHARGED, RESET and DUE, which the section 3/5/8 flows (notes, settings, purge, wipe, scheduler due) require; mutating actions carry `at` and `eventId` from the caller so the reducer stays pure, y.
- 2026-09-08, template task ids are `${patientId}:${key}`; custom task ids are supplied by the caller, n.
- 2026-09-08, package-lock.json generated with npm 11.19.1 via npx because the bundled npm 10.9.2 crashes in arborist ("edgesOut" of null) on Vitest's optional peer graph; `npm ci` with 10.9.2 verified clean from the resulting lockfile, n.
- 2026-09-08, pages.yml deploys on `workflow_run` success of CI on main (plus manual dispatch) so a red e2e never deploys, n.
- 2026-09-08, in-app routes use the URL hash (`#/dev`, `#/summary`, `#/settings`) so the SW navigation fallback and the Pages sub-path need no special handling, n.
- 2026-09-08, Stage 0 owns src/domain/types.ts and template.ts as the cross-stage contract; stage 1 may add to them but may not change exported shapes without reporting, n.
- 2026-09-08, stage reports are returned by each subagent as structured output and written into STATE.md by the orchestrator verbatim, so four parallel branches never conflict on this file, n.
- 2026-09-08, stage 1: Tiebreaks in compareUrgency compare the stored ISO strings lexicographically (normalised UTC so chronological) rather than parsed ms, so the order is total even for unparseable dates; PLAN.md silent, n.
- 2026-09-08, stage 1: medianAdmitToDischargeMin is returned unrounded (fractional minutes); the summary screen formats it; PLAN.md silent, n.
- 2026-09-08, stage 1: Rank 1 (due soon) boundary is inclusive: dueAt - now <= DUE_SOON_MS; rank 0 is dueAt <= now; PLAN.md silent, n.
- 2026-09-08, stage 1: PATIENT_DELETED event type stays unused: DELETE_PATIENT removes the patient and its events and appends nothing (per brief), n.
- 2026-09-08, stage 1: Completing or skipping tasks on a discharged patient is allowed (only SET_THEATRE_RETURN is refused for discharged patients, per brief); PLAN.md silent, n.
- 2026-09-08, stage 1: currentTask picks the minimum-order unfinished task by scanning, not the first array element, so imported records with unsorted tasks still behave; PLAN.md silent, n.
- 2026-09-08, stage 1: Exhaustive reducer switch keeps a default that routes a never-typed action to ignoreUnknown returning the same state, so an unknown persisted action type is ignored at run time and the coverage gate needs no exclusion comment; n.
- 2026-09-08, stage 1: Custom task inserted into an empty task list gets phase PRE_OP; otherwise inherits the phase of the task it follows (per brief), n.
- 2026-09-08, stage 1: Stats collects undoOf ids from every UNDO event regardless of the shift window, so an undo performed after 04:00 still nullifies the shift's event; PLAN.md silent, n.
- 2026-09-08, stage 1: revertTask on in_theatre only clears theatre return when the task was done, not skipped; deviation from brief wording, y.
- 2026-09-08, stage 1: UNDO of a discharge completion emits a second UNDO event for the derived DISCHARGED event; deviation from brief wording, y.
- 2026-09-08, stage 1: Reducer ignores actions with unparseable timestamps instead of throwing; addition, y.
- 2026-09-08, stage 1: Property test (a) uses a violations() string list with one assertion per step instead of hundreds of expects, bringing the suite from 11 s to 0.3 s; n.
- 2026-09-08, stage 1: Test-only xorshift32 PRNG and deepFreeze live in tests/unit/domain/helpers.ts (not a test file, outside coverage include); n.

- 2026-09-08, stage 2: 2026-09-08, db.ts issues factory.open() on an injected IDBFactory and uses idb's `wrap` rather than idb's openDB, because openDB reads the global indexedDB and the brief requires an injectable factory (undefined simulates private mode), n.
- 2026-09-08, stage 2: 2026-09-08, `blocked` rejects immediately with BlockedError and a later success closes the stray connection; PLAN section 8 says memory mode with no retry loop, n.
- 2026-09-08, stage 2: 2026-09-08, all repo writes are serialised through one promise chain so a retried write can never overwrite a newer write to the same record; the retry queue is flushed in submission order ahead of the newly submitted write, which is the only order that keeps the same-record guarantee, n.
- 2026-09-08, stage 2: 2026-09-08, write_failed is re-emitted on every subsequent submit while the backlog still fails, so the banner state stays current; recovered is emitted once when the queue drains, n.
- 2026-09-08, stage 2: 2026-09-08, settings are one logical record: rows assembled over DEFAULT_SETTINGS so missing rows take defaults without being counted; any invalid value or malformed row marks the settings corrupt once and keeps every settings row in rawCorrupt, n.
- 2026-09-08, stage 2: 2026-09-08, saveSettings writes one row per defined Settings field inside one clear-then-put transaction, so an undefined lastExportAt removes its row, n.
- 2026-09-08, stage 2: 2026-09-08, deletePatient scans the events store with a cursor in the same readwrite transaction (no patientId index in the binding schema), O(n) over events, n.
- 2026-09-08, stage 2: 2026-09-08, error reasons are duck-typed from {name, message} because a DOMException is not instanceof Error across realms (jsdom, workers), n.
- 2026-09-08, stage 2: 2026-09-08, exportRawStore wraps the rows in {schemaVersion, kind: 'raw-corrupt-records', records} so a recovery file is self-describing, n.
- 2026-09-08, stage 2: 2026-09-08, the unavailable notice is the only one replayed on subscribe, as the brief specifies; corrupt and write_failed are point-in-time, n.
- 2026-09-08, stage 2: 2026-09-08, write failures in tests are simulated by spying IDBObjectStore.prototype.put with mockImplementationOnce that routes through fake-indexeddb's _execRequestAsync with a throwing operation, so the request error event, transaction abort and idb rejection are all genuine; a sync-throw variant is also covered, n.
- 2026-09-08, stage 2: 2026-09-08, events sorted from a store getAll() rather than the at index so corrupt rows without a valid at key are still counted (see deviations), y.
- 2026-09-08, stage 2: 2026-09-08, a settings row whose key is unknown (including `__proto__` and `constructor`) is dropped and not counted as corrupt, consistent with PLAN.md section 7 "unknown fields dropped" and the existing handling of forward-compatible keys; a row that is not a `{key: string}` object still marks the settings record corrupt, n..
- 2026-09-08, stage 2: 2026-09-08, `inTransaction` decides whether to call `tx.abort()` by `tx.error === null`: a request failure sets `error` while aborting on its own (fake-indexeddb finishes the transaction before the rejection reaches user code, so a second abort would throw InvalidStateError), whereas a synchronous throw leaves `error` null and the transaction active; the transaction's `done` rejection is observed with no-op handlers because it carries the same failure the body already propagates, n..
- 2026-09-08, stage 2: 2026-09-08, requests within one transaction are awaited one at a time rather than via Promise.all because idb v8 attaches no handler to request promises and a pending sibling would reject unhandled when the transaction aborts; at most six requests per transaction so cost is negligible, n..
- 2026-09-08, stage 2: 2026-09-08, regression tests assert the absence of unhandled rejections explicitly with a `process.on('unhandledRejection')` capture plus two setImmediate flushes, rather than relying only on Vitest's run-level error reporting, so the assertion sits next to the behaviour under test, n..
- 2026-09-08, stage 2: 2026-09-08, the clearAll atomicity test makes the first store clear throw (both attempts) rather than the second, because passing the untouched calls through to the original method would require holding an unbound method reference that the lint config rejects; rollback of a completed request in the same transaction is exercised by the deletePatient and saveSettings tests, n..
- 2026-09-08, stage 2: 2026-09-08, deleted the verifier's untracked scratch file tests/unit/store/probe.test.ts from the worktree (never committed, used console.log and would fail lint); its two scenarios are now covered by committed tests, n..
- 2026-09-08, stage 3: 2026-09-08, timers delay excludes tasks already announced at their current dueAt while nextDueAt() keeps the plain section 8 meaning, an announced overdue task would otherwise re-arm a 0 ms timer in a loop, n (brief wording only).
- 2026-09-08, stage 3: 2026-09-08, newly due tasks are dispatched and listed most overdue first (sorted by dueAt, ties in patient then task order), gives the five-line notification cut a meaningful order and is independent of patient map iteration order, n.
- 2026-09-08, stage 3: 2026-09-08, onVisible() sweeps whether or not timers are started and re-arms only when running, so boot order start()/onVisible() does not matter and a stopped scheduler never arms a timer, n.
- 2026-09-08, stage 3: 2026-09-08, the announced set is kept across stop()/start() and across backwards clock jumps; a task re-announces only when it leaves todo (including undo back to todo) or its dueAt changes, matches the brief rule and avoids double alerts after an NTP correction, n.
- 2026-09-08, stage 3: 2026-09-08, fire and onVisible re-arm in a finally block so a throwing dispatch or notifier propagates (never swallowed) but does not kill the timer chain, n.
- 2026-09-08, stage 3: 2026-09-08, realClock uses window.setTimeout/clearTimeout (numeric ids in browsers); under vitest jsdom Node timers back window.setTimeout so the runtime id is a Timeout object there, and the unit test asserts only the arm-then-clear round trip, n.
- 2026-09-08, stage 3: 2026-09-08, renotify is absent from the TS DOM lib NotificationOptions; a local DueNotificationOptions extension carries it and is passed as a variable so the brief's showNotification signature is unchanged, n.
- 2026-09-08, stage 3: 2026-09-08, browserNotifyDeps uses navigator.serviceWorker.getRegistration() rather than .ready because ready never resolves when no worker is registered (dev server), an undefined registration routes to onError, n.
- 2026-09-08, stage 3: 2026-09-08, a rejecting Notification.requestPermission is caught, routed to onError as plain English and the current state returned, n.
- 2026-09-08, stage 3: 2026-09-08, due([]) is a complete no-op (no vibration, no notification); timers never calls it empty anyway, n.
- 2026-09-08, stage 3: 2026-09-08, onError reasons append the platform error message when it is a non-empty Error message (browser text, never patient data), n.
- 2026-09-08, stage 3: 2026-09-08, additive exports beyond the binding interfaces: DEFAULT_MAX_DELAY_MS, dueTitle, dueBody, DUE_TAG, DUE_VIBRATION, DUE_BODY_LINES, so Settings and tests share the copy, n.
- 2026-09-08, stage 3: 2026-09-08, fake clock timers kept in an array sorted by (due, sequence) with binary-search insert and shift from the front, O(log n) search per arm and O(1) pop per fire, n.
- 2026-09-08, stage 3: Prune `notified` inside arm() rather than on every reschedule() only: arm is the single choke point reached by start, reschedule, fire and onVisible, so every path that re-arms sees a map consistent with the board; same O(tasks) walk arm already performed, PLAN.md deviation n..
- 2026-09-08, stage 3: Share one generator (timedTodo) across sweep, prune and nextDueAt instead of three hand-written loops: removes duplicated filtering and keeps the unparseable-dueAt rule in one place, PLAN.md deviation n..
- 2026-09-08, stage 3: realClock test spies on window.setTimeout/clearTimeout via a `const win: Window = window` binding (a widening assignment, not a cast) because the `Window & typeof globalThis` intersection resolves the mock signature to Node's NodeJS.Timeout overload under @types/node, PLAN.md deviation n..
- 2026-09-08, stage 3: Added a third regression test (announced overdue task completed then undone, re-announced on onVisible) beyond the two the verifier asked for, because it exercises the prune path with a task already overdue at arm time, PLAN.md deviation n..

## Open questions
Anything blocking or deferred, with the stage it affects.

## Next action
Orchestrator: stages 1-4 running as parallel subagents on stage/1-domain, stage/2-store, stage/3-scheduler, stage/4-ui; verify each, merge in order 1, 2, 3, 4.
