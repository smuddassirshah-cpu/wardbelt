# Build state

Current stage: 8 merged; confirming deploy
Last updated: 2026-09-09
Mode: autonomous one-shot (see CLAUDE.md)

## Stages
| # | Stage | Status | Verifier | Merged |
|---|-------|--------|----------|--------|
| 0 | Scaffold | merged | orchestrator (stage 0 has no verifier per CLAUDE.md) | 2026-09-08 |
| 1 | Domain core | merged | PASS 2026-09-08 (first round) | 2026-09-08 |
| 2 | Store | merged | 1 FAIL then PASS 2026-09-08 | 2026-09-08 |
| 3 | Scheduler | merged | 1 FAIL then PASS 2026-09-08 | 2026-09-08 |
| 4 | Visual system and components | merged | 1 FAIL then PASS 2026-09-08 | 2026-09-09 |
| 5 | Integration: App wiring and flows | merged | PASS 2026-09-09 (first round) | 2026-09-09 |
| 6 | Shift stats, summary, settings, transfer | merged | 1 FAIL then PASS 2026-09-09 | 2026-09-09 |
| 7 | Hardening and evidence | merged | PASS 2026-09-09 (first round) | 2026-09-09 |
| 8 | README, DECISIONS, pre-push gate, deploy | merged | 2 FAIL then orchestrator fix, gate green 2026-09-09 | 2026-09-09 |

Status values: pending / in progress / awaiting verification / verified / merged.
Verifier column records PASS with date, or the fail count before PASS.

## Stage reports
(One section per stage, written by the implementing subagent: built, DoD evidence, deviations, open questions.)

### Stage 0: Scaffold (orchestrator, 2026-09-08)
Built: Vite 7 + Preact 10 + TS 5.9 strict (exactOptionalPropertyTypes, noUncheckedIndexedAccess); ESLint 10 flat config with typescript-eslint strictTypeChecked and security selectors (no dangerouslySetInnerHTML/innerHTML/eval, no em-dash, no-empty, no-floating-promises, no-console, no TODO in code); Prettier; Vitest 4 + jsdom + fake-indexeddb with 100% thresholds on src/domain; Playwright 1.63 Pixel 5 profile against `vite preview` of the production build; vite-plugin-pwa injectManifest with `src/sw.ts` (Workbox precache, navigation fallback, SKIP_WAITING message, notificationclick focus/open); `index.html` with the section 7 CSP; icons generated by `scripts/icons.mjs` (pure Node PNG writer); `ci.yml` (lint, typecheck, unit+coverage, build, e2e) and `pages.yml` (workflow_run on CI success for main, VITE_BASE_PATH=/wardbelt/); MIT LICENSE; `.gitignore`; `tests/fixtures/synthetic.ts`. Shared contract for stages 1-4: `src/domain/types.ts` (Patient, Task, Event, Settings, State, Action union, TransferFile) and `src/domain/template.ts` (19 steps, codes, phases).

DoD evidence (actual runs, 2026-09-08):
- `npm run lint`: eslint 0 problems; prettier "All matched files use Prettier code style!"
- `npm run typecheck`: both tsconfig projects clean.
- `npm test`: 1 file, 4 tests passed; coverage src/domain 100% statements/branches/functions/lines (17/17, 0/0, 3/3, 17/17).
- `npm run build`: dist/index.html 1.27 kB, assets/index 10.92 kB (4.61 kB gzip), sw.js precache 10 entries; no inline script in dist/index.html (CSP compatible).
- `npm run test:e2e`: 2 passed (heading visible; manifest.webmanifest and sw.js served).
- `actionlint`: no findings on ci.yml and pages.yml.
- `npm audit`: 0 vulnerabilities. `gitleaks detect`: no leaks.

Deviations: see Decisions (dependency versions and the extra dev packages).
Open questions: none.

### Stage 1: Domain core (implementing subagent, 2026-09-08, commit 930b150 on stage/1-domain)
Built:
All under /Users/misbahshah/code/wardbelt-wt/stage-1-domain (branch stage/1-domain), pure TypeScript, no I/O, no Date.now():
- src/domain/time.ts: toMs / toIso / addMinutes, MINUTE_MS, DAY_MS; the only ISO-to-ms conversion point (UTC Z form).
- src/domain/recovery.ts: scheduleChecks(tasks, returnedAt) sets dueAt on check_1..4 at exactly +15/30/45/60 min in ms via CHECK_OFFSETS_MIN; clearChecks removes them; other tasks keep object identity.
- src/domain/patient.ts: createPatient, currentTask (min-order todo), isBeltComplete, completeTask/skipTask (todo only, any order, doneAt on both; in_theatre completion sets theatreReturnAt + schedules checks; discharge completion discharges), addCustomTask (insert after / at end, phase inherited or PRE_OP when first, O(n) renumber, duplicate id no-op), setNote (task note with delete-on-empty, or patient notes), dischargePatient, revertTask. Same reference on every no-op; property order kept stable for byte-for-byte undo.
- src/domain/urgency.ts: DUE_SOON_MS, nextDue, overdueTasks, urgencyOf (ranks 0-3), urgencyKey + compareUrgency (strict total order: rank, dueAt, intake, createdAt, id), sortByUrgency (keys once per patient, one sort).
- src/domain/stats.ts: shiftBounds (04:00 local to next 04:00 local via local Date constructors), shiftStats (ShiftStats per section 6; two linear passes over events, undone events excluded, UNDO never counted, 3-minute grace, streak across mixed events, median with even-count averaging, O(k log k) sort justified in a comment).
- src/domain/reducer.ts: initialState, reduce (exhaustive switch with a never-typed runtime fallback), activePatients, dischargedPatients; every action per the stage brief; derived event ids `${eventId}:1`.
- tests/unit/domain/{helpers,time.test,recovery.test,patient.test,urgency.test,stats.test,reducer.test,properties.test}.ts: xorshift32 PRNG, deepFreeze purity guard, unit tests for every branch, and the four property suites (a)-(d) with 33 seeded cases.

DoD evidence (actual runs):
Full gate `npm run lint && npm run typecheck && npm test && npm run build` exit 0 (log at scratchpad/gate.log):
- lint: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!"
- typecheck: tsconfig.json and tsconfig.sw.json both clean.
- npm test (vitest run --coverage): 9 files, 125 tests passed, 2.1 s. Coverage summary: Statements 100% (548/548), Branches 100% (406/406), Functions 100% (108/108), Lines 100% (532/532). Per file (lines/branches): patient.ts 64/64, 55/55; recovery.ts 9/9, 6/6; reducer.ts 120/120, 98/98; stats.ts 56/56, 32/32; urgency.ts 38/38, 35/35; time.ts 5/5; template.ts, types.ts, validate.ts unchanged at 100%. Existing validate and scaffold tests still pass (24 of the 125).
- Property suites all green: (a) 12 seeds x 150-300 random actions incl. invalid targets, deep-frozen states, invariants: exactly one current task or complete belt, orders 0..n-1, task ids unique, status/dischargedAt/discharge-task consistency, event ids unique; (b) 8 seeds x 3 snapshot states x every patient, N random complete/skip then N undos, JSON.stringify equality and event count; (c) 5 seeds x 40 random return times with odd milliseconds, both via completeTask(in_theatre) and SET_THEATRE_RETURN, delta exactly 15/30/45/60 x 60000 ms; (d) 8 seeds, two shuffles plus original sort identical, comparator antisymmetric, transitive, and never 0 for distinct keys.
- Unit tests cover: 3-minute grace at exactly +3:00 (on time) and +3:00.001 (late); undone events excluded and UNDO itself never counted; streak across mixed and undone events; median odd (100), even (65), single, none; shift boundary 03:59:59 vs 04:00:00 local and year rollover; urgency ranks incl. exact due time and exact DUE_SOON_MS edges and every tiebreak level; every reducer branch incl. malformed timestamps and an unknown action type.
- npm run build: dist/index.html 1.27 kB (0.57 kB gzip), dist/assets/index 10.92 kB (4.61 kB gzip), sw.mjs 17.07 kB (5.73 kB gzip), precache 10 entries. Domain code is not yet imported by main.tsx so the bundle is unchanged from stage 0.
- e2e not run (stages 1-3 must not, per the common brief).

Deviations: 1. revertTask clears theatreReturnAt and check dueAts only when the reverted in_theatre task was done, not when it was skipped (brief said "if key is in_theatre"): a skip never set them, so reverting a skip must not remove a separately recorded theatre return; this keeps undo byte-for-byte in that case. 2. UNDO of a discharge completion appends two UNDO events (`eventId` naming the TASK_COMPLETED and `${eventId}:1` naming the derived DISCHARGED), so stats' "excluding undone events" rule also nullifies the discharge count; the brief said "append UNDO { undoOf }" (singular). 3. Actions carrying an unparseable `at`, `returnedAt` or ADD_TASK `dueAt` return the same state instead of throwing (toISOString would throw on NaN); not in the brief, added for the malformed-input case. 4. UNDO still appends its UNDO event when the target's task is already unfinished (e.g. after an IMPORT replaced the record) so undo cannot get stuck on the same event; the brief's "invalid transition returns same state" was read as applying to complete/skip. Everything else follows PLAN.md and the stage brief exactly.

Open questions: 1. Undo after a manual SET_THEATRE_RETURN: if the nurse records a theatre return manually while in_theatre is still unfinished, then completes in_theatre (which overwrites theatreReturnAt per the brief), an UNDO removes theatreReturnAt entirely rather than restoring the manual value; event history would be needed to restore it. Byte-for-byte undo therefore holds for every state except that one ordering; property (b) excludes SET_THEATRE_RETURN from snapshot generation for that reason. Stage 5 may want the UI to prefer COMPLETE_TASK(in_theatre) over SET_THEATRE_RETURN for the normal flow. 2. A DISCHARGE action (as opposed to completing the discharge cell) leaves no TASK_COMPLETED event, so a following UNDO reverts the previous completion rather than the discharge, exactly as PLAN.md section 6 defines undo ("last completion/skip"); stage 5 should decide whether the discharge button offers undo. 3. shiftBounds is tested with local Date constructors on the build machine's zone; a DST-transition day cannot be forced in-test without changing process TZ, which the tests deliberately avoid.

Verifier (independent, 2026-09-08): PASS on first round. Gate re-run green; 66 probe tests across 7 time zones (UTC, New York, Auckland, Kolkata, Chatham, London, Santiago) all pass; probes covered identity on 25 no-op and malformed reducer cases, byte-for-byte revert for every template task, DST and leap-day check scheduling, 200 custom tasks, unicode notes, urgency total order. Notes for later stages: UNDO of a completed discharge appends two UNDO events (eventId and eventId:1) so the derived DISCHARGED event is nullified too; the DISCHARGE action emits only DISCHARGED and cannot be undone by UNDO, so the stage 5 UI should route the discharge button through COMPLETE_TASK on the discharge task; reduce() is only defined for well-typed Action objects.

### Stage 2: Store (implementing subagent, 2026-09-08, commits 88f0d64 and 865a587 on stage/2-store)
Built:
Branch stage/2-store, worktree /Users/misbahshah/code/wardbelt-wt/stage-2-store.
- src/store/db.ts: DB_NAME 'wardbelt', DB_VERSION 1, typed idb DBSchema (patients keyPath id + index status; events keyPath id + index at; settings keyPath key, rows {key, value}); MIGRATIONS map of versioned steps and `upgrade(db, old, new)` applying old+1..new incrementally (throws on a missing step, which aborts and rolls back the open); `openDatabase(factory, name, version)` issues the request on an injected IDBFactory and uses idb's `wrap` for the promise API; `blocked` rejects at once with BlockedError (no retry loop) and a late success closes the stray connection; `versionchange` closes the connection so another tab's upgrade is never blocked.
- src/store/repo.ts: exports StorageNotice, LoadResult, Repo, RepoOptions, DEFAULT_RETRY_DELAY_MS, `openRepo` (never rejects) exactly per the brief. One RepoImpl over two backends (IdbBackend, MemoryBackend). Writes are serialised through one promise chain; each write tries once, waits retryDelayMs via the injectable `wait`, retries once; a second failure emits write_failed and queues the write; the queue is flushed in submission order when the next write is submitted, then `recovered` is emitted. `load()` validates every row with validatePatientRecord / validateEvent / validateSettings, skips and counts corrupt rows, keeps their raw values, sorts events by (at, id), assembles settings from rows with defaults for missing fields, emits `corrupt` when count > 0. `unavailable` is replayed to late subscribers. `close()` closes the db.
- src/store/transfer.ts: buildExport (copies records), serialiseExport (pretty JSON), parseImport (UTF-8 byte cap then validateImport), exportRawStore (JSON envelope with the raw corrupt rows).
- tests/unit/store/helpers.ts: per-test fake IDBFactory, request-like EventTarget factories for blocked/error/success, a factory that throws, `failNextPuts` (fails the next N object-store puts through fake-indexeddb's real request/transaction error path, or sync throw), notice/wait harness, raw open/put/getAll helpers for seeding corrupt rows.
- tests/unit/store/db.test.ts (11), repo.test.ts (28), transfer.test.ts (29): schema and index creation, reopen keeps records, real blocked (held v1 connection vs v2 open), late-success close, request error, VersionError, versionchange self-close, migration loop and abort rollback; round trip through reopen, event ordering, overwrite, deletePatient scope, clearAll, settings row per field and row removal, writes after close surface; fallback for blocked / error / VersionError / sync throw (Error and non-Error values) / indexedDB undefined / migration abort with exact notices, replay on subscribe and every method working in memory; corrupt patient, event (including an event with no `at`), settings rows; retry-once success, fail-twice queue + flush + recovered with same-record ordering, backlog across repeated failures, sync-throw path, concurrent write serialisation, default 250 ms real timer, listener error propagation; 21 malformed import variants (each rejected whole, store unchanged), export/parse deep-equal round trip, non-aliasing, unknown-field drop, file exactly at the byte cap, exportRawStore.

DoD evidence (actual runs, after fixes):
All run in /Users/misbahshah/code/wardbelt-wt/stage-2-store after the fix (no e2e, as instructed):
- `npm run lint`: eslint 0 problems (max-warnings 0); prettier "All matched files use Prettier code style!"
- `npm run typecheck`: `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.sw.json --noEmit` clean, no output.
- `npm test` (vitest run --coverage): Test Files 5 passed (5); Tests 96 passed (96) (was 91; 5 new regression tests); no unhandled errors. Coverage all files: statements 100% (454/454), branches 98.85% (258/261), functions 100% (101/101), lines 100% (443/443); src/domain thresholds (100%) satisfied. src/store: 100% statements/functions/lines, 96.29% branches; the only uncovered branches are pre-existing and noted by the verifier (db.ts:70 `newVersion ?? version`, repo.ts:259 equal-id sort tie); the new `inTransaction` is fully covered on both the abort and the already-aborted path.
- `npm run build`: dist/index.html 1.27 kB (gzip 0.57), dist/assets/index-C5IxCUWh.js 10.92 kB (gzip 4.61), dist/sw.mjs 17.07 kB (gzip 5.73), precache 10 entries (16.69 KiB), built without errors.
- Regression check: with src/store/repo.ts stashed to the pre-fix version, the same test file gives 5 failed | 27 passed, the five failures being exactly the new tests; restored, 32/32 pass.
- Verifier's original probe (sync throw in cursor loop): before the fix load() returned 0 patients and 4 events plus 2 Vitest "Unhandled Rejection: AbortError" errors; after the fix the equivalent test asserts 1 patient and 4 events intact, `write_failed` with reason containing "QuotaExceededError: quota", one wait of 5 ms, then `recovered` and 0 events after the flush, with zero unhandled rejections.
- `git status --porcelain` empty after commit and push.

Deviations: 1. Events are read with getAll() on the events store and sorted by (at, id) in the repo rather than read through the `at` index. Reason: an index cannot see rows whose `at` is not a valid key, so index-only reads would make exactly those corrupt rows invisible to the corrupt count and rawCorrupt; the sort reproduces index order at O(n log n). The index still exists per the binding schema and is tested.
2. Repo write methods resolve (never reject) after a double failure; the failure surfaces as the write_failed notice and the write stays queued. The brief's interface says Promise<void> and section 8 says the action is already applied in memory, so a rejection would give stage 5 nothing extra to do. A listener that throws does propagate to the caller of the write.
3. `load()` propagates a read failure (rejects) rather than switching modes; section 8 lists no boot-read failure mode, and silently flipping `mode` after open would be incoherent. Stage 5 should catch it and show the storage banner.
Everything else matches PLAN.md sections 5, 7, 8 and the stage brief.
Fix round: Fixed both verifier failures plus one related latent defect the verifier's own probe exposed, all in /Users/misbahshah/code/wardbelt-wt/stage-2-store/src/store/repo.ts.

1. Partial commit on a synchronous throw mid-transaction (verifier finding 1). Added `inTransaction(tx, body)`: runs the body, and on any failure aborts the transaction when `tx.error === null` (a failed request has already aborted itself and set `error`; a throw from anywhere else leaves the transaction idle and about to auto-commit half-applied), observes `tx.done`, and rethrows the body's error. `deletePatient`, `putSettings`, `clear` and `readAll` now run through it, so the patient delete, the settings clear and the first store clear are all rolled back when a later step fails, and the write is queued for retry exactly as before.

2. Unhandled rejection of idb's eagerly created `tx.done` (found while reproducing finding 1: the verifier's probe produced two "Unhandled Rejection: AbortError" errors in Vitest). `inTransaction` always observes `done`, and requests inside one transaction are now awaited one at a time (at most six per transaction) because idb v8 does not attach a handler to request promises: a request left pending when a sibling threw synchronously would reject unobserved on abort.

3. `__proto__` settings row leaking through the prototype chain (verifier finding 2). `load()` now copies only keys in `KNOWN_SETTINGS_KEYS` (a Set built from `SETTINGS_KEYS`) into the assembled record; unknown keys, `__proto__` and `constructor` included, are dropped rather than assigned, matching PLAN.md section 7 "unknown fields dropped" and the existing `futureField` behaviour (not counted as corrupt).

Regression tests (tests/unit/store/repo.test.ts, new describe 'multi-request transactions are atomic' plus one case under 'load with corrupt rows'): deletePatient rolled back entirely on a synchronous cursor-delete throw and on a cursor-delete request error (both then flushed on the next write with `recovered`); saveSettings keeps previous rows when a put throws after the clear; clearAll leaves every store intact when a clear fails, then applies on flush; `__proto__`/`constructor` settings rows dropped with no prototype leak, corrupt 0. Each transaction test also asserts no unhandled rejection via a new `unhandledRejectionsDuring` helper. tests/unit/store/helpers.ts: extracted `erroringRequest` (genuine fake-indexeddb request that errors and aborts), added `failNextClears`, `failNextCursorDeletes`, `unhandledRejectionsDuring`. Verified all five new tests fail against the previous repo.ts (git stash) and pass with the fix. Removed the verifier's untracked scratch tests/unit/store/probe.test.ts (never committed; it used console.log and would fail lint).

Open questions: 1. Domain defect outside my ownership (src/domain/validate.ts, stage 0/1): `isTaskKey` uses `u in TEMPLATE_BY_KEY`, and TEMPLATE_BY_KEY is a plain object with Object.prototype, so inherited names pass as task keys. Verified in-session: validatePatientRecord accepts a task with key 'constructor' and validateEvent accepts taskKey '__proto__'. Fix is `Object.hasOwn(TEMPLATE_BY_KEY, u)`; the import boundary then rejects such records. My __proto__ import variants target other fields, so they pass regardless.
2. Stage 5 wiring notes: subscribe before load() to receive the corrupt notice; catch load() rejection into the storage banner; the repo never rejects writes on storage failure, but a throwing notice listener does propagate to the write's caller.
3. The typescript-eslint stylistic rule non-nullable-type-assertion-style asks for `!` while no-non-null-assertion forbids it; tests use a guarded accessor instead. Not blocking, but future stages will hit the same pair. The verifier's notes (a) to (h) describe implementer decisions rather than failures and are unchanged by this fix: load() reads events from the store and sorts by (at, id) rather than using the `at` index so rows with an invalid `at` are still counted as corrupt; corrupt settings count as one logical record; queued writes flush ahead of the newly submitted write; write methods resolve rather than reject on storage failure (notice is the channel); load() rejects on a read transaction failure (stage 5 must catch this at boot); openRepo has no timeout if the factory never fires an event; appendEvent uses put so a duplicate event id overwrites; exportRawStore uses JSON.stringify (throws on BigInt, flattens Map). Stage 2 report is returned here rather than written to STATE.md, per the common brief.

Verifier (independent, 2026-09-08): round 1 FAIL with 2 findings (minor: deletePatient commits a partial delete when the cursor loop throws synchronously (patient removed, its events orphaned); minor: A settings row keyed `__proto__` injects fields into loaded settings via the prototype chain instead of being ignored or counted corrupt). Round 2 PASS after fixes. Probes: DoD gate from scratch on 865a587: npm run lint (eslint 0 problems, prettier clean, exit 0), npm run typecheck (both projects clean), npm test (5 files, 96 tests passed; src/store 100% stmts/lines/funcs, 96.3% branches; domain thresholds met), npm run build (dist + sw.js, 10 precache entries). No unhandled rejections reported. API shape: type-level bidirectional equality (Equals<A,B>) of StorageNotice, LoadResult, Repo, RepoOptions, openRepo, buildExport, serialiseExport, parseImport, exportRawStore against the brief's declarations; tsc clean. PASS. Schema: raw factory inspection after openRepo shows version 1, stores patients(id, index status), events(id, index at), settings(key, no indexes); reopen keeps records; two connections at v1 both read/write without blocking. PASS. Retry once: put fails once (genuine fake-indexeddb request error) then succeeds: wait called exactly once with the injected retryDelayMs (123), no notice, data present. PASS. Genuine internals-free failure: put diverted to add so a real ConstraintError fires; twice -> write_failed containing 'ConstraintError', old record intact, next write flushes and emits recovered. PASS. Quota path on every write method (savePatient, appendEvent, saveSettings via put and via clear, clearAll, deletePatient): each yields exactly one {kind:'write_failed', reason} with 'QuotaExceededError', one wait, zero unhandled rejections, and rollback of the multi-request transaction. PASS. Head-of-line queue: W1 and W2 both fail twic
Verifier notes: Decisions the implementer made where the brief was silent or where it deviated with reasoning (none change observable behaviour against the DoD):
1. Queue flush order: when a backlog exists, the backlog is attempted first and the new write only after it, so a still-failing head write also queues the new write (with a second write_failed whose reason describes the head write). PLAN.md section 8 reads more like 'attempt the new write; on success flush the queue'. The implementer's ordering guarantees a retried write never lands after a newer write to the same record, and nothing is lost either way. Stage 5 should expect possibly repeated write_failed notices while storage is broken, and a single recovered when it drains.
2. Events are read from the object store and sorted by (at, id) in O(n log n) rather than via the at index, because rows whose at is not a valid key are invisible to the index and are exactly the corrupt rows that must be counted. Order matches index order for valid rows.
3. Settings are treated as one logical record: any bad row (or any row without a string key) makes the whole record fall back to DEFAULT_SETTINGS, counts as 1 corrupt, and pushes every settings row (valid ones included) into rawCorrupt. So corrupt can be less than rawCorrupt.length; the UI should display count and offer rawCorrupt without assuming equality.
4. load() rejects (rather than resolving with a notice) when the read transaction fails or the connection is closed (InvalidStateError). S

### Stage 3: Scheduler (implementing subagent, 2026-09-08, commits 92a3d9a and 275f6ca on stage/3-scheduler)
Built:
/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler on branch stage/3-scheduler:
- src/scheduler/clock.ts: `Clock` interface, `realClock` (Date.now, window.setTimeout/clearTimeout), `createFakeClock(startMs)` with `advance` (fires due callbacks in (due, arm-order) sequence, sets now to each due time before calling, handles timers armed during the advance), `set` (jump either way, fires nothing), `pending`. Sorted-insert array, O(log n) search per arm; negative/NaN delays clamp to 0; a throwing callback propagates and leaves the rest armed.
- src/scheduler/timers.ts: `DueTask`, `Notifier`, `TimersOptions`, `Timers`, `DEFAULT_MAX_DELAY_MS`, `createTimers`. One timer at a time (`arm` always clears first, so re-entrant `reschedule` from a dispatch stays single); sweep dispatches TICK, collects newly due todo tasks of active patients not yet announced at their current dueAt (Map taskId -> dueAt, rebuilt each sweep so a task drops out when done/skipped/removed or its dueAt moves), dispatches one DUE each (most overdue first), one `notifier.due` when non-empty; delay = clamp(earliest unannounced due - now, 0, max) or max when none; `onVisible` sweeps immediately then re-arms if running; `start`/`stop` idempotent; fire re-arms in `finally`; unparseable dueAt skipped; only ids, names and labels retained.
- src/scheduler/notify.ts: `PermissionState`, `NotifyDeps`, `NotifierApi`, `createNotifier`, `browserNotifyDeps`, plus `dueTitle`, `dueBody`, `DUE_TAG`, `DUE_VIBRATION`, `DUE_BODY_LINES`. `state()` reads the live permission ('unsupported' without a Notification API); `requestPermission` prompts only from 'default' and routes a rejecting prompt to onError; `due` vibrates [200,100,200] then, when granted, shows `${n} check(s) due` with up to 5 `${patientName}: ${label}` lines then `and N more`, tag 'wardbelt-due', renotify true; missing registration or rejecting showNotification/getRegistration route plain-English reasons to onError (default no-op). `browserNotifyDeps` feature-detects navigator.vibrate, navigator.serviceWorker.getRegistration and Notification without throwing.
- tests/unit/scheduler/clock.test.ts (11), timers.test.ts (25), notify.test.ts (19): fake clock only, no vi.useFakeTimers, async notifier paths awaited via promises resolved by the fakes.

DoD evidence (actual runs, after fixes):
All run in the worktree on 2026-09-08 after the fix:
- `npm run lint`: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!".
- `npm run typecheck`: tsc -p tsconfig.json and tsc -p tsconfig.sw.json both clean.
- `npm test` (vitest run --coverage): 5 files, 82 tests passed (scheduler: 58 across clock/notify/timers, up from 55). Coverage summary 100% statements (405/405), 100% branches (269/269), 100% functions (80/80), 100% lines (396/396). Per file: scheduler/clock.ts, notify.ts, timers.ts each 100/100/100/100; src/domain threshold met at 100%.
- `npm run build`: dist/index.html 1.27 kB (gzip 0.57), dist/assets/index 10.92 kB (gzip 4.61 kB), dist/sw.mjs 17.07 kB (gzip 5.73 kB), PWA precache 10 entries.
- Regression check: the three new timers tests run against the previous timers.ts (git show HEAD~1) fail (3 failed | 25 passed); against the fix all 28 pass.
- Verifier's real-timer trap reproduced: a temporary setup file replacing window.setTimeout and globalThis.setTimeout with a function throwing 'REAL TIMER USED', merged into vitest.config via a temporary config in the worktree; tests/unit/scheduler: 3 files, 58 passed, 0 failed. Temp files deleted before commit; `git status --porcelain` empty after push.
- e2e not run (stages 1 to 3 must not, per the common brief).

Deviations: None from PLAN.md. One from the brief's literal wording: the brief says delay = clamp(nextDue - now, 0, max) with nextDue the earliest dueAt over all todo tasks; taken literally an overdue task that has already been announced but is still todo (the nurse has not done the check yet) yields delay 0 forever and a busy re-arm loop. The delay therefore uses the earliest dueAt among tasks not yet announced at their current dueAt, while `nextDueAt()` keeps the literal meaning (earliest dueAt over todo tasks of active patients, overdue included). Tested ("announces a task added with a past dueAt on the next reschedule without a busy loop").
Fix round: Fixes after verification, branch stage/3-scheduler, commit 275f6ca (pushed).

/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler/src/scheduler/timers.ts (major fix): `notified` is now rebuilt from the current board on every arm as well as every sweep. A new `prune()` runs inside `arm()`: it walks the timed outstanding tasks once, keeps only entries whose task is still todo on an active patient with the same dueAt, and returns the earliest unannounced dueAt used for the delay (the same O(tasks) walk arm already did). A task completed then undone, or whose dueAt moves away and back, between two `reschedule()` calls with no tick in between is therefore re-announced on the next sweep. The three task walks (sweep, prune, nextDueAt) share one generator `timedTodo()` that yields active-patient todo tasks with a parseable dueAt, replacing the previous duplicated loops. Exported shapes unchanged. Decision-notes header updated to describe the new behaviour.

/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler/tests/unit/scheduler/timers.test.ts: three regression tests (all fail on the previous timers.ts, pass on the fix): complete then undo within a single tick window re-notifies (the verifier's exact repro, asserting two DUE actions at +30 s and +33 s and two batches, then no third); dueAt moved to D2 then back to D1 within a single tick window re-notifies at D1; an announced overdue task completed then undone is announced fresh on the next onVisible sweep.

/Users/misbahshah/code/wardbelt-wt/stage-3-scheduler/tests/unit/scheduler/clock.test.ts (minor fix): the realClock test no longer arms a real window.setTimeout; it spies on `window.setTimeout` / `window.clearTimeout` (target typed as plain `Window` so the DOM signature returning number is used rather than the Node overload) with inert mock implementations and asserts delegation of the callback, delay and id. vitest.config `restoreMocks: true` restores them after the test.

Open questions: For stage 5 wiring: call timers.start() then timers.onVisible() at boot (either order works), timers.reschedule() after every state change, timers.onVisible() on visibilitychange to visible, and timers.stop() in the read-only second tab; notifier.due is fire-and-forget, so pass onError from browserNotifyDeps() consumers through to the banner. For stage 6: the denied/unsupported explanation copy is Settings' to write from state(); no helper was added. Verifier note: the scheduler is not yet imported by main.tsx, so the bundle size is unchanged by this stage. Carried over from the verifier's notes, unchanged by this fix: stage 5 must not call onVisible() on the read-only second tab (BroadcastChannel lock) or that tab will dispatch DUE and vibrate in parallel with the primary tab; stage 5 should leave maxDelayMs at the default (a non-positive or NaN value would arm zero delays in a loop).

Verifier (independent, 2026-09-08): round 1 FAIL with 2 findings (major: Notified set is pruned only at sweep time, so a task that leaves todo and returns before the next tick is never re-notified (complete then undo within a tick); minor: realClock test arms a real window.setTimeout in a unit test). Round 2 PASS after fixes. Probes: Gate from scratch on 275f6ca: npm run lint (eslint + prettier), npm run typecheck (both tsconfig projects), npm test (82 tests, coverage 100/100/100/100 incl. src/scheduler), npm run build: all exit 0; e2e not run per instruction RE-PROBE prior failure 1: complete then undo between two reschedule() calls with no tick between -> 2 DUE, 2 batches, no further re-notify over 10 min, single timer (PASS, fixed by prune() in arm()) RE-PROBE prior failure 1 variants: dueAt moved D1->D2->D1 with no tick; skipped->todo; patient discharged->active; patient deleted->re-added, all within one tick window: each re-notifies exactly once (PASS) RE-PROBE prior failure 2: scheduler suite plus all probes run with globalThis/window setTimeout, setInterval, setImmediate, requestAnimationFrame, requestIdleCallback replaced by throwing stubs: 129 tests pass, no trip; positive control confirmed the trap fires on realClock.setTimeout (PASS, clock.test.ts now uses vi.spyOn mocks) API shape: type-level Equal<> assertions under tsc for Clock, FakeClock, DueTask, Notifier, TimersOptions, Timers, PermissionState, NotifyDeps, NotifierApi, createFakeClock, createTimers, createNotifier, browserNotifyDeps, realClock against the brief's declarations: all identical; negative control produced TS2344 so the assertions are live (PASS) Timers: task due exactly at start fires once at delay 0 then 60 s cadence, no busy loop (PASS) Timers: due at now+cap fires at cap; due at now+cap+1 fires at cap+1 after the 60 s TICK
Verifier notes: Both prior failures are fixed: arm() now calls prune(), which rebuilds the notified map from the live board on every reschedule() as well as every sweep, so complete/undo, skip/undo, dueAt move-and-back, discharge/re-activate and delete/re-add within a single tick window all re-notify exactly once; and the realClock test now delegates through vi.spyOn mocks instead of arming a real window timer. Decisions for the STATE.md log where the brief or PLAN.md was silent or literally unworkable: (1) the armed delay is computed from the earliest UNANNOUNCED todo task rather than the brief's literal clamp over all todo tasks, because the literal formula arms a 0 ms delay forever once an announced overdue check is left outstanding; nextDueAt() keeps the plain section 8 meaning (earliest outstanding due including announced ones). (2) realClock uses window.setTimeout rather than globalThis.setTimeout (identical in the browser; needed because @types/node makes globalThis.setTimeout return NodeJS.Timeout); realClock therefore requires a window and is not usable in the service worker, which nothing needs. (3) Newly due tasks are dispatched and listed most-overdue first (k log k over the newly due subset only, justified in the file header). (4) onVisible() runs the sweep and dispatches TICK/DUE and notifies even when stopped; only the re-arm is gated on running. Stage 5 must not call onVisible on the read-only second tab or it will vibrate and dispatch DUE in parallel with the primary tab. (5

### Stage 4: Visual system and components (implementing subagent, 2026-09-08, commits 655158b and aadadff on stage/4-ui)
Note: the implementing agent committed and pushed but was cut off by a usage limit before returning its report; a fresh agent reconstructed the report below from the branch and re-ran the gate.
Built:
Branch stage/4-ui, commit 655158b, 32 files, 4854 insertions, worktree /Users/misbahshah/code/wardbelt-wt/stage-4-ui. All paths below are relative to that worktree.

Visual system
- src/ui/tokens.css: the only place colours, spacing, type, radii and motion are defined. Light palette on :root (PLAN.md literals verbatim), dark palette duplicated under @media (prefers-color-scheme: dark) { :root:not([data-theme='light']) } and :root[data-theme='dark']. Dark accent #3FB48F, warning #E0A83A, danger #F08A80. Adds --colour-on-accent (#FFFFFF light, #121212 dark), --colour-warning-text (#8F5E10 light, same as warning in dark), --colour-hatch (ink at 30%), --colour-scrim, and size tokens --target 48px, --row-height 88px, --cell 24px, --cell-gap 4px, --phase-gap 8px, --outline-current 2px, --radius 0, --radius-cell 2px. Motion tokens --motion-fill 120ms, --motion-progress 200ms, --motion-sheet 200ms, --motion-sweep 400ms, --motion-pulse-period 2000ms, all zeroed under prefers-reduced-motion. Sets color-scheme per theme.
- src/ui/ui.css (821 lines): every component style from tokens only. Reset, .btn variants (primary, danger, quiet), .field/.field__input/.field__error with aria-invalid styling, .seg segmented radio group with visually hidden inputs, .chip tones, .belt/.belt__phase/.belt__square states (todo hairline border, current 2px accent border, done accent fill plus belt-fill 120ms scale-in, skipped repeating-linear-gradient hatch in --colour-hatch, overdue 2px danger border plus belt-pulse 2s keyframe flat from 0 to 84% and 100% with a scale(1.2) peak at 92%), .belt__hit 48px button with -12px margins around the 24px square, .row (88px: 8 + 48 header + 24 belt + 8), .row__progress 2px bar with 200ms width transition and progress-sweep 400ms on complete, .sheet fixed bottom sheet with scrim and 200ms slide plus .sheet--inline static variant, .task rows min 48px, .stats dl, .toggle, .banner, .toast, .gallery, and a global prefers-reduced-motion block zeroing animation and transition durations with !important.

Components (Preact, props in, callbacks out, no store or scheduler imports, no Date.now)
- src/ui/format.ts: pure display helpers shared by components: SPECIES_LABEL, taskCode (template code or customCode), cellState, isOverdue (todo with dueAt <= now), cellClass, formatCountdown (mm:ss, minutes grow past 59), formatClock (local HH:MM), toDatetimeLocal, formatMinutes, formatPercent, progressOf (done plus skipped over total, complete flag), classes joiner.
- src/ui/theme.ts: applyTheme(theme) writes or removes data-theme on <html>; readThemeParam() reads ?theme= from the search string or after the hash.
- src/ui/Belt.tsx: props { tasks; currentTaskId?; now; onTapCurrent?; compact? }. One pass groups contiguous phases; role="group" with aria-label "N of M done, current: <label>" or "nothing left to do"; only the current cell is a <button class="belt__hit"> (aria-label "Complete <label>" plus " (overdue)"), other cells are aria-hidden spans; belt gets tabIndex 0 when no current task so the scroller stays keyboard reachable.
- src/ui/PatientRow.tsx: props { patient; now; currentTaskId?; urgency; nextDue?; onCompleteCurrent; onOpen }. Exports Urgency and NextDue types. 48px header button (name 16/600, species, mono kennel, muted procedure), TimerChip from nextDue and now ("C1 overdue 05:00" danger, "BA in 25:00" warning, with a visually hidden task label), intake chip (accent when urgency is intake), Belt, aria-hidden progress bar with data-urgency on the article.
- src/ui/Sheet.tsx: role="dialog", aria-modal="true", aria-labelledby title, tabIndex -1 panel, Escape on the panel closes, scrim click closes, 48px Close button, focus moves to initialFocus or the panel on open and returns to the previously focused element on close. inline prop renders static with no scrim, no aria-modal, no focus management (for the gallery).
- src/ui/PatientSheet.tsx: props per brief plus inline. Details section (procedure, species, breed, sex, weight, kennel, intake, admitted time, owner tel: link with spaces stripped). Task list: each li min 48px with cell, label, status text (Done HH:MM, Skipped, Due HH:MM in danger when overdue, Current, To do), Custom marker, note; Done and Skip buttons for todo tasks (aria-labels "Complete X" and "Skip X"); per-task Note toggle button (aria-expanded) opening a 500-char textarea saved on blur through a last-saved ref. Undo button disabled when !canUndo. AddTaskForm: label (maxLength 60), datetime-local due, Insert after select defaulting to current task or last task, validateCustomTask(input, now) at submit, inline errors with aria-invalid and aria-describedby, form reset on success. Patient notes textarea (500) saved on blur or Save notes without duplicate calls. Theatre return datetime-local parsed with parseIso, inline error when empty. Discharge button (disabled and showing "Discharged HH:MM" once discharged). Delete via ConfirmButton.
- src/ui/ConfirmButton.tsx: reusable inline two-step confirm; first tap arms a "<confirmLabel>" danger button plus a "Keep" button for windowMs (CONFIRM_WINDOW_MS 4000), timer cleared on disarm and unmount.
- src/ui/AddPatientSheet.tsx: props { showOwnerPhone; onSubmit; onClose; inline?; initialErrors? }. Uncontrolled inputs read via FormData at submit and validated once with validatePatientForm; per-field errors with aria-invalid and aria-describedby; form-level error as role="alert". Fields: Name (40, aria-required), Species seg radio (default dog), Procedure (80, aria-required), Intake slot seg radio (default none), Kennel (10, mono), Sex seg radio (default unknown, labels M/MN/F/FN/Unknown), Breed (40), Weight in kg (maxLength 7, inputmode decimal, mono), Owner phone (20, inputmode tel, mono, only when showOwnerPhone), Notes textarea (500). Name autofocused through Sheet initialFocus. Add patient submit and Cancel buttons.
- src/ui/ShiftSummary.tsx: props { stats; shiftLabel; onClose; inline? }. dl of seven stats; null shown as "n/a"; percent rounded; minutes as "5 h 12 min".
- src/ui/Settings.tsx: props per brief plus inline. Exports NotificationState, StorageMode, NOTIFICATION_HINT, STORAGE_LINE. Toggles (checkbox in a 48px label with hint via aria-describedby) for Notifications (disabled and unchecked when denied or unsupported; turning on in default state also calls onRequestNotifications), Click on completion, Show owner phone field. Theme seg radio. Purge days number input with 1 to 365 inline validation then onChange({ purgeDays }). "Purge discharged older than N days" button. Export button; Import button clicking a hidden file input (accept application/json,.json) read with file.text() or FileReader fallback then onImportText(text), unreadable file shows a role="alert". Last export line. Delete everything via ConfirmButton. Storage mode and version lines.
- src/ui/Banner.tsx: role="status" bar with tone (info, warning, danger changes the hairline only), optional primary action and Dismiss.
- src/ui/Toast.tsx: role="status", one setTimeout for onExpire clamped to TOAST_MAX_MS 4000 and cleared on unmount, optional Undo button, inline prop.
- src/ui/Chip.tsx: tone (neutral, accent, warning, danger) and mono classes.
- src/ui/feedback.ts: completionFeedback(opts) vibrate(30); beltCompleteFeedback(opts) vibrate([30, 40, 30]); both feature-detect navigator.vibrate and AudioContext, play a 40 ms 880 Hz oscillator click at gain 0.08 when sound, create the AudioContext lazily and resume() it if suspended, report every failure through optional onError and never throw, return { animate: !reducedMotion, vibrated }. prefersReducedMotion() via matchMedia with feature detection. resetAudioForTests() seam.
- src/ui/DevGallery.tsx: the #/dev route. Sections with ids belt, rows, sheet, admit, summary, settings, banner, toast, chips, each with an h2. Six fixture variants (fresh, pre-op with skip, in theatre, recovery with overdue, custom task due soon, complete and discharged) with hard-coded current task ids and urgencies; PatientSheet twice (canUndo true and false); AddPatientSheet with phone, without phone, and submitted-empty via initialErrors computed from validatePatientForm; ShiftSummary with numbers and with nulls; Settings for granted, denied, default, unsupported on idb plus granted on memory; three Banners; Toast with and without Undo; chip tones. Theme switch (System, Light, Dark with aria-pressed) applying data-theme, preselected from ?theme= in search or hash query.
- src/main.tsx: imports tokens.css and ui.css; if location.hash starts with #/dev, dynamic-imports DevGallery (fallback role="alert" paragraph if the chunk fails), else renders the stage 0 Shell; mount error kept.

Tests
- tests/unit/ui/contrast.test.ts: parses tokens.css with node:fs, own relative luminance formula, asserts light literals equal PLAN.md, dark literals, both dark blocks identical, 4.5:1 for ink, ink-muted, accent, danger and warning-text against bg and surface in both themes, 3:1 for warning, on-accent vs accent 4.5:1, documents that #B7791F on #FAFAF9 and white on #3FB48F both fail 4.5:1, every motion token 400 ms or under except the 2000 ms pulse period and all zeroed under reduced motion.
- tests/unit/ui/no-literal-colours.test.ts: every .css, .tsx, .ts under src/ui except tokens.css has no hex, rgb() or hsl() literal.
- tests/unit/ui/belt.test.tsx, patient-row.test.tsx, sheet.test.tsx, patient-sheet.test.tsx, add-patient-sheet.test.tsx, shift-summary.test.tsx, settings.test.tsx, banner-toast-chip.test.tsx, feedback.test.ts, format.test.ts, dev-gallery.test.tsx: render every state, fire every callback with the right arguments, two-step delete under fake timers, Toast expiry and 4 s clamp under fake timers, AddPatientSheet shows every section 7 error and submits normalised forms, feedback vibrates only when present and never throws without AudioContext.
- tests/e2e/dev-gallery.spec.ts (Pixel 5, production build): axe wcag2a/wcag2aa/wcag21aa clean in light, dark via ?theme=dark and dark via emulateMedia; body background rgb(250, 250, 249) light and rgb(18, 18, 18) dark; every visible button, a[href], input, select, textarea, [role=button] at least 48 x 48 (radios and checkboxes measured through their label, more than 200 elements measured); current belt cell is a 48 px button around a 24 px square; six rows at exactly 88 px; reduced motion gives 0s animation and transition durations on done cell, overdue cell and progress fill; done animation 120 ms and sweep 400 ms when motion is allowed; scrollWidth <= clientWidth.

DoD evidence (actual runs, after fixes):
Fresh runs in /Users/misbahshah/code/wardbelt-wt/stage-4-ui at commit aadadff (2026-09-08):
- npm run lint: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!"
- npm run typecheck: tsc -p tsconfig.json and tsconfig.sw.json both clean.
- npm test (vitest run --coverage): 15 files, 141 tests passed (was 135). All-files coverage 99.14% statements (577/582), 96.19% branches (455/473), 98.66% functions (148/150), 99.3% lines (572/576). src/domain from coverage/coverage-summary.json: 3 files, 241/241 lines, 241/241 statements, 180/180 branches, 35/35 functions = 100% on every metric (threshold gate passed).
- npm run build: dist/index.html 1.35 kB; assets/index CSS 13.77 kB (2.95 kB gzip); assets/index JS 12.50 kB (5.35 kB gzip); DevGallery chunk 36.24 kB (11.61 kB gzip, lazy, dev route only); sw.mjs 17.07 kB (5.73 kB gzip); precache 12 entries (67.15 KiB).
- npm run test:e2e (Pixel 5, production build via vite preview): 14 passed (13.6 s), 0 failed: axe clean light / ?theme=dark / prefers-color-scheme dark; every visible interactive element >= 48 x 48 (measured > 200, including the new tel: anchor); current belt cell 48 px button around 24 px square; rows 88 px; timer chip spacing; header chips inside header and clear of belt; sheet owner phone / note / discharged; reduced motion 0s; motion 120 ms fill and 400 ms sweep; no horizontal scroll; smoke spec 2 passed.
- Regression check: with src/ui stashed and the new tests kept, vitest reported 6 failed / 30 passed across the four touched test files; restored, all pass.
- Visual check: Playwright screenshot of #rows shows "C1 overdue 05:00" and "BA in 25:00" with spaces, both stacked chips inside the header, belt cells clear below.
- git status --porcelain empty after commit; pushed 655158b..aadadff to origin/stage/4-ui.

Deviations: 1. Light warning text held to 3:1, not 4.5:1. The brief asked the contrast test to assert >= 4.5:1 for warning against background and surface in both themes, but the binding PLAN.md literal #B7791F is 3.49:1 on #FAFAF9 and 3.64:1 on #FFFFFF. The implementer kept the literal, held it to the WCAG 1.4.11 non-text floor of 3:1 (borders, bars, outlines) and added a new token --colour-warning-text (#8F5E10, 5.32:1 and 5.56:1) for warning-toned text such as the due-soon timer chip. In dark the two warning tokens are the same value (#E0A83A). contrast.test.ts asserts exactly this arrangement and includes a test documenting that #B7791F cannot carry text at 4.5:1.
2. Dark done-cell glyph is #121212, not white. PLAN.md section 10 says "done fill ... with white glyph"; white on the lightened dark accent #3FB48F is 2.58:1, so --colour-on-accent is #121212 in dark (7.26:1) and #FFFFFF in light (6.20:1). Pre-authorised by the brief; recorded here as the decision it asked for.
3. Overdue pulse is a scale transform, not an outline pulse, and the keyframe iterates infinitely. PLAN.md says "danger outline pulsing once per 2 s (no continuous animation)". The cell carries a constant 2 px danger border and belt-pulse runs infinite with a 2000 ms period that is flat from 0 to 84% and 100%, peaking at scale(1.2) at 92% (about 320 ms of motion per cycle). The contrast test excludes the 2000 ms pulse-period token from the 400 ms cap; ui.css forces animation-iteration-count 1 and 0s durations under reduced motion.
4. Visible per-task action button says "Done" rather than "Complete" (aria-label is "Complete <task>"). Brief wording was "per-row Complete and Skip buttons".
5. Two-step confirm shows the "Confirm delete" button in place of the original button alongside a "Keep" button, rather than a second button appearing next to the original. Behaviour (4 s window, no window.confirm) matches the brief.
6. Extra props beyond the brief: inline on Sheet, PatientSheet, AddPatientSheet, ShiftSummary, Settings and Toast; initialErrors on AddPatientSheet; initialFocus on Sheet; windowMs and disabled on ConfirmButton. onAddTask takes CustomTaskInput from src/domain/validate.ts, which is the same shape as the brief's { label; dueAt? }. completionFeedback and beltCompleteFeedback return { animate, vibrated } (brief specified no return value).
7. Extra components and modules not named in the brief: ConfirmButton.tsx, format.ts, theme.ts, and an extra "Chips" gallery section.
8. main.tsx loads DevGallery through a dynamic import (separate 35.88 kB chunk) instead of a static import, with a role="alert" fallback if the chunk fails to load.
9. AddPatientSheet uses aria-required rather than the HTML required attribute (form is noValidate so validatePatientForm is the single validator), and gives the weight input maxLength 7, a value not in section 7.
10. Settings validates purge days (integer 1 to 365) in the UI with an inline error before calling onChange({ purgeDays }); section 7 does not list settings as a validated entry point, so this is a UI-side range check beyond "nothing else validates".
11. body has overflow-x: hidden in ui.css in addition to the belt's own overflow-x: auto; the e2e test still measures documentElement.scrollWidth, which reports overflow regardless.
Fix round: Stage 4 verification fixes on branch stage/4-ui (worktree /Users/misbahshah/code/wardbelt-wt/stage-4-ui), commit aadadff.

1. Timer chip spacing (major). /Users/misbahshah/code/wardbelt-wt/stage-4-ui/src/ui/PatientRow.tsx: the code span and the " overdue mm:ss" / " in mm:ss" text now share one inline <span> inside the flex .chip, so the leading space is a normal inline space rather than a separate flex item whose whitespace collapses. Rendered text is now "C1 overdue 05:00" / "BA in 25:00" (confirmed by innerText and a Pixel 5 screenshot). Regression tests: tests/unit/ui/patient-row.test.tsx (visibleText helper asserts exactly one in-flow child element carrying "C1 overdue 05:00" / "BA in 25:00"); tests/e2e/dev-gallery.spec.ts "timer chips keep the space between code and countdown" (innerText contains the spaced string).

2. Toast timer restart (major). /Users/misbahshah/code/wardbelt-wt/stage-4-ui/src/ui/Toast.tsx: latest onExpire held in a useRef; the setTimeout effect depends only on durationMs, so a parent re-rendering every tick with a fresh callback no longer re-arms the auto-hide. Cleared on unmount as before; the decision note says a new toast should be keyed so it mounts fresh. Regression test: tests/unit/ui/banner-toast-chip.test.tsx "holds its deadline when a ticking parent re-renders with a new onExpire" (rerenders every 1 s with a new arrow; expires exactly once at 4 s, invoking the latest callback, never the first).

3. Header chips overflow (minor). /Users/misbahshah/code/wardbelt-wt/stage-4-ui/src/ui/tokens.css adds --chip-height: 20px; ui.css .chip uses it, so timer chip + 4 px gap + intake chip = 44 px fits the 48 px header. While measuring, found the .row__belt wrapper was displaced 12 px up into the header because the .belt scroller's negative margins collapsed through it (visually masked by min-height); .row__belt is now display: flex so margins cannot collapse, and the row's bottom padding is calc(8px - 1px hairline) so the row stays exactly 88 px border-box. Regression tests: tests/e2e/dev-gallery.spec.ts "header chips stay inside the 48 px header and clear of the belt" (per row: header 48 px, every .row__side .chip inside the header box, .row__belt and first .belt__square top >= header bottom; asserts at least one row has two chips); tests/unit/ui/contrast.test.ts token arithmetic (2 * chip-height + space-1 <= target; row-height = 2 * space-2 + target + cell).

4. Dev gallery PatientSheet states (minor). /Users/misbahshah/code/wardbelt-wt/stage-4-ui/src/ui/DevGallery.tsx renders four sheets: recovery with undo, recovery without undo, patientWithCustomTask() with a synthetic ownerPhone ('+44 0000 000000', same value as the FORM_CAT fixture) giving the tel: link and the task note, and patientDischarged() giving the disabled "Discharged HH:MM" button. Shared sheetHandlers object replaces the repeated noop props. Regression tests: tests/unit/ui/dev-gallery.test.tsx (4 dialogs; tel:+440000000000 href; .task__note text; exactly one disabled /^Discharged \d\d:\d\d$/ button and three enabled "Discharge" buttons); tests/e2e/dev-gallery.spec.ts "patient sheet shows owner phone, task note and discharged states" (tel anchor visible, >= 48 x 48, note text, discharged button disabled). The tel anchor is now also covered by the axe and 48 px target sweeps.

Test-only: dev-gallery.test.tsx theme test now scopes getByRole to the gallery header via within(); with four sheets the document-wide accessible-name walk in jsdom took 4.7 s (2.3 s before) and tripped the 5 s timeout; scoped it takes 0.28 s.

All six new/changed unit tests were confirmed to fail against the previous src/ui (git stash of src/ui only: 6 failed, 30 passed) and pass with the fix.

Open questions: 1. PLAN.md section 10 says the overdue cell is a "danger outline pulsing once per 2 s (no continuous animation)". The implementation pulses scale, not the outline, and uses animation-iteration-count infinite on a mostly flat 2 s keyframe. The verifier or orchestrator should confirm this reading of "no continuous animation" is acceptable, or ask for a border-colour or outline pulse instead.
2. The light warning literal #B7791F cannot carry text at 4.5:1 on either light surface. The stage kept the PLAN literal for non-text use and introduced --colour-warning-text #8F5E10. If PLAN.md's palette is meant to be exhaustive, the orchestrator should record the new token in STATE.md Decisions or amend PLAN.md.
3. Sheet has no focus trap or inert background: Tab can leave the dialog while it is open. The brief only required focus in on open and back on close, and axe passes, but stage 5 (App wiring) should decide whether to add a trap or use the inert attribute on the board while a sheet is open.
4. belt-fill (120 ms scale-in) plays whenever a done cell mounts, including on first paint of a board with many done cells, not only at the moment of completion; stage 5 may want to gate the animation class on a just-completed flag (feedback.ts already returns animate for this purpose).
5. Purge-days range validation lives in Settings.tsx (1 to 365). Section 7 does not list settings as a validated entry point; the orchestrator should confirm this UI-side check is acceptable or move the rule into the domain validator.
6. src/ui coverage is 98.5% statements and 93.77% branches (no threshold applies to src/ui, only to src/domain). The seven uncovered lines are defensive branches (missing form ref, FileReader onerror, disconnected previous focus, navigator undefined).
7. e2e was not re-run by this reporting agent; the 11-passed figure is the orchestrator's run at the same commit. The earlier verifier's non-failure observations remain as they were (belt-fill scale-in plays for every done cell on first render; Confirm delete replaces Delete in place with a Keep button beside it; AddTaskForm 'after' default captured on mount; fieldset default inline margin on legends). None were in the failure list, so none were changed; the orchestrator may want to hand any of them to stage 5 or a later hardening pass.

Verifier (independent, 2026-09-08): round 1 FAIL with 4 findings (major: Timer chip renders without the space between code and word: "C1overdue 05:00" / "BAin 25:00"; major: Toast auto-hide timer restarts on every re-render that passes a new onExpire identity, so a ticking parent keeps the toast alive indefinitely; minor: Two stacked header chips (52 px) overflow the fixed 48 px row header by 2 px top and bottom, overlapping the belt cells; minor: Dev gallery does not render three PatientSheet states: owner phone tel: link, discharged patient (Discharge disabled with time), task with a note). Round 2 PASS after fixes. Probes: Gate on branch stage/4-ui @ aadadff: npm run lint exit 0 (eslint 0 problems, prettier clean); npm run typecheck exit 0; npm test exit 0 (15 files, 141 tests, coverage thresholds met, ui 98.5% stmts); npm run build exit 0 (DevGallery in its own 36 kB chunk, sw precache 12 entries); npm run test:e2e exit 0 (14 passed incl. smoke.spec.ts). Re-probed the four previously fixed points: (a) PatientSheet gallery variants now include tel: link (href tel:+440000000000, 48 px), task note text, and a disabled 'Discharged HH:MM' button; (b) rows measure exactly 88 px on Pixel 5, at 360 px and at 320 px, in light and dark; (c) Toast re-rendered every tick with a fresh onExpire does not re-arm (expired at 1000 ms using the latest callback), clamps 60 s to 4 s, expires at 0 for negative, unmount clears; (d) timer chip text is 'Post-op check 2 C2 in 04:12' / 'C2 overdue 03:10' with the space preserved. Belt: exactly one <button> when current exists, zero when currentTaskId undefined or unknown, belt gets tabindex=0 only when no current; empty task list ok ('0 of 0 done, nothing left to do'); overdue boundary dueAt==now overdue, now+1 ms not; done tasks never overdue; now=0, MAX_SAFE_INTEGER, NaN and a garbage dueAt never crash; 500 custom tasks incl. unicode/whitespace labels all render two-char codes; codes match PLAN list in order; 5 phase groups; onTapCurrent fires once; non-current cells aria-hidden. PatientRow: header click -> onOpen only, current cell -> onCompleteCurrent only; progress
Verifier notes: 1. Contrast vs PLAN literal conflict (decision made by implementer, documented in tokens.css and contrast.test.ts, not in STATE.md): the binding light warning literal #B7791F is 3.49:1 on #FAFAF9, so it cannot pass the brief's 4.5:1 text requirement. tokens.css keeps --colour-warning at the literal (held to the 3:1 non-text floor, used only for borders) and adds --colour-warning-text #8F5E10 (5.3:1) for warning-toned text; in dark both are #E0A83A. On-accent glyph in dark is #121212 as the brief instructed. All other tokens pass 4.5:1 against both background and surface in both themes (recomputed by hand). The orchestrator should copy these two decisions into STATE.md Decisions when merging.
2. STATE.md on this branch has no stage 4 report section (CLAUDE.md protocol step 3). Possibly by orchestrator design to avoid four-way merge conflicts; flagging so the report is not lost.
3. Gallery-only geometry at 320 px: inside the gallery's 16 px page padding plus the inline sheet border and body padding, the sex segmented control's M/MN/F/FN options measure 43 x 48 px (Unknown takes 81 px). The DoD profile is Pixel 5 (393 px) where they are ~63 px, at 360 px ~54 px, and in the real fixed-position sheet at 320 px the arithmetic gives (288 - 81) / 4 = 51.75 px, so production layout passes. Stage 5 should keep an e2e 48 px assertion on the real Add patient sheet.
4. Integration hazard for stage 5: PatientSheet holds uncontrolled/initial-only state (add-task 'after' select state, useCommit saved-note baseline, theatre-return defaultValue) and Settings holds purge-days defaultValue. If one mounted PatientSheet instance is handed a different patient, the 'after' select keeps the previous patient's task id (probe showed an empty select that would submit p-recovery:check_1 for patient

### Stage 5: Integration, App wiring and flows (implementing subagent, 2026-09-09, commit a1d750d on stage/5-integration)
Built:
Branch stage/5-integration, commit a1d750d, worktree /Users/misbahshah/code/wardbelt-wt/stage-5-integration. 32 files: 3 modified (src/main.tsx, src/ui/Belt.tsx, src/ui/PatientRow.tsx), 29 added.

Wiring modules under src/ui/app/ (all injectable, unit-tested under jsdom):
- ids.ts: createIdSource(clock) with crypto.randomUUID feature detection and a counter+clock fallback; stamp() gives { at: ISO from the clock, eventId }.
- actions.ts: createActionFactory(ids) builds every stamped Action; discharge(patient) is COMPLETE_TASK on the discharge task while it is todo (undoable) and DISCHARGE only when skipped, per the stage 1 verifier note; dischargeIsUndoable helper.
- persist.ts: persistDiff(repo, prev, next, reset) compares by reference: changed patient entries -> savePatient, removed -> deletePatient (repo drops events), events appended from prev.length when the previous tail is intact -> appendEvent, settings by reference -> saveSettings, RESET -> clearAll. hasPersistableChange skips TICK/DUE.
- store.ts: createStore({ repo | Promise<Repo>, clock, onError }): one State signal; dispatch = reduce, move `now` to the action's own timestamp (or clock for unstamped actions), publish, queue the persist diff on a single promise chain (starts by awaiting the repo promise so nothing writes before boot); rejected chain -> onError and continues; hydrate(load) builds State from a LoadResult with no writes; subscribe(listener) for the scheduler; flush().
- notices.ts: nextStorageBanner mapping (unavailable permanent; write_failed = same headline plus reason; recovered clears only write_failed; corrupt separate) and corruptMessage.
- lock.ts: acquireTabLock over an injectable ChannelLike (browserChannel wraps BroadcastChannel('wardbelt-lock')): posts claim, held within 300 ms -> readonly, else primary; primary answers later claims with held and posts released on pagehide (and on release()); readonly tab gets onReleased. Malformed and own messages ignored. No BroadcastChannel -> primary.
- sw.ts: setupServiceWorker(registerSw, onError, supported): registerSW({ immediate: true, onNeedRefresh -> updateReady signal, onRegisterError -> 'Offline copy could not be installed' }); reload() calls updateSW(true), a rejection goes to the banner.
- boot.ts: bootRepo({ openRepo, onNotice, timeoutMs }): subscribes before load(); races open+load against a 5 s real-clock timer; on timeout or a load() rejection falls back to openRepo({ indexedDB: undefined }) (memory) with an empty LoadResult and emits unavailable with the reason; closes a late connection and unsubscribes it.
- router.ts: hash routes (board, #/patient/<id>, #/add, #/summary, #/settings, #/dev); navigate pushes a hash entry (Android back closes the sheet), close() replaces the entry so back never reopens a sheet.
- select.ts: boardRows (sortByUrgency + urgencyOf rank -> PatientRow urgency, nextDue for the chip at any rank, currentTask), dischargedRows, canUndo (mirrors the reducer's UNDO target search).
- download.ts: <a download> blob helper returning false on failure (Share API and textarea fallbacks left to stage 6).
- errors.ts: describeError (duck-typed name/message).
- session.ts: composition root createSession(platform): store over a deferred repo, timers (realClock in production) with getPatients/dispatch and a notifier gated by settings.notifications (off = vibration only), notifier with onError -> transient banner, theme effect (applyTheme from settings), service worker handle, banners (storage, corrupt count with raw export, transient auto-clearing after 8 s, lock), toast with undo, flows complete/skip/undo/discharge/addTask/setNote/setTheatreReturn/deletePatient/setSettings/requestNotifications/exportData/importText/purgeDischarged/reset, notificationState signal refreshed after prompts and on visibility, start() = Promise.all(lock, bootRepo) then hydrate, then (primary only) timers.start(), timers.onVisible(), visibilitychange -> onVisible; readonly tab sets readOnly, shows 'Wardbelt is open in another tab' and ignores every action.
- app.css: shell layout (sticky header, fixed 48 px bottom bar, empty state, toast lifted above the bar), tokens only.

Views:
- src/ui/Board.tsx: header with app name and mono HH:MM clock from state.now; rows from boardRows (re-sorted on every TICK); empty state 'No patients on the board. Add one to start.' with a 48 px Add button; 'Show discharged (N)' aria-pressed toggle listing discharged rows with no current cell; fixed bottom bar Add patient / Summary / Settings; inert on main and nav while a sheet is open; every control disabled in read-only.
- src/ui/App.tsx: reads the session signals; renders Board with the Notices (lock banner with Reload, storage danger banner, dismissible corrupt banner with 'Export raw records', 'Update ready' with Reload, transient warning banner), the route's sheet (AddPatientSheet, PatientSheet keyed by patient id with canUndo, ShiftSummary with shiftStats and a shift label, Settings wired for theme/toggles/purge days plus export/import/purge/delete-all through the session), and the keyed Toast (4 s) with Undo.
- src/main.tsx: mounts App at once, builds the browser Platform (realClock, openRepo, BroadcastChannel lock with pagehide, browserNotifyDeps, registerSW from virtual:pwa-register guarded by 'serviceWorker' in navigator, visibilitychange, downloadText, prefersReducedMotion, __APP_VERSION__), then session.start(); a boot failure renders a role=alert line. #/dev gallery unchanged (dynamic import).
- Additive props: Belt `disabled` (current cell button disabled), PatientRow `readOnly` (header and belt disabled). No restyling.

Tests: tests/e2e/flows.spec.ts (10 tests: the nine DoD flows plus axe + 48 px targets on the real board, Add sheet and patient sheet); tests/unit/app/ (12 files, 82 tests): persist diff per action type, store, ids/actions stamping, notices/select, lock protocol with a fake bus under vi.useFakeTimers, sw + boot (timeout, load rejection), router + download, session (boot roles, fallbacks, banners, flows, settings/transfer, notification gating, visibility), Board, App wiring, errors.

DoD evidence (actual runs):
Full gate run in the worktree at a1d750d (log: scratchpad/gate.log), exit 0:
- npm run lint: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!".
- npm run typecheck: tsc -p tsconfig.json and tsc -p tsconfig.sw.json both clean.
- npm test (vitest run --coverage): 39 files, 453 tests passed (was 27 files / 371 before this stage; 12 new files / 82 new tests). Coverage all files: statements 99.2% (1756/1770), branches 97.28% (1147/1179), functions 98.35% (479/487), lines 99.35% (1707/1718). src/domain from coverage-summary.json: lines 532/532, branches 406/406 (100% threshold met). src/ui/app: 98.4% statements, 95.33% branches, 96.77% functions, 98.83% lines (424/429). App.tsx 96% statements / 100% branches; Board.tsx 100% statements / 94.73% branches.
- npm run build: dist/index.html 1.35 kB (0.60 kB gzip); assets/index CSS 14.93 kB (3.13 kB gzip); assets/index JS 91.08 kB (31.65 kB gzip, now carries domain + store + scheduler + components + preact/signals/idb); workbox-window chunk 5.75 kB (2.36 kB gzip, loaded by registerSW); DevGallery chunk 8.15 kB (2.87 kB gzip, fixtures stay out of the main bundle); sw.mjs 17.07 kB (5.73 kB gzip); precache 13 entries (123.20 KiB). Total shipped gzip about 43.5 kB including the SW.
- npm run test:e2e (Pixel 5, production build via vite preview, fresh build): 24 passed in 22.3 s: dev-gallery 12, smoke 2, flows 10: (1) adds a patient in 6 counted taps (Add, Name, Procedure, Cat, 10:00, Add patient) and the row shows name, species, 19 cells and belt aria-label '0 of 19 done, current: Handover and admit'; (2) three taps on the current cell give 3 done cells, current Premed, aria summary '3 of 19 done, current: Premed', toast 'Draw up meds completed'; (3) under page.clock, completing through In theatre shows chip 'C1 in 15:00' and the sheet lists Due times at theatre return +15/30/45/60 read back from the sheet's own return time; (4) fastForward 16 min: chip '.chip--danger' reads 'C1 overdue 01:0x', row data-urgency overdue, one overdue cell, row order flips to put it first ahead of the earlier-added fresh patient; (5) custom task 'Bandage check' with a due time inserted after Premed appears at index 4 in the sheet and belt with code BA and 'Custom'; (6) Skip Bloods hatches the cell, toast Undo restores it, skip again and the sheet Undo restores it and disables itself; (7) Discharge disables to 'Discharged HH:MM', row leaves the board, 'Show discharged (1)' toggle reveals it with no current cell; (8) after waiting for the IndexedDB write and the SW controller, context.setOffline(true) + reload renders the board with the same patient; (9) second page in the same context shows 'Wardbelt is open in another tab', Add and the belt button disabled while the first tab stays enabled; closing the first and reloading the second makes it primary; (10) axe (wcag2a/2aa/21aa) clean and every visible target >= 48 px on the empty board, board with a row (88 px), the open Add sheet and the open patient sheet.
- git status --porcelain empty after commit; pushed a1d750d to origin/stage/5-integration.

Deviations: 1. Read-only tab renders the board as a viewer only: the row header, belt cell and all three bar buttons are disabled and mutating routes (add, patient, settings, summary) render nothing; the brief said 'disables buttons and ignores actions', and disabling every control inside the sheets would have meant a readOnly prop across four stage 4 components, so sheets are not opened at all in that tab. Actions are still ignored at the session level as a second line.
2. On `released` from the primary, the read-only tab does not reload itself; the banner text changes to 'The other tab has closed. Reload to take over.' with a Reload button (PLAN.md section 8 forbids silent reloads; the brief's 'may reload to take over' is read as user-initiated).
3. A load() rejection falls back to a memory repo (not just an empty state on the idb repo), closing the opened connection, because writing new records beside unreadable ones would contradict the 'Not saving' banner.
4. The transient banner (notifier errors, feedback errors, SW registration, rejected write chain) auto-clears after 8 s through the injected clock; the brief said 'short-lived' without a duration.
5. Export/import/purge/delete-all Settings callbacks are wired now (download anchor only, parseImport with the rejection message and failing count in the banner, PURGE_DISCHARGED, RESET) because the store/transfer APIs made it one line each; the Share API and copyable-textarea export fallbacks and the e2e for these stay with stage 6 as planned.
6. `now` in State follows the dispatched action's own timestamp (TICK/DUE `now`, stamped `at`) rather than a separate clock read, so a fresh countdown reads exactly 15:00; unstamped actions (SET_NOTE, SET_SETTINGS, IMPORT, RESET) use the clock.
7. e2e test 1 counts 6 taps (species and intake included) rather than 8; the brief's cap of 8 is asserted with <=.
8. Additive edits outside src/ui/app: Belt `disabled` prop, PatientRow `readOnly` prop (both optional, default false, no style change); their tests live in tests/unit/app/board.test.tsx. No edits to src/domain, src/store, src/scheduler, config files or STATE.md.

Open questions: 1. Countdown and header clock granularity: with PLAN.md section 8's single scheduler timer (cap 60 s) and `now` updated only by TICK and actions, the mm:ss chips and the HH:MM clock refresh at most once a minute between due times. If the orchestrator wants second-level countdowns, stage 6 or 7 could lower TimersOptions.maxDelayMs or add a display-only ticker; the stage 3 verifier asked to leave maxDelayMs at the default, so it was left.
2. The write window on unload: an IndexedDB write still in flight when the tab is closed is aborted (the e2e had to wait for the write before reloading). Writes land within milliseconds, and nothing in PLAN.md asks for a beforeunload guard; flagged for stage 7 hardening if wanted.
3. Read-only tab data goes stale: it hydrates once from IndexedDB and never re-reads while the primary writes. PLAN.md only requires 'stay read-only'; a periodic re-load or a BroadcastChannel state relay would be a stage 6/7 addition.
4. Stage 6 items left deliberately: Share API export and the copyable-textarea fallback, weekly export nudge, auto-purge on boot (PURGE_DISCHARGED is only fired from the Settings button now), e2e for export/wipe/import round trip and stats.
5. Carried from stage 4: the belt-fill scale-in still plays for every done cell on first paint (feedback.ts returns animate for gating but the Belt has no just-completed prop); cosmetic, not changed here.
6. No new dependencies; workbox-window (already a vite-plugin-pwa dependency) now ships as a lazy 2.36 kB gzip chunk because registerSW imports it.

Verifier (independent, 2026-09-09): PASS on first round. Gate: npm run lint: eslint . --max-warnings 0 && prettier --check . => "All matched files use Prettier code style!" exit 0
npm run typecheck: tsc -p tsconfig.json --noEmit && tsc -p tsconfig.sw.json --noEmit => exit 0
npm test: Test Files 39 passed (39); Tests 453 passed (453); Coverage v8 All files 99.2 % stmts | 97.28 % branch | 98.35 % funcs | 99.35 % lines; thresholds met; exit 0
npm run build: dist/index.html 1.35 kB (gzip 0.60 kB); dist/assets/index-BgkVBKBj.css 14.93 kB (gzip 3.13 kB); dist/assets/workbox-window.prod.es5-BBnX5xw4.js 5.75 kB (gzip 2.36 kB); dist/assets/DevGallery-DaNvh0tX.js 8.15 kB (gzip 2.87 kB); dist/assets/index-CHeTZNbv.js 91.08 kB (gzip 31.65 kB); dist/sw.mjs 17.07 kB (gzip 5.73 kB); exit 0
npm run test:e2e: 24 passed (22.5s) on android-chromium (Pixel 5): dev-gallery.spec.ts 12, flows.spec.ts 10 (8 taps, complete steps, theatre checks +15/30/45/60, overdue sorts t
Probes: Gate (pristine tree): npm run lint (eslint + prettier) exit 0; npm run typecheck exit 0; npm test 39 files / 453 tests passed, coverage all files 99.2% lines, 97.28% branches, src/domain thresholds met; npm run build exit 0 (main JS 91.08 kB / 31.65 kB gzip, CSS 3.13 kB gzip, workbox-window 2.36 kB gzip, DevGallery chunk 2.87 kB gzip, sw 5.73 kB gzip); npm run test:e2e 24 passed (12 dev-gallery, 10 flows, 2 smoke). Unit probe tests/unit/verify/persist-probe.test.ts (9 pass): ADD_PATIENT savePatient then appendEvent; discharge completion appends TASK_COMPLETED then DISCHARGED (eventId:1); UNDO of discharge appends two UNDO events; DELETE_PATIENT of a patient whose events sit mid-array issues deletePatient only and appends nothing; IMPORT saves imported patients, appends only unseen events, saves settings; PURGE_DISCHARGED one deletePatient per stale patient; RESET clearAll only; SET_NOTE savePatient only; TICK/DUE write nothing; write chain waits for a late repo and keeps dispatch order; a rejected write reports 'Not saving: Error: disk' and the chain continues; hydrate writes nothing, appends no events, now follows the clock; ignored action (unknown patient) and unparseable `at` write nothing. Unit probe tests/unit/verify/lock-boot-probe.test.ts (8 pass): held at 299 ms => readonly, held at 301 ms => primary; primary answers later claims with held, readonly never answers, released reaches readonly onReleased only; own, malformed (null, string, missing/non-string id, unknown type) and stray messages ignored, settle exactly once, late held cannot demote; release idempotent, readonly release posts nothing; pagehide hook registered only for primary; no BroadcastChannel => primary. Boot: hung openRepo falls back to memory after the timeout, second openRepo called with indexe
Verifier notes: 1. Brief wording deviation with no behavioural effect: the brief says 'the App gets a readOnly prop'; the implementation exposes readOnly as a signal on the Session (App({ session, router })) and passes a readOnly prop down to Board, PatientRow and Belt. Every control is disabled and every dispatch ignored in the read-only tab as required; stage 6 should wire against Session/SessionActions in src/ui/app/session.ts (state, ready, readOnly, storageMode, notificationState, banners, updateReady, toast, actions.{addPatient, complete, skip, undo, addTask, setNote, setTheatreReturn, discharge, deletePatient, setSettings, requestNotifications, exportData, importText, purgeDischarged, reset}, stats(), start(), flush()).
2. Robustness gap not reachable from the UI: an action dispatched through session.actions before start() has hydrated is persisted to the repo (write chain waits for the repo) but then dropped from memory when hydrate() replaces the state, so memory and storage disagree until the next reload. Every UI entry point is gated on ready (no rows, no sheets before hydration; the bottom bar only navigates), so no user can hit it. A guard in the session's dispatch (ignore or queue while !ready) would close it cheaply.
3. Countdown and header clock refresh on TICK only, which the scheduler caps at 60 s when nothing is due sooner, so the mm:ss chip can lag up to a minute between ticks. This follows PLAN.md section 8 (one setTimeout capped at 60 s) and the brief ('now signal updat

### Stage 6: Shift stats, summary, settings, transfer (implementing subagent, 2026-09-09, commits 39e8954 and 5b6e7af on stage/6-summary)
Built:
Branch stage/6-summary, commit 39e8954, worktree /Users/misbahshah/code/wardbelt-wt/stage-6-summary. 13 files: 4 new source/test modules, 9 edited. No edits to src/domain, src/store, src/scheduler, src/main.tsx, configs or STATE.md.

Source:
- src/ui/app/transfer.ts (new): the PLAN.md section 8 export chain `exportText(name, text, { share, download })` returning 'shared' | 'downloaded' | 'textarea' | 'cancelled': builds a File and tries `share.canShare({ files })` then `share.share(...)` (AbortError from a dismissed share sheet = cancelled, any other failure falls through), then the stage 5 `<a download>` helper, else textarea. `browserShare()` (navigator or {} without one), `exportFileName(nowMs)` (`wardbelt-export-YYYY-MM-DD.json` by local date), `needsExportNudge(settings, patientCount, nowMs)` (section 9: at least one patient and lastExportAt missing, unparseable or older than EXPORT_NUDGE_MS = 7 days), NUDGE_MESSAGE, EXPORT_TITLE.
- src/ui/app/session.ts: dispatch is ignored until the store has hydrated (`hydrated` flag; closes the stage 5 verifier gap); after hydration the primary tab dispatches PURGE_DISCHARGED once (respects settings.purgeDays, deletions persist through the normal diff path, never in a read-only tab); `actions.exportData` is now `() => Promise<ExportOutcome>` and never rejects: builds the file via buildExport/serialiseExport, runs the chain with `platform.share ?? browserShare()`, on shared/downloaded dispatches SET_SETTINGS { lastExportAt } and toasts "Exported", on textarea sets the new `exportText` signal, on cancelled does nothing; `actions.importText` sets the new `importError` signal ("Import rejected: <message>", the message already carries the failing count) and leaves state untouched, or dispatches IMPORT and toasts "Imported N patient(s)"; `actions.purgeDischarged` toasts "Purged N discharged patient(s)" or "Nothing to purge"; new `exportNudge` computed signal (ready, not read-only, not dismissed this session, needsExportNudge over state.now), `dismissNudge()`, `dismissTransfer()` (clears exportText and importError). Platform gains optional `share?: ShareApi`.
- src/ui/Settings.tsx: new props `importError`, `exportText`, `onDismissExportText`; inline role="alert" shows the caller's rejection or the local unreadable-file error; a read-only `textarea.field__input.mono` (label EXPORT_TEXT_LABEL "The file could not be saved. Copy this text instead", selects all on focus) with a Done button appears when exportText is set; purge button pluralises ("1 day" / "30 days").
- src/ui/App.tsx: shiftLabel now "Shift from 04:00 Tue 10 Mar" (local shift start, en-GB short weekday and month); weekly nudge Banner (Export action + Dismiss) in Notices; `exportFrom(session, router)` runs the chain and opens #/settings when it ends in the textarea; Settings wired with importError/exportText/dismissTransfer, and Close or Delete everything clear the transfer surfaces.

Tests:
- tests/unit/app/transfer.test.ts (new, 8): share success with File name/type/content and canShare argument, download when canShare is false / share rejects with a non-abort error / share absent / canShare only / canShare throws, cancelled on AbortError with no download, textarea when download fails, share skipped without a File constructor, browserShare with and without navigator, local-date file name, nudge rule at the exact 7-day boundary, garbage and future lastExportAt, zero patients.
- tests/unit/app/session.test.ts (+4 new, 3 rewritten): actions before hydration dropped and nothing written while later actions persist; boot purge deletes only stale discharged patients via deletePatient with no toast; purgeDays honoured at boot and no purge in a read-only tab; download path records lastExportAt; Share API preferred and a dismissed share leaves lastExportAt and toast untouched; textarea fallback sets exportText without lastExportAt or a banner; nudge appears with a patient, clears on export, returns 7 days + 1 ms later, dismiss for the session; import rejection sets importError and leaves state identical by reference, success clears it and toasts "Imported 1 patient"; purge toasts.
- tests/unit/app/app.test.tsx (+3): nudge banner render, export from it, re-show after the clock passes 7 days, dismiss; nudge export with a failing download opens Settings with the read-only textarea, Done clears it, Close clears it so a reopened sheet is clean; malformed import shows the inline alert and leaves the board; shift label exact strings either side of 04:00.
- tests/unit/ui/settings.test.tsx (+3): purge singular, importError prop, exportText textarea selects on focus and Done fires onDismissExportText. tests/unit/app/helpers.ts: fakePlatform `share` option defaulting to {} so no unit test touches the jsdom navigator.
- tests/e2e/helpers.ts (new): the stage 5 helpers moved out of flows.spec.ts plus openSettings, closeSheet, dischargePatient, readStore (raw IndexedDB via indexedDB.databases() guard so a probe never creates an empty database), waitForStore (expect.poll), axeViolations, smallTargets. flows.spec.ts now imports them (no behaviour change).
- tests/e2e/shift.spec.ts (new, 1): page.clock at 10:00 local 10 Mar 2026; A, B, C admitted; A through In theatre; skip Handover from theatre; check 1 at +14, check 2 at +31, check 3 at +49 (chip "C3 overdue 04:xx"), check 4 at +60; skip Bloods on B; discharge A and B; summary asserts label "Shift from 04:00 Tue 10 Mar" and exactly ['12', '2', '75%', '2', '3', '2', <median>] where the median is computed from the createdAt/dischargedAt the app stored (asserted between 60 and 65 min) and formatted like format.ts; fastForward 18 h then "Shift from 04:00 Wed 11 Mar" and ['0','0','n/a','0','0','0','n/a'].
- tests/e2e/transfer.spec.ts (new, 4): (1) two patients with 2 completions and 1 skip, belt snapshot, Export via page.waitForEvent('download') with canShare forced false by init script, suggested file name equals the page's local date, file has 2 patients and 5 events, "Exported" toast and "Last export" line, Delete everything two-step, board empty, reload, raw store {patients: [], events: [], settings: []}, Import via setInputFiles, toast "Imported 2 patients", belts deep-equal the snapshot, raw patients and events deep-equal the export sorted by id and settings rows equal file.settings, then a file with one blank name gives alert "Import rejected: 1 record failed validation" and non-JSON gives "Import rejected: File is not valid JSON" with board and store unchanged; (2) auto-purge: discharge, purge days 1, setSystemTime +2 days, reload, patient gone from board and store; then purge days 5, +2 more days, reload, patient kept; (3) settings persistence: dismissable nudge, notification hint copy shown for the browser's state, sound, owner phone, theme dark (html[data-theme]) and purge days 14 survive reload, owner phone field appears in the add sheet, nudge Export downloads and the nudge stays gone after reload; (4) URL.createObjectURL throwing ends in the read-only textarea with the export JSON, focus selects all, import error alongside, axe clean and all targets 48 px in that state, Done removes it.

DoD evidence (actual runs, after fixes):
Full gate run as one command in the worktree after the fixes, exit 0: `npm run lint && npm run typecheck && npm test && npm run build && npm run test:e2e`.
- lint: eslint --max-warnings 0 clean; prettier: "All matched files use Prettier code style!"
- typecheck: tsc -p tsconfig.json --noEmit and tsc -p tsconfig.sw.json --noEmit, no output (clean).
- npm test (vitest run --coverage): Test Files 40 passed (40), Tests 477 passed (477) (was 470; +7 new). Coverage all files: Statements 99.18% (1827/1842), Branches 97.1% (1206/1242), Functions 98.41% (497/505), Lines 99.32% (1778/1790). The v8 table lists only files under 100%; src/domain/** does not appear (100% on all four metrics, the enforced gate), and neither do src/ui/app/download.ts nor src/ui/app/transfer.ts (both 100% after the new branch tests).
- npm run build: dist/assets/index-ClAHynj2.js 93.67 kB (gzip 32.40 kB), index CSS 14.93 kB (gzip 3.13 kB), DevGallery chunk 8.15 kB (gzip 2.87 kB), sw.mjs 17.07 kB (gzip 5.73 kB).
- npm run test:e2e (android-chromium, Pixel 5, against npm run build && npm run preview): 31 passed (41.0s) (was 29; +2 new), including transfer.spec.ts "manual purge says nothing to purge on a running clock and counts what it removed" and "a download anchor whose click throws leaves no anchor behind and shows the textarea".
- Negative check: with src/ui/app/{download,session,transfer}.ts stashed, the 5 new unit tests fail (5 failed | 33 passed in the three touched files) and the 2 new e2e tests fail with exactly the verifier's symptoms ('Nothing to purge' toast never visible; a[download] count 1 instead of 0). Restored with git stash pop.
- git status --porcelain empty after commit 5b6e7af, pushed to origin/stage/6-summary (39e8954..5b6e7af).

Deviations: 1. Import rejection is shown inline in the Settings sheet (role="alert" next to the Import button) rather than through the stage 5 transient banner, because the banner sits behind the open sheet's scrim; the message is "Import rejected: <validator message>" and the failing count is the validator's own ("1 record failed validation") rather than appended a second time as stage 5 did.
2. The export file name uses the local calendar date (exportFileName in transfer.ts) instead of the UTC date slice stage 5 used; unit and e2e tests compute the expected name the same way.
3. The textarea outcome does not set lastExportAt or toast "Exported": the app cannot vouch that the text was saved anywhere, so the weekly nudge stays until a share or download succeeds (PLAN.md says lastExportAt is set on success).
4. The shift label format changed from "Tuesday 10 March, 04:00 to 04:00" to the brief's "Shift from 04:00 Tue 10 Mar"; the stage 5 app.test regex was updated.
5. SessionActions.exportData now returns Promise<ExportOutcome> (never rejects) instead of void; App wraps it (`void ...then`) so the Settings onExport prop stays `() => void`. The Web Share surface reaches the session through an optional Platform.share, defaulting to the browser navigator inside the session (browserShare) so src/main.tsx, which is outside stage 6 ownership, is untouched.
6. Toast grammar: "Imported 1 patient" / "Imported 2 patients" (brief wording "Imported N patients"); purge button reads "1 day" / "30 days"; purge from Settings toasts "Purged N discharged patient(s)" or "Nothing to purge" (not asked for, added so the button has visible effect).
7. tests/e2e/flows.spec.ts (stage 5 file) was edited only to import the extracted helpers from tests/e2e/helpers.ts, as the brief allowed; the axe and 48 px helpers moved too so transfer.spec.ts can reuse them.
8. Web Share is disabled in the e2e through an init script forcing navigator.canShare to return false: headless Chromium has no share sheet and Linux builds have no navigator.share, so the download path is the only deterministic one; the share path is covered by unit tests with fakes.
9. The notifications toggle e2e asserts the state explanation for whatever permission headless Chromium reports (it reports "denied" even after context.grantPermissions, so the toggle is disabled there) and only checks persistence of the toggle when it is enabled; the prompt path (requestPermission when turning on in the default state) is unit-tested in settings.test and session.test.
Fix round: Fixes for both verifier failures plus one observation, on branch stage/6-summary (worktree /Users/misbahshah/code/wardbelt-wt/stage-6-summary), commit 5b6e7af "stage 6: fixes after verification".

1. Purge toast (src/ui/app/session.ts). Root cause: the session's dispatch returned `state.value !== before`, which is true whenever the store republished because `now` moved, so in real use "Nothing to purge" was unreachable and the toast read "Purged 0 discharged patients". Two changes: (a) session.dispatch now returns `hasPersistableChange(before, state.value)` (imported from ./persist), i.e. true only when the reducer changed patients, events or settings; this also stops a no-op undo or delete from clearing the toast merely because the clock ticked. (b) purgeDischarged computes n from the patient count before and after the dispatch and shows "Nothing to purge" when n is 0, otherwise "Purged N discharged patient(s)"; it also returns silently before hydration (like every other action) instead of reporting "Nothing to purge" (verifier observation 2). Decision-notes header updated.

2. Download leak (src/ui/app/download.ts). anchor.remove() and the object URL release moved into a finally block: after a successful click the revoke still waits REVOKE_DELAY_MS (Chrome starts the download asynchronously); when the click never happened (createElement/appendChild/click threw) the URL is revoked at once, so a failed attempt leaks neither a DOM node nor a Blob URL and arms no timer. Header comment rewritten.

3. Contract tightening (src/ui/app/transfer.ts, verifier observation 1): exportText wraps deps.download in try/catch and returns 'textarea' when it throws, so SessionActions.exportData genuinely never rejects and App.exportFrom's un-caught .then is safe.

Regression tests (each fails on the pre-fix code, verified by stashing the src changes):
- tests/unit/app/session.test.ts: "reports nothing to purge once the wall clock has moved since the last action" (clock.advance(1) and advance(60_000) before purge; toast is 'Nothing to purge', no repo write); "keeps the undo toast when the clock moved but there was nothing to undo".
- tests/unit/app/router-download.test.ts: "leaves no anchor behind and revokes the URL when the click throws" (click spy throws; returns false, zero a[download], revoke called once with the URL, still once after 60 s of fake timers); "returns false with nothing to revoke when the object URL cannot be created"; the existing createElement-throws test now also asserts the immediate revoke.
- tests/unit/app/transfer.test.ts: download step that throws ends in 'textarea'.
- tests/e2e/transfer.spec.ts (Pixel 5, production build): "manual purge says nothing to purge on a running clock and counts what it removed" (page.clock.install running naturally: empty board -> 'Nothing to purge', freshly discharged patient with purgeDays 1 -> 'Nothing to purge', setSystemTime +2 days -> 'Purged 1 discharged patient', board and IndexedDB hold only the active patient); "a download anchor whose click throws leaves no anchor behind and shows the textarea" (init script: canShare false, HTMLAnchorElement.prototype.click throws; textarea shown with the export, a[download] count 0, 'Never exported.' unchanged). setPurgeDays and purgeButton lifted to module-level helpers in the spec and reused by the auto-purge test.

Open questions: 1. src/ui/DevGallery.tsx (stage 4, not in stage 6 ownership) does not render the two new Settings states (export textarea, import error). The transfer e2e runs axe and the 48 px sweep on the live sheet in both states instead; the orchestrator may want stage 7 to add them to the gallery for completeness of the '/dev renders every state' DoD.
2. Headless Chromium reports Notification.permission as 'denied' even after context.grantPermissions(['notifications']) under the Pixel 5 profile, so the e2e cannot exercise the granted toggle end to end; the state explanation and the disabled toggle are asserted, and the request-on-enable path is unit-tested.
3. The read-only tab still ignores exportData only implicitly (no sheets render there); exporting from a read-only tab would be harmless (read-only data) but is unreachable from the UI.
4. The nudge banner adds up to 48 px above the board whenever a patient exists and no export happened in 7 days; stage 7's Lighthouse run should be done with the nudge either dismissed or after an export if layout shift scores matter.
5. Carried from stage 5: countdown chips and the header clock refresh on TICK (up to 60 s) and the belt-fill animation plays on first paint; unchanged here. None.

Verifier (independent, 2026-09-09): round 1 FAIL with 2 findings (minor: Manual purge with nothing to purge shows "Purged 0 discharged patients" instead of "Nothing to purge"; minor: Download anchor throw path leaks the hidden <a download> element and never revokes the object URL). Round 2 PASS after fixes. Gate: npm run lint: eslint . --max-warnings 0 && prettier --check . -> "All matched files use Prettier code style!" exit 0. npm run typecheck: tsc -p tsconfig.json && tsc -p tsconfig.sw.json -> exit 0. npm test: vitest run --coverage -> Test Files 40 passed (40), Tests 477 passed (477); All files 99.18% stmts / 97.1% branches / 98.41% funcs / 99.32% lines; src/domain thresholds (100/100/100/100) met; exit 0. npm run build: vite build -> dist/sw.js generated, exit 0. npm run test:e2e: playwright test -> 31 passed (40.9s) on android-chromium (Pixel 5), including shift.spec.ts (1) and transfer.spec.ts (6); exit 0. Verifier probes: tests/unit/verify 16/16 passed; tests/e2e/verify 8/8 passed (deleted a
Probes: Gate re-run from scratch on 5b6e7af: npm run lint (eslint + prettier) clean; npm run typecheck clean; npm test 40 files / 477 tests green with src/domain 100% thresholds met; npm run build green; npm run test:e2e 31/31 green on Pixel 5 (shift.spec, transfer.spec x6, flows x10, dev-gallery x12, smoke x2). Unit probe (tests/unit/verify/stage6.test.ts, 13 tests, all green): independent recompute of the seven PLAN section 6 stats from raw events/patients matched session.stats() for a scripted shift; check completed at exactly dueAt + 3 min counted on time, at + 3 min + 1 ms counted late (67%, best streak 1); an undone check completion, an undone task completion and an undone discharge were excluded from counts and the median; shift label 'Shift from 04:00 Tue 10 Mar' held at 03:59:59.999 next day, flipped to 'Wed 11 Mar' at 04:00:00.000 with all-zero/n/a stats, and a backwards clock jump returned to the earlier shift without error. Nudge rules probe: no nudge with zero patients; nudge with missing, empty, whitespace or garbage lastExportAt; exactly 7 days old -> no nudge, 7 days + 1 ms -> nudge; future lastExportAt (clock went backwards) -> no nudge; session nudge false before ready and in a read-only tab, true with only discharged patients. Dispatch-before-hydrate guard probe: all 14 session actions (add, complete, skip, undo, addTask, setNote, setTheatreReturn, discharge, delete, setSettings, purge, reset, importText, exportData) dispatched before start() left the state reference untouched, applied no theme, wrote nothing to the repo, and the hydrated state was the loaded one; actions after start() persisted normally. Auto-purge boundary probe with a fake clock: discharged exactly N days ago kept (no repo write), N days + 1 ms and N days + 1 min purged (deletePatient issu
Verifier notes: The three points fixed after the previous FAIL were re-probed specifically and hold: downloadText removes the anchor and revokes the object URL in a finally block on a throwing click; exportText catches a throwing download and ends in the textarea; session.dispatch reports hasPersistableChange so a purge on a moved clock says 'Nothing to purge' and purge is guarded before hydration. Observations that are not failures: (1) the summary label and window follow state.now, which the scheduler refreshes on TICK (at most 60 s apart) and on visibility change, so after a forward device-clock jump the summary can lag up to 60 s until the next TICK; a reload is exact at once. (2) session.actions.exportData() called before start() exports the empty initial state and does not record lastExportAt; this is unreachable from the UI because RouteSheet and the nudge banner render only when ready. (3) IMPORT replaces the settings record wholesale with the file's settings (a stage 1 reducer decision; PLAN 'merge by id' covers records only), so importing an old backup overwrites current theme/purgeDays/lastExportAt; the implementer's and my tests assert this behaviour. (4) STATE.md on this branch carries no stage 6 report or decision lines; per the Decisions log the orchestrator writes them verbatim from the subagent's structured output. Worktree left clean (git status --porcelain empty), nothing committed or pushed.

### Stage 7: Hardening and evidence (implementing subagent, 2026-09-09, commit c2db8dd on stage/7-hardening)
Built:
Branch stage/7-hardening, commit c2db8dd (from main at dd6b370), worktree /Users/misbahshah/code/wardbelt-wt/stage-7-hardening. 15 files: 14 added, 1 modified (.github/workflows/ci.yml). No file under src/ changed: every Lighthouse target was already met, so the production build is byte-identical to main (asset hashes index-ClAHynj2.js, index-BgkVBKBj.css, DevGallery-CT-gv7vt.js, workbox-window.prod.es5-BBnX5xw4.js, sw.js with 13 precache entries).

Scripts (all lint-clean under the repo's no-console rule, output via process.stdout, evidence JSON formatted through the repo's own Prettier so `prettier --check .` accepts it):
- scripts/evidence-lib.mjs: shared helpers (ensureBuild honouring --no-build, writeEvidence honouring --write with date and short SHA stamped, text table, kB formatting, fail).
- scripts/bundle-size.mjs: gzips dist/assets/*.js and *.css with node:zlib, prints a table and total, exits 1 over 60,000 bytes; sw.js reported but not counted; writes docs/evidence/bundle-size.json.
- scripts/hardening.mjs: 8 static checks over dist (CSP meta verbatim, no inline script/handler/javascript: URL, no console in app assets, sw.js console limited to Workbox's no-revision guard, precache covers index.html + manifest + every asset, every precache entry carries a revision key so that guard is dead code, manifest installable, no eval/new Function); exits 1 on any failure; writes docs/evidence/hardening.json.
- scripts/lighthouse.mjs: reuses or starts vite preview on 4173, resolves Chrome (CHROME_PATH, Google Chrome, Playwright Chromium), runs `npx --yes lighthouse@13.4.1` (performance, accessibility, best-practices) and `npx --yes lighthouse@11.7.1 --only-categories=pwa` with --chrome-flags=--headless=new, prints a score table and metrics, exits 1 if PWA/performance/accessibility < 95, writes the trimmed summary (scores, per-category audit counts, metrics, audits below 1) to docs/evidence/lighthouse.json; full reports go to a temp dir.
- scripts/coverage-table.mjs: builds the per-file src/domain table and directory totals from coverage/coverage-summary.json; writes docs/evidence/coverage.json.

Tests:
- tests/e2e/a11y.spec.ts (2 tests): real board with two patients added through the Add sheet (intake chip; one through theatre and 16 min fast-forwarded so a check is overdue), then the open patient sheet with due times, a saved note and enabled Undo; AxeBuilder wcag2a/2aa/21aa, zero violations, 48 px sweep, light and dark (prefers-color-scheme, body background asserted); attaches counts to the report and, with WARDBELT_EVIDENCE=1, writes docs/evidence/axe.json.
- tests/unit/hardening/storage-unavailable.test.tsx (3): real openRepo + rendered App: no IndexedDB boots in memory with the permanent banner and every action works (Settings shows the memory line); an open that throws does the same; four genuine fake-indexeddb put failures show the banner naming QuotaExceededError (and not the patient) while the action stays applied, next successful write clears it.
- tests/unit/hardening/clock-jump.test.tsx (3): session on the fake clock, overdue check; clock set 60 min back then swept: no throw, state.now follows, chip "C1 in 55:00", stored dueAt unchanged, one timer armed, urgency due-soon then overdue as time advances; +60 min forward jump "C1 overdue 65:00" with the row first; shift window and stats coherent either side.

Docs: docs/TESTING.md (Gate, Coverage, Lighthouse, axe, Bundle size, Audit, Hardening checklist, How to reproduce; date and commit stated) and docs/evidence/{lighthouse,bundle-size,hardening,coverage,axe}.json (each under 4 kB).
CI: .github/workflows/ci.yml gains "Bundle size under 60 kB gzipped" (node scripts/bundle-size.mjs --no-build) and "Hardening checks on dist" (node scripts/hardening.mjs --no-build) after npm run build; actionlint clean.

DoD evidence (actual runs):
Final gate run on the committed tree, 2026-09-09, exit 0 (log scratchpad/gate-final.log): `npm run lint && npm run typecheck && npm test && npm run build && npm run test:e2e && node scripts/bundle-size.mjs --no-build && node scripts/hardening.mjs --no-build && actionlint`.
- lint: eslint 0 problems (--max-warnings 0); prettier "All matched files use Prettier code style!" (docs/evidence JSON included).
- typecheck: tsc -p tsconfig.json and tsc -p tsconfig.sw.json clean.
- npm test: Test Files 42 passed (42), Tests 483 passed (483) (was 40 files / 477; +2 files / +6 tests). Coverage from coverage-summary.json: src/domain 100% on all four metrics (548/548 statements, 406/406 branches, 108/108 functions, 532/532 lines, every file 100%); src/store 100/96.30/100/100; src/scheduler 100/100/100/100; src/ui/app 98.38/94.81/97.04/98.76; src/ui 98.35/95.25/97.35/98.56; all files 99.19/97.10/98.42/99.33.
- build: index 93.67 kB (32.40 gzip), CSS 14.93 kB (3.13 gzip), DevGallery 8.15 kB (2.87), workbox-window 5.75 kB (2.36), sw.js 17.97 kB (6.05 gzip), precache 13 entries.
- e2e: 33 passed (52.7 s) on android-chromium Pixel 5 (a11y 2 new, dev-gallery 12, flows 10, shift 1, smoke 2, transfer 6); offline reload, second-tab lock, reduced motion and 48 px target tests all passed.
- Lighthouse (docs/evidence/lighthouse.json, three consistent runs, Google Chrome HeadlessChrome/152.0.0.0, mobile profile, simulated throttling): PWA 100 (lighthouse 11.7.1: 6 passed, 3 manual), performance 100 (13.4.1: 49 audits, FCP 1.2 s, LCP 1.4 s, TBT 0 to 10 ms, CLS 0, SI 1.2 s), accessibility 100 (76 audits: 15 passed, 51 n/a, 10 manual, 0 failed), best-practices 100 (21: 13 passed, 1 n/a, 7 informative). Audits below 1: first-contentful-paint 0.99, network-dependency-tree-insight and render-blocking-insight at weight 0 (the 3.1 kB stylesheet, est. 150 ms).
- Bundle size: total 40.76 kB gzipped (40,761 bytes) against the 60 kB limit, 19.24 kB headroom; script exit 0.
- axe on the real board (docs/evidence/axe.json, axe-core 4.13.0): 0 violations in all four scenes; rules run 62 (board) and 63 (sheet) per theme; passes 18/25; incomplete 0 on the board, 1 on the sheet (color-contrast on the 2 nodes of the task row straddling the sheet body's clip edge, verified by geometry: top 691/698, body bottom 727); inapplicable 44/37.
- npm audit --audit-level=low: "found 0 vulnerabilities" (568 packages, 5 production).
- Hardening script: all 8 checks passed (CSP verbatim; 0 inline scripts/handlers/javascript: URLs; 0 console matches in 4 app assets; sw.js exactly one console.warn = Workbox no-revision guard with every entry carrying a revision key; precache 13 entries / 9 unique covering index.html, manifest, all assets, icons; manifest standalone with 192/512/maskable; 0 eval/new Function).
- grep -rn "console\." src/ returns nothing; no-console is an ESLint error.
- actionlint: no findings.
- git status --porcelain empty after commit c2db8dd; pushed to origin/stage/7-hardening.

Deviations: 1. "No console in dist JS": the app's own assets have zero console references, but dist/sw.js contains one `console.warn` inside Workbox's precaching library (the guard for precache entries without revision info). It cannot execute because every injected manifest entry carries a `revision` key (asserted by scripts/hardening.mjs); removing the string would need a minifier option in the frozen vite.config.ts, so it is documented in TESTING.md and checked rather than stripped.
2. Lighthouse is not added to CI (only bundle-size and hardening steps were): performance scores on shared runners vary run to run and would make the gate flaky; the reproducible script plus docs/evidence/lighthouse.json is the record.
3. The bundle-size total counts the two lazy chunks (DevGallery, workbox-window) as well as the shell's own JS and CSS, giving the worst-case figure (40.76 kB) rather than the first-paint figure (35.53 kB); both are stated in TESTING.md.
4. The axe evidence file is written by the e2e test only when WARDBELT_EVIDENCE is set, so an ordinary `npm run test:e2e` (and CI) never touches a tracked file; the counts are always attached to the Playwright report.
5. No src/** changes were needed; the brief's allowance for targeted performance/a11y fixes was not used because every target scored 100 on the first run.

Open questions: 1. Stage 6 open question 1 (dev gallery lacks the export-textarea and import-error Settings states) is still open; the transfer e2e covers both states live, and stage 7 did not touch DevGallery since src/ was left unchanged. 2. The render-blocking-insight item (the 3.1 kB stylesheet, weight 0, est. 150 ms) could only be removed by inlining CSS via a Vite plugin or index.html change, both frozen; performance is 100 regardless. 3. The precache manifest lists the three icons and the manifest twice (13 entries, 9 unique) because includeAssets and the injectManifest glob both match them; harmless (Workbox dedupes) but a vite.config.ts tidy-up for a later stage if wanted. 4. Countdown chips and the header clock still refresh on TICK (up to 60 s apart), carried from stages 5 and 6; not a Lighthouse or axe finding.

Verifier (independent, 2026-09-09): PASS on first round. Gate: cd /Users/misbahshah/code/wardbelt-wt/stage-7-hardening (branch stage/7-hardening, HEAD c2db8dd, git status clean)
npm run lint -> exit 0 (eslint --max-warnings 0: no output; prettier: All matched files use Prettier code style!)
npm run typecheck -> exit 0 (tsc -p tsconfig.json and tsc -p tsconfig.sw.json clean)
npm test -> exit 0; Test Files 42 passed (42); Tests 483 passed (483); Duration 15.25s; coverage All files 99.18/97.1/98.41/99.32, src/domain 100% on all metrics (thresholds met)
npm run build -> exit 0; dist/assets/index-BgkVBKBj.css 14.93 kB (gzip 3.13), workbox-window.prod.es5-BBnX5xw4.js 5.75 kB (2.36), DevGallery-CT-gv7vt.js 8.15 kB (2.87), index-ClAHynj2.js 93.67 kB (32.40); sw.js precache 13 entries (125.73 KiB)
node scripts/bundle-size.mjs --no-build -> exit 0; OK: 40.76 kB gzipped, 19.24 kB under the limit
node scripts/hardening.mjs --no-build -> exit 0; all 8 checks pas
Probes: Gate: npm run lint (exit 0, eslint 0 problems, prettier clean), npm run typecheck (exit 0), npm test (42 files, 483 tests, exit 0, thresholds met), npm run build (exit 0, same asset hashes as TESTING.md: index-ClAHynj2.js, index-BgkVBKBj.css, DevGallery-CT-gv7vt.js, workbox-window.prod.es5-BBnX5xw4.js, 13 precache entries), npm run test:e2e (33 passed, 49.0 s, exit 0), actionlint on ci.yml and pages.yml clean. Coverage table: node scripts/coverage-table.mjs on my own npm test run reproduces every row of TESTING.md exactly (domain 548/548, 406/406, 108/108, 532/532; store 213/213, 78/81, 66/66, 202/202; scheduler 164/164, 89/89, 45/45, 155/155; ui/app 486/494, 292/308, 131/135, 479/485; ui 416/423, 341/358, 147/151, 410/416; all 1827/1842, 1206/1242, 497/505, 1778/1790). Regenerated coverage.json differs only in generatedAt and commit. Bundle size: node scripts/bundle-size.mjs --no-build reproduces 40.76 kB gzip total (2.87 + 3.13 + 32.40 + 2.36), 122.50 kB raw, sw.js 17.97/6.05 kB, exit 0. Regenerated bundle-size.json identical apart from timestamp and SHA. Lighthouse: node scripts/lighthouse.mjs --no-build ran npx lighthouse@13.4.1 (performance, accessibility, best-practices) and lighthouse@11.7.1 (pwa) against vite preview on 4173 with --chrome-flags=--headless=new and Google Chrome (HeadlessChrome/152.0.0.0), formFactor mobile, simulated throttling. Scores 100/100/100/100. Audit counts from the full reports match the table exactly (perf 49: 29 passed, 5 n/a, 12 informative; a11y 76: 15/51/10; BP 21: 13/1/7; PWA 9: 6 passed installable-manifest, splash-screen, themed-omnibox, content-width, viewport, maskable-icon, 3 manual). Metrics FCP 1.2 s, LCP 1.4 s, TBT 0 ms, CLS 0, SI 1.2 s. Audits below 1 identical (first-contentful-paint 0.99, network-dependency-tree-insight 
Verifier notes: Every figure in docs/TESTING.md reproduced within tolerance (all four Lighthouse scores exactly 100 again; every count in coverage, bundle, axe, audit and hardening identical). Points for the orchestrator, none a failure: (1) The evidence JSON files record commit dd6b370 because they were generated before the stage 7 commit existed; the coverage and e2e counts in them include the stage 7 tests that were uncommitted at that time, so "sources measured: commit dd6b370" is slightly imprecise. The numbers are reproducible at c2db8dd; the orchestrator may want to re-run the four --write scripts after merge so the SHAs point at main. (2) The EBADENGINE warning TESTING.md mentions did not print in my run because npx had 13.4.1 cached; the engines claim (node >=22.19) is correct. (3) The precache manifest lists manifest.webmanifest and the three icons twice with identical revisions (13 entries, 9 unique); this is a stage 0 vite-plugin-pwa includeAssets/glob overlap, harmless at runtime, and not in stage 7 ownership. (4) STATE.md has no stage 7 section, matching the established pattern where the orchestrator writes the report at merge (stages 5 and 6 implementer commits did not touch STATE.md either). (5) test-results/ and coverage/ are gitignored build products; I removed test-results and the temp Lighthouse directories and left dist/ and coverage/ in place.

### Stage 8: README, DECISIONS, pre-push gate, deploy (implementing subagent, 2026-09-09, commits 3c81d9a, 010af26 and aaf00d1 on stage/8-release)
Built:
Branch stage/8-release, worktree /Users/misbahshah/code/wardbelt-wt/stage-8-release, two commits on top of main 3aa07b1: 5ca912e "stage 8: README, DECISIONS, pre-push gate, deploy" and 3c81d9a "stage 8: gate evidence in TESTING.md and README" (pushed). 18 files, 1148 insertions, 24 deletions; nothing under src/, package.json, the lockfile, configs, index.html or .github/ changed.

- README.md (dual register). Nurse half: what it is, install on Android Chrome (six numbered steps, the Pages URL https://smuddassirshah-cpu.github.io/wardbelt/), add a patient and work the belt (cell states, the 19 codes, board order), what the vibration means (one short buzz = step done, two short = belt complete, two long = check due, late on wake), summary/export/settings (weekly export, nudge, purge, theme, second tab, update bar), lock the phone (holds client-identifiable data). Engineer half: architecture tree matching PLAN.md section 3 plus src/ui/app, data flow, stack and why, Getting started (npm ci, dev, test, test:e2e, build, lint, typecheck, evidence scripts), pre-push gate and hook install by symlink or core.hooksPath (nothing committed), Evidence bullets, Deploy (ci.yml, pages.yml, VITE_BASE_PATH, the Pages source setting), Data and privacy (what/where/retention/export/validation/no network/CSP/no logging/two tabs), Limitations (PLAN section 9 plus two implementation limits), Licence, and a 42-row Claims and evidence table mapping every numeric or behavioural claim to PLAN.md, TESTING.md, DECISIONS.md or the test/script file. Six screenshots embedded with relative paths and descriptive alt text. The PLAN.md section 2 CV line is omitted (see decisions).
- docs/screenshots/ (six PNGs, 74,914 to 149,174 bytes, all under 300 kB): board-light, board-dark, patient-sheet, add-patient, summary, settings, from the Pixel 5 profile under page.clock at 10:00 Tue 10 Mar 2026.
- tests/e2e/screenshots.spec.ts (1 test): seeds four synthetic patients through the real Add sheet (dog through theatre with check 1 done on time and check 2 overdue, cat in pre-op with a done and a skipped cell and a 10:00 intake chip, fresh rabbit, discharged "other" shown via the board toggle), dismisses the export nudge, asserts each state, the summary values ['10','1','100%','1','4','1',/min$/], the dark background, and captures with animations disabled; writes to docs/screenshots only when WARDBELT_EVIDENCE is set, otherwise to the Playwright output dir; asserts every PNG under 300,000 bytes and attaches them.
- tests/e2e/install.spec.ts (3 tests): manifest fetched from the page's own manifest link (base derived, so it holds on /wardbelt/): name, short_name, display standalone, start_url and scope equal the base, theme_color #0F6E56 matching the light theme-color meta, background_color, icons include 192x192, 512x512 and maskable 512x512 and every icon is fetched with 200, image/png and the PNG signature; manifest link and apple-touch-icon present, the icon fetched as PNG; navigator.serviceWorker.ready resolves with the app scope and an active worker, and after reload the controller is <base>sw.js.
- docs/DECISIONS.md: 167 entries in one table per stage (Id, Date, Decision, Reason, Alternatives considered, Deviation): all 148 STATE.md Decisions lines (stage 0: 15, 1: 14, 2: 18, 3: 17, 4: 38, 5: 18, 6: 18, 7: 10), two labelled extras sourced from the scaffold and the stage 4 report (0.16 custom task codes, 4.39 purge days 1 to 365), 11 stage 8 decisions, and the six PLAN.md section 9 trade-offs.
- scripts/pre-push.sh (executable, bash 3.2 compatible): preview port 4173 must be free (so Playwright's webServer always builds and serves fresh), npm run lint, typecheck, test, build, node scripts/bundle-size.mjs --no-build, node scripts/hardening.mjs --no-build, node scripts/check-base-path.mjs, npm run test:e2e, gitleaks detect --source . --no-banner (full history), clean clone of HEAD into $WARDBELT_TMP or mktemp then npm ci, build, test (clone removed on exit); stops at the first failure and prints a timed PASS/FAIL summary.
- scripts/check-base-path.mjs: builds with VITE_BASE_PATH=/wardbelt/ into a temporary outDir (dist/ untouched) and asserts manifest start_url and scope /wardbelt/, every index.html asset reference under /wardbelt/assets/, the manifest link at /wardbelt/manifest.webmanifest, no absolute reference outside the base, /wardbelt/sw.js referenced by the bundle, sw.js emitted; 7 checks, exit 1 on any failure; --base and --keep flags.
- docs/evidence/*.json regenerated once at the branch base 3aa07b1 before any stage 8 change (bundle-size, hardening, coverage, lighthouse via the four --write scripts; axe via WARDBELT_EVIDENCE=1 a11y spec): every count identical to stage 7 except Lighthouse metrics (LCP 1.5 s, TBT 10 ms, max-potential-fid 0.99 at weight 0; scores still 100/100/100/100).
- docs/TESTING.md: header now names 3aa07b1 as the measured commit, Lighthouse rows updated from the re-run, the "No secret in the tree" row filled in (21 commits scanned, no leaks), a new "Stage 8 gate" section with the pre-push summary and a table for the e2e count (37), the base-path check, gitleaks and the clean clone, and three more "How to reproduce" items.
- .github/workflows unchanged; actionlint clean; pages.yml verified correct through check-base-path.mjs.

DoD evidence (actual runs, after fixes):
Isolation repeat of the previously racy spec (fixed tree): `npx playwright test tests/e2e/dev-gallery.spec.ts --repeat-each 15` -> 180 passed (2.3m), 0 failed (verifier had 1 of 15 failing before the fix).

scripts/pre-push.sh in the worktree with the fixes (before commit), exit 0, log at /private/tmp/claude-501/-Users-misbahshah-code-wardbelt/3141a7bf-499a-45b2-b7db-535601784f61/scratchpad/gate-fix.log:
==== pre-push summary (stage-8-release, HEAD 3c81d9a) ====
PASS  preview port 4173 is free (0 s)
PASS  npm run lint (7 s)
PASS  npm run typecheck (3 s)
PASS  npm test (11 s)
PASS  npm run build (2 s)
PASS  bundle size (scripts/bundle-size.mjs) (0 s)
PASS  hardening checks (scripts/hardening.mjs) (0 s)
PASS  Pages base path (scripts/check-base-path.mjs) (1 s)
PASS  npm run test:e2e (57 s)
PASS  gitleaks detect (full history) (0 s)
PASS  clean clone: npm ci, build, test (20 s)
total 101 s
pre-push gate GREEN
Details from that log: npm test Test Files 42 passed (42), Tests 483 passed (483), coverage Statements 99.18% (1827/1842), Branches 97.1% (1206/1242), Functions 98.41% (497/505), Lines 99.32% (1778/1790), thresholds met; bundle "OK: 40.76 kB gzipped, 19.24 kB under the limit"; hardening "all 8 checks passed"; base path "all checks passed for base /wardbelt/"; e2e "37 passed (56.6s)"; gitleaks "22 commits scanned. no leaks found"; clean clone 42 files / 483 tests passed.

Verifier's failure-2 repro, now green, against a fresh `git clone` of the committed fixes (b0f5713, later amended only to add the TESTING.md paragraph, now 010af26) with `npm ci` (518 packages) then `ln -s ../../scripts/pre-push.sh .git/hooks/pre-push` and `.git/hooks/pre-push origin https://example.invalid/wardbelt.git` from the clone root: exit 0, "==== pre-push summary (hook-probe, HEAD b0f5713) ====", all 11 steps PASS (lint 8 s, typecheck 4 s, test 13 s, build 2 s, bundle 0 s, hardening 0 s, base path 1 s, e2e 57 s, gitleaks 0 s, clean clone 17 s), total 102 s, "pre-push gate GREEN". Same clone through the second documented install (`git config core.hooksPath <dir>` with a symlink there, invoked as `<dir>/pre-push origin <url>`): exit 0, all 11 steps PASS, total 99 s, "pre-push gate GREEN". Logs: scratchpad/hook-probe.log and hook-probe2.log.

Post-commit: `npm run lint` on the committed tree -> eslint clean, "All matched files use Prettier code style!"; `git status --porcelain` empty after push; no em-dash in any changed file (grep count 0). Screenshot PNGs untouched (no WARDBELT_EVIDENCE run).

Deviations: 1. docs/TESTING.md was edited although it is not in the stage 8 ownership list: the header lines now name the re-run commit 3aa07b1, the Lighthouse table and metrics quote the re-run where it differed (28 passed / 4 below 1, LCP 1.5 s, TBT 10 ms, max-potential-fid 0.99 at weight 0; scores unchanged at 100), the "No secret in the tree" row is filled in, and a "Stage 8 gate" section plus three "How to reproduce" items were added. Every number is copied from a run output, none retyped. The stage 7 Gate table row for e2e still records 33 (the stage 7 run); the stage 8 section records 37.
2. Two commits instead of one, both prefixed "stage 8:", so that the TESTING.md stage 8 section could quote the gate run at a SHA (5ca912e) that remains in history rather than amending it away; the gate was re-run on the final commit 3c81d9a with the same result.
3. docs/evidence/axe.json carries a timestamp but no commit field, because tests/e2e/a11y.spec.ts (stage 7 ownership) writes only generatedAt; left as is rather than editing a stage 7 test.
4. The screenshot scene seeds four patients rather than the brief's "two or three": the discharged patient leaves the board by design, so a fourth, in-progress patient was added to show the done and skipped cells and the intake chip; the discharged row is shown through the board's own toggle.
5. Screenshots are written to docs/screenshots/ only under WARDBELT_EVIDENCE (the stage 7 axe switch); an ordinary npm run test:e2e writes them to the Playwright output directory, so CI and the pre-push gate never dirty the tree. The brief did not specify this.
6. scripts/check-base-path.mjs builds into a temporary outDir instead of building into dist/ and restoring the default build afterwards; dist/ is never touched, which satisfies "restore the default build" trivially and keeps the e2e that follows on the default build.
7. scripts/pre-push.sh runs two steps the brief's list did not name (hardening.mjs, required by "call the existing scripts", and check-base-path.mjs, required by the deploy item) and a preview-port check before lint (required by "do not run e2e with a stale preview server").
8. The PLAN.md section 2 CV line is not in the README: its "deployed via GitHub Actions" part is not evidenced in TESTING.md at the time of writing.
9. docs/DECISIONS.md contains two entries that are not STATE.md Decisions lines (0.16 custom task codes from the scaffold source; 4.39 purge days 1 to 365 from the stage 4 report), both labelled with their source, because README claims needed them for traceability; decision 1.3 and 3.12 texts were extended with the DUE_SOON_MS (5 minutes) and DUE_VIBRATION (200, 100, 200 ms) values so the README's vibration and board-order claims trace to docs/.
10. The install spec accepts an active worker in state "activating" as well as "activated" (serviceWorker.ready resolves mid-activation on the first load; the first run asserted "activated" and failed).
Fix round: Fix round for stage 8 on branch stage/8-release (worktree /Users/misbahshah/code/wardbelt-wt/stage-8-release), one commit 010af26 "stage 8: fixes after verification", pushed (3c81d9a..010af26, fast-forward). All four verifier failures fixed:
1. Blocker (gate red on npm run test:e2e): /Users/misbahshah/code/wardbelt-wt/stage-8-release/tests/e2e/dev-gallery.spec.ts now reads the body background with `await expect.poll(() => bodyBackground(page)).toBe(...)` at all four sites (lines 69, 75, 83, 86: after openGallery in light, ?theme=dark and prefers-color-scheme dark, and after the Light button click); header note explains that DevGallery applies data-theme in a useEffect flushed after paint. No src/** change (the app was correct).
2. Major (hook installs ran the gate from the wrong directory): /Users/misbahshah/code/wardbelt-wt/stage-8-release/scripts/pre-push.sh line 14 is now `ROOT="$(git rev-parse --show-toplevel)"` (was dirname of the symlink's own path) with a two-line comment; README.md hook section now states the script locates the repository through git so both documented installs and a by-hand run from any directory inside the checkout work.
3. Major (patient-sheet alt text): README.md line 57 alt text rewritten to match docs/screenshots/patient-sheet.png (In theatre done 10:00, Handover from theatre done 10:14, Post-op check 1 done 10:14, Post-op check 2 current with red-outlined cell, Due 10:30 in red, Done and Skip; checks 3 and 4 due 10:45 and 11:00; Food and water, Take out, Pain score to do; Note buttons; clock 10:31). The other five alt texts were checked against their PNGs (board-light verified by eye) and left as they were.
4. Minor (99.19 vs 99.18): /Users/misbahshah/code/wardbelt-wt/stage-8-release/scripts/coverage-table.mjs now floors percentages to two decimals (istanbul's convention, `Math.floor((10_000 * covered) / total) / 100`) and computes coverage.json allFiles with the same function, so table, JSON and the vitest printout agree. Re-ran npm test and `node scripts/coverage-table.mjs --write`: docs/evidence/coverage.json regenerated (commit 3c81d9a, same counts; store branches 96.29, ui/app branches 94.80 and functions 97.03, ui statements 98.34 and lines 98.55, all files 99.18/97.10/98.41/99.32, exactly the figures `npm test` prints). docs/TESTING.md coverage table and preamble updated from that output; README.md lines 248 and 372 now say 99.18%.
Also: docs/DECISIONS.md gained entries 8.12 (ROOT via git), 8.13 (floor convention) and 8.14 (expect.poll), stage 8 table re-aligned, 8.10's entry count updated to 170; README claims table row for the pre-push gate now cites 8.12; docs/TESTING.md Stage 8 gate section gained a "Fix round after verification" paragraph recording the three green gate runs and the 15x repeat.

Open questions: 1. Deploy: the README states the app URL as https://smuddassirshah-cpu.github.io/wardbelt/ (repository owner and name, pages.yml base /wardbelt/); the repository's Pages source must be "GitHub Actions" for deploy-pages to publish, which the run cannot set. After the orchestrator verifies the deployed manifest and sw.js, the CV line could be added to the README with a TESTING.md line recording that check. 2. Lighthouse varies run to run in the weight-0 audits and in LCP/TBT (stage 7: LCP 1.4 s, TBT 0 ms, 29 passed; stage 8 re-run: 1.5 s, 10 ms, 28 passed); scores are 100 in every run. TESTING.md now quotes the re-run and notes the stage 7 values. 3. The Settings screenshot shows Notifications as "Blocked in browser settings" because headless Chromium reports the permission as denied (stage 6 open question 2); the alt text says so. 4. The overdue chip in the board screenshots reads "C2 overdue 01:01" (real seconds elapse between clock jumps); regenerating with WARDBELT_EVIDENCE may shift a few pixels or the final digit, so PNG diffs are expected on regeneration and nothing else rewrites them. 5. docs/evidence/axe.json has no commit stamp (a stage 7 test writes it); adding one needs an edit to tests/e2e/a11y.spec.ts. 6. Carried from stage 7, unchanged: the dev gallery lacks the export-textarea and import-error Settings states; the precache lists the manifest and icons twice (13 entries, 9 unique); countdown chips refresh on TICK (up to 60 s), which the README now states as a limitation. 1. docs/evidence/axe.json has no commit field (the other four evidence files do); if the orchestrator wants it, tests/e2e/a11y.spec.ts needs a git rev-parse stamp and one WARDBELT_EVIDENCE re-run. 2. STATE.md stage 7 report quotes the per-directory coverage figures under the old rounding (store 96.30, ui/app 94.81/97.04, ui 98.35/98.56); TESTING.md and coverage.json now carry the floored figures for the same counts (96.29, 94.80/97.03, 98.34/98.55); STATE.md is the orchestrator's file and records a historical run, so it was not touched. 3. The README export-nudge sentence (line 105, 'anything on the board') is imprecise against decision 6.4 (patients of any status, including hidden discharged ones); harmless but could be tightened when the orchestrator next edits the README.

Verifier (independent, 2026-09-09): the first verifier was cut off by a usage limit before reporting; a fresh verifier then ran round 1: FAIL with 4 findings (blocker: Pre-push gate is red: npm run test:e2e fails on a non-polling theme assertion (race with Preact useEffect); major: Both documented pre-push hook installs run the gate from the wrong directory and always go red; major: README patient-sheet screenshot alt text describes a scene the PNG does not show; minor: All-files statement coverage quoted as 99.19% while docs/evidence/coverage.json records 99.18). Round 2 after the implementer's fixes: FAIL with one minor finding (README says every evidence script takes --no-build and --write, but check-base-path.mjs takes neither and coverage-table.mjs has no build to skip), which escalated to the orchestrator per CLAUDE.md. Orchestrator fix (commit aaf00d1): README evidence-script sentence states the flags each script takes; the claims-table row for the check timings points at the flows e2e test; pre-push.sh removes the mktemp parent after the clean clone. Orchestrator re-ran scripts/pre-push.sh on the branch: all 11 steps PASS, 95 s, "pre-push gate GREEN" (lint, typecheck, 483 unit tests, build, bundle 40.76 kB, hardening 8/8, base path, 37 e2e, gitleaks full history no leaks, clean clone npm ci/build/test).
Round 2 gate: $ cd /Users/misbahshah/code/wardbelt-wt/stage-8-release && WARDBELT_TMP=<scratchpad>/verify8 scripts/pre-push.sh   (HEAD 010af26, 2026-09-09)

==> preview port 4173 is free
==> npm run lint            eslint . --max-warnings 0: 0 problems; prettier: All matched files use Prettier code style!
==> npm run typecheck       tsc -p tsconfig.json and tsc -p tsconfig.sw.json clean
==> npm test                Test Files 42 passed (42), Tests 483 passed (483), 11.16 s; All files 99.18 / 97.1 / 98.41 / 99.32; src/domain 100/100/100/100 (thresholds met)
==> npm run build           dist/assets: DevGallery-CT-gv7vt.js, index-BgkVBKBj.css, index-ClAHynj2.js, workbox-window.prod.es5-BBnX5xw4.js; sw.js 13 pr
Verifier notes: The single failure is a one-sentence README inaccuracy (the intro line above the evidence-scripts block claims flags two of the listed scripts do not have); everything else in the DoD, the brief, PLAN.md and CLAUDE.md checked out, and the fix needs only a README edit plus `npm run lint` (prettier) rather than a full gate re-run. Notes for the orchestrator: (1) The claims table row for '+15/30/45/60' cites 'docs/TESTING.md hardening checklist (flows e2e theatre checks)', a row that does not exist in that checklist; the PLAN.md section 6 citation on the same row is correct and the flows e2e 'completing in theatre schedules the four checks at +15/30/45/60' passed, so the claim is traceable, only the secondary pointer is loose. (2) The screenshot scene seeds four synthetic patients where the brief said two or three (DECISIONS 8.3); it covers every requested state and the discharged toggle. (3) scripts/pre-push.sh clones into WARDBELT_TMP when set, else into a mktemp directory; only the clone subdirectory is removed, so an empty mktemp parent is left in the system temp dir when the variable is unset. check-base-path.mjs builds into the system tmpdir and removes it. (4) gitleaks' default run scans the 23 non-merge commits; with -m over the 7 merges all 30 are clean. (5) DECISIONS.md marks 4.1, 4.3 and 4.6 as PLAN deviations where the STATE.md stage 4 lines carried no flag; that is a 

## Decisions
Running log. One line each: date, decision, reason, PLAN.md deviation? y/n.
- 2026-09-08, TypeScript 5.9.3 rather than 7.x, typescript-eslint 8.70 peer range is <6.1 and TS 6 changed tsconfig defaults, n.
- 2026-09-08, Vite 7.3.6 and Vitest 4.1.11 rather than 8.x/5.x, vitest 5.0.0 was five days old at scaffold time and the 4.x line is the mature one, n.
- 2026-09-08, jsdom 29.1.1 rather than 30, jsdom 30 needs node >=22.22 and the build machine runs 22.16, n.
- 2026-09-08, added dev deps beyond section 10: @vitest/coverage-v8 4.1.11 (Vitest's own provider, needed for the stage 1 coverage gate), @types/node 22.20.1 (process.env in configs), workbox-precaching and workbox-routing 7.4.1 (Workbox is named in section 4; pinned explicitly instead of relying on hoisting from workbox-build). All maintained, released within the last month, MIT, y.
- 2026-09-08, eslint-plugin-preact omitted (last release 2022, v0.1.0, unmaintained); section 7 rules enforced with no-restricted-syntax selectors in eslint.config.js, n (plan allows the maintained equivalent).
- 2026-09-08, ESLint 10 no longer ships @eslint/js so the core rule set is listed explicitly in eslint.config.js, n.
- 2026-09-08, no @preact/preset-vite; JSX via esbuild automatic runtime with jsxImportSource preact, n.
- 2026-09-08, template has 19 steps: section 6 enumerates 19 keys and 19 codes although section 1 says "17-step"; the enumerated list is binding, y (documented discrepancy).
- 2026-09-08, Action union extended beyond the section 6 list with SET_NOTE, SET_SETTINGS, PURGE_DISCHARGED, RESET and DUE, which the section 3/5/8 flows (notes, settings, purge, wipe, scheduler due) require; mutating actions carry `at` and `eventId` from the caller so the reducer stays pure, y.
- 2026-09-08, template task ids are `${patientId}:${key}`; custom task ids are supplied by the caller, n.
- 2026-09-08, package-lock.json generated with npm 11.19.1 via npx because the bundled npm 10.9.2 crashes in arborist ("edgesOut" of null) on Vitest's optional peer graph; `npm ci` with 10.9.2 verified clean from the resulting lockfile, n.
- 2026-09-08, pages.yml deploys on `workflow_run` success of CI on main (plus manual dispatch) so a red e2e never deploys, n.
- 2026-09-08, in-app routes use the URL hash (`#/dev`, `#/summary`, `#/settings`) so the SW navigation fallback and the Pages sub-path need no special handling, n.
- 2026-09-08, Stage 0 owns src/domain/types.ts and template.ts as the cross-stage contract; stage 1 may add to them but may not change exported shapes without reporting, n.
- 2026-09-08, stage reports are returned by each subagent as structured output and written into STATE.md by the orchestrator verbatim, so four parallel branches never conflict on this file, n.
- 2026-09-08, stage 1: Tiebreaks in compareUrgency compare the stored ISO strings lexicographically (normalised UTC so chronological) rather than parsed ms, so the order is total even for unparseable dates; PLAN.md silent, n.
- 2026-09-08, stage 1: medianAdmitToDischargeMin is returned unrounded (fractional minutes); the summary screen formats it; PLAN.md silent, n.
- 2026-09-08, stage 1: Rank 1 (due soon) boundary is inclusive: dueAt - now <= DUE_SOON_MS; rank 0 is dueAt <= now; PLAN.md silent, n.
- 2026-09-08, stage 1: PATIENT_DELETED event type stays unused: DELETE_PATIENT removes the patient and its events and appends nothing (per brief), n.
- 2026-09-08, stage 1: Completing or skipping tasks on a discharged patient is allowed (only SET_THEATRE_RETURN is refused for discharged patients, per brief); PLAN.md silent, n.
- 2026-09-08, stage 1: currentTask picks the minimum-order unfinished task by scanning, not the first array element, so imported records with unsorted tasks still behave; PLAN.md silent, n.
- 2026-09-08, stage 1: Exhaustive reducer switch keeps a default that routes a never-typed action to ignoreUnknown returning the same state, so an unknown persisted action type is ignored at run time and the coverage gate needs no exclusion comment; n.
- 2026-09-08, stage 1: Custom task inserted into an empty task list gets phase PRE_OP; otherwise inherits the phase of the task it follows (per brief), n.
- 2026-09-08, stage 1: Stats collects undoOf ids from every UNDO event regardless of the shift window, so an undo performed after 04:00 still nullifies the shift's event; PLAN.md silent, n.
- 2026-09-08, stage 1: revertTask on in_theatre only clears theatre return when the task was done, not skipped; deviation from brief wording, y.
- 2026-09-08, stage 1: UNDO of a discharge completion emits a second UNDO event for the derived DISCHARGED event; deviation from brief wording, y.
- 2026-09-08, stage 1: Reducer ignores actions with unparseable timestamps instead of throwing; addition, y.
- 2026-09-08, stage 1: Property test (a) uses a violations() string list with one assertion per step instead of hundreds of expects, bringing the suite from 11 s to 0.3 s; n.
- 2026-09-08, stage 1: Test-only xorshift32 PRNG and deepFreeze live in tests/unit/domain/helpers.ts (not a test file, outside coverage include); n.

- 2026-09-08, stage 2: 2026-09-08, db.ts issues factory.open() on an injected IDBFactory and uses idb's `wrap` rather than idb's openDB, because openDB reads the global indexedDB and the brief requires an injectable factory (undefined simulates private mode), n.
- 2026-09-08, stage 2: 2026-09-08, `blocked` rejects immediately with BlockedError and a later success closes the stray connection; PLAN section 8 says memory mode with no retry loop, n.
- 2026-09-08, stage 2: 2026-09-08, all repo writes are serialised through one promise chain so a retried write can never overwrite a newer write to the same record; the retry queue is flushed in submission order ahead of the newly submitted write, which is the only order that keeps the same-record guarantee, n.
- 2026-09-08, stage 2: 2026-09-08, write_failed is re-emitted on every subsequent submit while the backlog still fails, so the banner state stays current; recovered is emitted once when the queue drains, n.
- 2026-09-08, stage 2: 2026-09-08, settings are one logical record: rows assembled over DEFAULT_SETTINGS so missing rows take defaults without being counted; any invalid value or malformed row marks the settings corrupt once and keeps every settings row in rawCorrupt, n.
- 2026-09-08, stage 2: 2026-09-08, saveSettings writes one row per defined Settings field inside one clear-then-put transaction, so an undefined lastExportAt removes its row, n.
- 2026-09-08, stage 2: 2026-09-08, deletePatient scans the events store with a cursor in the same readwrite transaction (no patientId index in the binding schema), O(n) over events, n.
- 2026-09-08, stage 2: 2026-09-08, error reasons are duck-typed from {name, message} because a DOMException is not instanceof Error across realms (jsdom, workers), n.
- 2026-09-08, stage 2: 2026-09-08, exportRawStore wraps the rows in {schemaVersion, kind: 'raw-corrupt-records', records} so a recovery file is self-describing, n.
- 2026-09-08, stage 2: 2026-09-08, the unavailable notice is the only one replayed on subscribe, as the brief specifies; corrupt and write_failed are point-in-time, n.
- 2026-09-08, stage 2: 2026-09-08, write failures in tests are simulated by spying IDBObjectStore.prototype.put with mockImplementationOnce that routes through fake-indexeddb's _execRequestAsync with a throwing operation, so the request error event, transaction abort and idb rejection are all genuine; a sync-throw variant is also covered, n.
- 2026-09-08, stage 2: 2026-09-08, events sorted from a store getAll() rather than the at index so corrupt rows without a valid at key are still counted (see deviations), y.
- 2026-09-08, stage 2: 2026-09-08, a settings row whose key is unknown (including `__proto__` and `constructor`) is dropped and not counted as corrupt, consistent with PLAN.md section 7 "unknown fields dropped" and the existing handling of forward-compatible keys; a row that is not a `{key: string}` object still marks the settings record corrupt, n..
- 2026-09-08, stage 2: 2026-09-08, `inTransaction` decides whether to call `tx.abort()` by `tx.error === null`: a request failure sets `error` while aborting on its own (fake-indexeddb finishes the transaction before the rejection reaches user code, so a second abort would throw InvalidStateError), whereas a synchronous throw leaves `error` null and the transaction active; the transaction's `done` rejection is observed with no-op handlers because it carries the same failure the body already propagates, n..
- 2026-09-08, stage 2: 2026-09-08, requests within one transaction are awaited one at a time rather than via Promise.all because idb v8 attaches no handler to request promises and a pending sibling would reject unhandled when the transaction aborts; at most six requests per transaction so cost is negligible, n..
- 2026-09-08, stage 2: 2026-09-08, regression tests assert the absence of unhandled rejections explicitly with a `process.on('unhandledRejection')` capture plus two setImmediate flushes, rather than relying only on Vitest's run-level error reporting, so the assertion sits next to the behaviour under test, n..
- 2026-09-08, stage 2: 2026-09-08, the clearAll atomicity test makes the first store clear throw (both attempts) rather than the second, because passing the untouched calls through to the original method would require holding an unbound method reference that the lint config rejects; rollback of a completed request in the same transaction is exercised by the deletePatient and saveSettings tests, n..
- 2026-09-08, stage 2: 2026-09-08, deleted the verifier's untracked scratch file tests/unit/store/probe.test.ts from the worktree (never committed, used console.log and would fail lint); its two scenarios are now covered by committed tests, n..
- 2026-09-08, stage 3: 2026-09-08, timers delay excludes tasks already announced at their current dueAt while nextDueAt() keeps the plain section 8 meaning, an announced overdue task would otherwise re-arm a 0 ms timer in a loop, n (brief wording only).
- 2026-09-08, stage 3: 2026-09-08, newly due tasks are dispatched and listed most overdue first (sorted by dueAt, ties in patient then task order), gives the five-line notification cut a meaningful order and is independent of patient map iteration order, n.
- 2026-09-08, stage 3: 2026-09-08, onVisible() sweeps whether or not timers are started and re-arms only when running, so boot order start()/onVisible() does not matter and a stopped scheduler never arms a timer, n.
- 2026-09-08, stage 3: 2026-09-08, the announced set is kept across stop()/start() and across backwards clock jumps; a task re-announces only when it leaves todo (including undo back to todo) or its dueAt changes, matches the brief rule and avoids double alerts after an NTP correction, n.
- 2026-09-08, stage 3: 2026-09-08, fire and onVisible re-arm in a finally block so a throwing dispatch or notifier propagates (never swallowed) but does not kill the timer chain, n.
- 2026-09-08, stage 3: 2026-09-08, realClock uses window.setTimeout/clearTimeout (numeric ids in browsers); under vitest jsdom Node timers back window.setTimeout so the runtime id is a Timeout object there, and the unit test asserts only the arm-then-clear round trip, n.
- 2026-09-08, stage 3: 2026-09-08, renotify is absent from the TS DOM lib NotificationOptions; a local DueNotificationOptions extension carries it and is passed as a variable so the brief's showNotification signature is unchanged, n.
- 2026-09-08, stage 3: 2026-09-08, browserNotifyDeps uses navigator.serviceWorker.getRegistration() rather than .ready because ready never resolves when no worker is registered (dev server), an undefined registration routes to onError, n.
- 2026-09-08, stage 3: 2026-09-08, a rejecting Notification.requestPermission is caught, routed to onError as plain English and the current state returned, n.
- 2026-09-08, stage 3: 2026-09-08, due([]) is a complete no-op (no vibration, no notification); timers never calls it empty anyway, n.
- 2026-09-08, stage 3: 2026-09-08, onError reasons append the platform error message when it is a non-empty Error message (browser text, never patient data), n.
- 2026-09-08, stage 3: 2026-09-08, additive exports beyond the binding interfaces: DEFAULT_MAX_DELAY_MS, dueTitle, dueBody, DUE_TAG, DUE_VIBRATION, DUE_BODY_LINES, so Settings and tests share the copy, n.
- 2026-09-08, stage 3: 2026-09-08, fake clock timers kept in an array sorted by (due, sequence) with binary-search insert and shift from the front, O(log n) search per arm and O(1) pop per fire, n.
- 2026-09-08, stage 3: Prune `notified` inside arm() rather than on every reschedule() only: arm is the single choke point reached by start, reschedule, fire and onVisible, so every path that re-arms sees a map consistent with the board; same O(tasks) walk arm already performed, PLAN.md deviation n..
- 2026-09-08, stage 3: Share one generator (timedTodo) across sweep, prune and nextDueAt instead of three hand-written loops: removes duplicated filtering and keeps the unparseable-dueAt rule in one place, PLAN.md deviation n..
- 2026-09-08, stage 3: realClock test spies on window.setTimeout/clearTimeout via a `const win: Window = window` binding (a widening assignment, not a cast) because the `Window & typeof globalThis` intersection resolves the mock signature to Node's NodeJS.Timeout overload under @types/node, PLAN.md deviation n..
- 2026-09-08, stage 3: Added a third regression test (announced overdue task completed then undone, re-announced on onVisible) beyond the two the verifier asked for, because it exercises the prune path with a task already overdue at arm time, PLAN.md deviation n..

- 2026-09-08, stage 4: Dark --colour-on-accent is #121212 (the dark background) because white on the lightened accent #3FB48F is 2.58:1; the done-cell glyph therefore reads dark-on-green in theatre mode..
- 2026-09-08, stage 4: Dark accent, warning and danger are the brief's suggested values exactly (#3FB48F, #E0A83A, #F08A80); all pass 4.5:1 against both #121212 and #1A1A1A with margin (lowest is danger on surface at 7.17:1)..
- 2026-09-08, stage 4: Light warning #B7791F is a non-text token (3:1 floor); a separate --colour-warning-text #8F5E10 carries warning text; in dark both are #E0A83A..
- 2026-09-08, stage 4: Hatch and scrim are tokens (--colour-hatch ink at 30%, --colour-scrim) so ui.css never needs an rgba literal; the no-literal-colours test also bans hsl() and scans .ts files, not only .css and .tsx..
- 2026-09-08, stage 4: Motion tokens are zeroed under prefers-reduced-motion in tokens.css and ui.css additionally forces animation-duration 0s, transition-duration 0s and animation-iteration-count 1 with !important on every element, so third-party or future rules cannot reintroduce motion..
- 2026-09-08, stage 4: The overdue pulse period (2000 ms) is a token excluded from the 400 ms cap because only about 16% of the cycle moves; the visible motion is a scale(1.2) bump, not a colour or border change..
- 2026-09-08, stage 4: The current belt cell button is 48 px with -12 px margins so the 24 px cell rhythm is kept; the belt scroller carries matching 12 px padding and negative margin so the hit area is not clipped, and the row header sits at z-index 1 above the scroller padding so its own 48 px target stays live; the button is z-index 2..
- 2026-09-08, stage 4: A belt with no current task has no button, so the scroller itself gets tabIndex 0 to satisfy axe's scrollable-region-focusable rule (WCAG 2.1.1)..
- 2026-09-08, stage 4: Belt cells group by contiguous phase in a single pass on the assumption that tasks arrive in order and a custom task carries its neighbour's phase (STATE.md id and template decisions)..
- 2026-09-08, stage 4: Only the current cell is a button; all other cells are aria-hidden spans and the belt carries the summary aria-label ('7 of 19 done, current: Post-op check 1' or 'nothing left to do')..
- 2026-09-08, stage 4: The timer chip prefixes the two-letter code with a visually hidden task label so screen readers hear 'Post-op check 1 C1 overdue 05:00'; formatCountdown lets minutes exceed 59 rather than rolling to hours..
- 2026-09-08, stage 4: The row is 88 px as 8 px padding + 48 px header button + 24 px belt + 8 px padding with the 2 px progress bar absolutely positioned on the bottom edge; the header is a single button wrapping name, species, kennel, procedure and chips..
- 2026-09-08, stage 4: Sheet handles Escape on the panel element (fires only while focus is inside), closes on scrim click, restores focus only if the previous element is still connected, and has no focus trap; inline mode drops the scrim, aria-modal and focus management so the gallery can show every sheet at once..
- 2026-09-08, stage 4: Notes (patient and per-task) save on blur through a last-saved ref so blur followed by Save never fires onSetNote twice; values are trimmed before comparison..
- 2026-09-08, stage 4: Per-task notes are edited through a Note toggle button (aria-expanded) on every task row, since onSetNote(taskId, note) implies per-task editing that the brief did not lay out..
- 2026-09-08, stage 4: The add-task datetime-local value is handed to validateCustomTask as-is because the validator's optIso normalises local time to ISO UTC; the theatre return input is parsed with parseIso and rejected inline when empty..
- 2026-09-08, stage 4: Insert-after defaults to the current task, or to the last task when the belt is complete..
- 2026-09-08, stage 4: The Discharge button becomes disabled and reads 'Discharged HH:MM' once the patient is discharged..
- 2026-09-08, stage 4: Owner phone renders as a tel: link with spaces stripped from the href, styled as a quiet 48 px button..
- 2026-09-08, stage 4: AddPatientSheet inputs are uncontrolled and read with FormData once at submit so validatePatientForm is the single validation point; aria-required is used instead of required because the form is noValidate..
- 2026-09-08, stage 4: Species, sex and intake are segmented radio groups with visually hidden inputs and 48 px labels, defaults dog, unknown and none preselected; sex options are labelled M, MN, F, FN, Unknown..
- 2026-09-08, stage 4: The Notifications toggle is disabled and shown unchecked when permission is denied or unsupported regardless of settings.notifications; turning it on in the default state calls onChange then onRequestNotifications..
- 2026-09-08, stage 4: Import reads with file.text() and falls back to FileReader; the text is handed up unparsed for the store to validate; the file input is hidden, aria-hidden and tabIndex -1 behind a 48 px Import button; the input value is cleared in finally so the same file can be chosen again..
- 2026-09-08, stage 4: Settings shows a 'Never exported.' or 'Last export <en-GB locale string>.' line from settings.lastExportAt..
- 2026-09-08, stage 4: ConfirmButton is shared by patient delete and Delete everything; it arms for CONFIRM_WINDOW_MS 4000 ms, offers Keep to disarm, and clears its timer on disarm and unmount..
- 2026-09-08, stage 4: Toast clamps durationMs to TOAST_MAX_MS 4000 and to a minimum of 0 so no caller can keep a toast longer than 4 s..
- 2026-09-08, stage 4: Banner tone changes only the hairline colour; text stays in ink so contrast never depends on warning or danger..
- 2026-09-08, stage 4: feedback.ts: reducedMotion does not silence vibration or sound (neither is on-screen motion); the AudioContext is a module-level lazy singleton resumed when suspended; vibrate and AudioContext are typed through local structural interfaces rather than any; resetAudioForTests is exported as a test seam..
- 2026-09-08, stage 4: The gallery hard-codes each fixture's current task id and urgency rather than computing them, because the UI must never derive the current task; it uses FIXED_NOW_MS so overdue and due-soon states are stable..
- 2026-09-08, stage 4: The gallery theme switch uses aria-pressed buttons and applyTheme writes or deletes data-theme on <html>; the theme query is read from both the search string and the part of the hash after '?', so /?theme=dark#/dev and /#/dev?theme=dark both work..
- 2026-09-08, stage 4: DevGallery is code-split from main.tsx so fixtures and the gallery never sit in the main bundle (main JS 12.50 kB, gallery chunk 35.88 kB)..
- 2026-09-08, stage 4: The e2e target test measures radios and checkboxes through their closest label and skips hidden elements or elements with no client rects, so the hidden file input counts via its Import button..
- 2026-09-08, stage 4: Chips are 20 px tall via a new --chip-height token in tokens.css (PLAN silent on chip height) so a timer chip stacked on an intake chip with the 4 px gap (44 px) fits the 48 px row header; the 24 px --cell stays for belt cells only..
- 2026-09-08, stage 4: .row__belt is display: flex so the belt scroller's negative margins cannot collapse through the wrapper; the row's bottom padding is calc(space-2 - hairline) so 8 + 48 + 24 + 7 + 1 px border = exactly 88 px border-box, and the wrapper box now sits where the cells are drawn..
- 2026-09-08, stage 4: Toast arms its timer on mount, re-arms only if durationMs changes, and reads the latest onExpire through a ref; stage 5 should key each toast by id so a new message gets a fresh countdown..
- 2026-09-08, stage 4: Timer chip markup: code span and countdown text share one inline span inside the flex chip (preserves the space in layout, innerText and copy), rather than adding a flex gap..
- 2026-09-08, stage 4: Dev gallery renders four PatientSheet states; the owner phone on the custom-task patient is the synthetic literal '+44 0000 000000' written in DevGallery.tsx because tests/fixtures/synthetic.ts is shared scaffold outside stage 4 ownership and its FORM_CAT.ownerPhone is typed optional (exactOptionalPropertyTypes forbids spreading it)..
- 2026-09-08, stage 4: dev-gallery.test.tsx scopes theme-button role queries to the gallery header with within(); document-wide getByRole over the enlarged gallery took 4.7 s in jsdom and hit the 5 s test timeout..

- 2026-09-09, stage 5: 2026-09-09, stage 5: the store accepts the repo as a promise and starts its write chain by awaiting it, so the app renders before boot and no write can run against a missing repo; hydration then replaces the state without writing, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: State.now follows the dispatched action's own timestamp (TICK/DUE now, stamped at) and the clock only for unstamped actions, so the board re-sorts on every action and a fresh check reads exactly 'C1 in 15:00'; row countdowns and the header clock therefore refresh on TICK (at most 60 s apart, the scheduler's single-timer cap) and on every action or visibility change, not every second, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: RESET persists as one clearAll instead of N deletes plus saveSettings; every other action is persisted purely from the reference diff (patients changed/removed, events appended from the previous length when the previous tail is intact, settings by reference), n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: the write_failed banner reads 'Not saving: storage unavailable. <reason>' and recovered clears it only when the banner was a write failure; an unavailable banner is permanent for the session, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: the scheduler's notifier is gated by settings.notifications: off means DUE_VIBRATION only, on defers to the notifier's permission check; vibration is never suppressed (PLAN.md section 8), n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: a read-only tab is a viewer: rows and bar buttons disabled through new Belt.disabled and PatientRow.readOnly props, no sheets rendered, and the session ignores every action; it never starts the scheduler or sweeps (stage 3 verifier note), n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: the read-only tab reacts to `released` by changing the lock banner to 'The other tab has closed. Reload to take over.' with a Reload button rather than reloading itself, keeping PLAN.md's no-silent-reload rule; two tabs booting within the same 300 ms window would both become primary, accepted because PLAN.md declares multiple tabs unsupported, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: boot races open+load against a 5 s real-clock timer; a stall or a load() rejection both fall back to openRepo({ indexedDB: undefined }) (memory) with an empty state, close the first connection when it arrives, and emit unavailable with the specific reason after the memory repo's own replayed notice, y (load rejection falls back to memory rather than an empty state on the idb repo)..
- 2026-09-09, stage 5: 2026-09-09, stage 5: routes push a hash entry on open (Android back closes a sheet) and history.replaceState on Close so back never reopens the sheet just closed; unknown hashes render the board; #/dev is still served by main.tsx before the app mounts, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: the discharge button dispatches COMPLETE_TASK on the discharge task while it is todo (undoable, belt-complete feedback when nothing is left) and DISCHARGE only when the task was skipped (toast without Undo), per the stage 1 verifier note, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: completion feedback and the undo toast live in session flows (complete, skip, discharge) rather than in the components; the toast is keyed by a sequence id so each message gets a fresh 4 s countdown (stage 4 Toast note), n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: the transient banner for notifier, feedback, service worker and write-chain errors is dismissible and clears itself after 8 s through the injected clock, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: registerSW is injected into setupServiceWorker so the virtual:pwa-register module is imported only by main.tsx (outside coverage) and unit tests exercise onNeedRefresh / onRegisterError / updateSW(true) with a fake, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: Settings export/import/purge/delete-all are wired now (download anchor, parseImport, PURGE_DISCHARGED, RESET) because they were one line each; Share API and textarea fallbacks and their e2e remain stage 6, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: interface members that get passed around (Session, SessionActions, Router, AppStore, SwHandle, TabLock) are declared as function-typed properties, not method signatures, so the strict unbound-method rule allows passing them as callbacks, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: e2e time control uses page.clock.install before navigation without pausing (Preact effects schedule through requestAnimationFrame/setTimeout, which the paused clock would freeze) and fastForward for jumps; the offline test waits for the IndexedDB write to land by reading the database from the page before reloading, because a reload inside the write's transaction window aborts it, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: main and the bottom bar get the inert attribute while a sheet is open (stage 4 open question 3) so focus cannot wander behind the dialog; the header stays live, n..
- 2026-09-09, stage 5: 2026-09-09, stage 5: app-shell layout lives in src/ui/app/app.css from tokens only (the no-literal-colours test scans it); the toast is lifted above the fixed bottom bar with a scoped rule rather than a change to ui.css, n..

- 2026-09-09, stage 6: 2026-09-09, stage 6: a Share API rejection named AbortError (nurse dismissed the share sheet) is 'cancelled': no download, no lastExportAt, no toast, no banner; any other share failure or a canShare that returns false or throws falls through to the download, PLAN.md deviation n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: the textarea fallback is a read-only textarea in the Settings sheet that selects all on focus with a Done button; it does not set lastExportAt because nothing was saved, so the weekly nudge persists until a share or download succeeds, PLAN.md deviation n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: export from the nudge banner opens #/settings when the chain ends in the textarea (App.exportFrom), because the text renders inside that sheet, PLAN.md deviation n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: the weekly nudge rule is lastExportAt missing, unparseable, or more than exactly 7 x 24 h before state.now, with at least one patient of any status; dismiss is a session signal only (PLAN.md section 9 'nudges weekly'), n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: import rejections and the export textarea are session signals rendered inline by Settings (a banner behind the sheet scrim would be unseen); both clear on Done, on sheet Close and on Delete everything, and importError also clears at the next import attempt, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: session.dispatch ignores actions until store.hydrate has run (hydrated flag) so a pre-boot action can never be written and then dropped by the load; boot purge runs after that flag and only for the primary tab, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: auto-purge at boot is a normal PURGE_DISCHARGED dispatch stamped from the clock, so deletions persist through persistDiff exactly like the Settings button; no toast at boot, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: Platform.share is optional and the session defaults it to browserShare() (navigator or {}) so main.tsx (stage 5 ownership) is unchanged; the unit fake platform passes {} by default so no test touches the jsdom navigator, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: export file name uses the local calendar date (the ward clock date) rather than the UTC date, y (stage 5 used the UTC slice; PLAN.md silent)..
- 2026-09-09, stage 6: 2026-09-09, stage 6: e2e median admit-to-discharge is computed from the timestamps the app stored (read from IndexedDB) and formatted like format.ts, because real seconds elapse between clock fast-forwards; a 60 to 65 minute sanity bound guards the computation, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: e2e reads raw IndexedDB only after checking indexedDB.databases() lists 'wardbelt', because a bare open() would create an empty version 1 database and defeat the app's upgrade, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: auto-purge e2e uses page.clock.setSystemTime plus reload (the installed clock survives navigation) rather than fastForward, so no timers fire across the two-day jump, n..
- 2026-09-09, stage 6: 2026-09-09, stage 6: the shared e2e helpers (including axe and 48 px target sweeps) live in tests/e2e/helpers.ts and flows.spec.ts imports them, n..
- 2026-09-09, stage 6: Session.dispatch now reports hasPersistableChange(before, after) rather than reference inequality, so its boolean means 'a record changed' and is independent of the store republishing when only `now` moved; the only behavioural side effect is that a no-op undo or delete no longer clears the toast, which is the correct behaviour (regression test added)..
- 2026-09-09, stage 6: purgeDischarged reports from the patient count difference (n === 0 -> 'Nothing to purge') and returns silently before hydration, matching every other action, rather than showing 'Nothing to purge' on an unhydrated store..
- 2026-09-09, stage 6: downloadText revokes the Blob URL immediately when the click never happened (nothing is in flight) and keeps the 30 s delayed revoke only after a successful click; the anchor is removed in finally on every path..
- 2026-09-09, stage 6: exportText treats a throwing download the same as one returning false (outcome 'textarea'), making the documented 'exportData never rejects' contract true without touching App.tsx..
- 2026-09-09, stage 6: The e2e purge regression uses page.clock.install with the clock running naturally (not pauseAt) so the wall clock moves between the discharge and the purge click exactly as in real use, then setSystemTime to make the patient stale..

- 2026-09-09, stage 7: 2026-09-09, stage 7: Lighthouse pinned to 13.4.1 (current) for performance/accessibility/best-practices and 11.7.1 (last release with the PWA category) for PWA, both invoked via `npx --yes lighthouse@<version>` as tools, not dependencies; 13.4.1 warns EBADENGINE on Node 22.16 (wants >= 22.19) but runs to completion, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: bundle limit defined as 60,000 bytes (decimal kB, matching Vite's own reporting) over every dist/assets/*.js and *.css including lazy chunks, excluding sw.js and index.html; measured 40,761 bytes, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: evidence scripts print through process.stdout.write because the repo lint config makes no-console an error for scripts/*.mjs too, and their JSON summaries are formatted through the repo's Prettier API so `prettier --check .` (which covers docs/evidence/*.json) passes, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: scripts take --no-build (measure an existing dist) and --write (refresh docs/evidence); CI calls bundle-size and hardening with --no-build after npm run build, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: Lighthouse kept out of CI (performance variance on shared runners); bundle-size and hardening checks added to ci.yml instead, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: Workbox's single console.warn in dist/sw.js is accepted and proven unreachable (every precache entry has a revision key) rather than stripped, because stripping needs a change to the frozen vite.config.ts, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: the axe pass on the real board scrolls the sheet body to the top before auditing so only the row straddling the scroll container's clip edge is reported incomplete (color-contrast, manual review); the palette contrast is verified arithmetically in contrast.test.ts, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: axe evidence is written to docs/evidence/axe.json only when WARDBELT_EVIDENCE is set (merged per theme, workers 1), otherwise attached to the Playwright report, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: TESTING.md states the measured sources as dd6b370 (main after stage 6) because stage 7 changed nothing under src/ and the build hashes are identical; the stage 7 commit SHA cannot be embedded in a file that is part of that commit, n..
- 2026-09-09, stage 7: 2026-09-09, stage 7: the storage write-failure test fails four puts because ADD_PATIENT persists two writes (patient then event) and the second write retries the queued first before itself; two failures alone are cleared by the event write's flush, which is the documented stage 2 behaviour, n..

- 2026-09-09, stage 8: 2026-09-09, stage 8: every evidence script (and the axe e2e with WARDBELT_EVIDENCE) was re-run once at commit 3aa07b1 before any stage 8 change, and TESTING.md was updated only where the re-run differed (Lighthouse LCP 1.5 s, TBT 10 ms, max-potential-fid 0.99 at weight 0; scores unchanged); 3aa07b1 is the first commit containing every measured source and test; numbers copied from the run, never retyped, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: README screenshots are written to docs/screenshots/ only when WARDBELT_EVIDENCE is set (the stage 7 axe switch), otherwise to the Playwright output directory, so npm run test:e2e in CI and in the pre-push gate never rewrites a committed PNG, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: the screenshot scene seeds four synthetic patients (dog back from theatre with check 1 done on time and check 2 overdue, cat in pre-op with a done and a skipped cell and a 10:00 intake chip, fresh rabbit, discharged patient shown through the board toggle), dismisses the export nudge and captures with animations disabled, so every belt state, both chip tones, non-trivial summary numbers and the discharged row are visible, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: install.spec.ts derives the app base from the page's manifest link (so it holds on the /wardbelt/ sub-path) and accepts an active worker in the activating or activated state, because serviceWorker.ready resolves as soon as a worker is active, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: scripts/check-base-path.mjs builds with VITE_BASE_PATH=/wardbelt/ into a temporary outDir and asserts the manifest, index.html and service worker registration there, leaving dist/ untouched rather than building into dist/ and rebuilding, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: scripts/pre-push.sh refuses to start while something listens on port 4173, because playwright.config.ts reuses an existing server outside CI and a stale preview would silently test an old bundle; setting CI=1 was rejected because it changes retries and reporters, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: the clean-clone step clones the working tree's HEAD (a worktree path works) into WARDBELT_TMP or a mktemp directory, runs npm ci, build and test, removes the clone on exit, and prints a note when the working tree is dirty because the clone tests commits only, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: the PLAN.md section 2 CV line is not reproduced in the README because its 'deployed via GitHub Actions' part is not evidenced in TESTING.md at the time of writing; the orchestrator verifies the deploy after the merge, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: the pre-push hook is installed by a symlink into .git/hooks or by core.hooksPath pointing at an untracked directory; no hook file is committed, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: DECISIONS.md is one table per stage with the six fields as columns (167 entries: 148 STATE.md lines, two labelled extras from the scaffold and the stage 4 report, 11 stage 8 entries, six section 9 trade-offs), n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: .github/workflows/pages.yml is unchanged; actionlint is clean and check-base-path.mjs proves the VITE_BASE_PATH build it makes is right, so no deploy fix was needed, n..
- 2026-09-09, stage 8: 2026-09-09, stage 8: two commits rather than one (5ca912e implementation, 3c81d9a TESTING.md/README gate evidence) so the quoted gate SHA stays in history; the gate was re-run green on 3c81d9a, n..
- 2026-09-09, stage 8: 8.12: scripts/pre-push.sh derives ROOT from `git rev-parse --show-toplevel` rather than from its own path, because as an installed hook it is a symlink under .git/hooks or core.hooksPath and git runs hooks from the top of the working tree; running it from outside any checkout now fails with git's own 'not a git repository' message..
- 2026-09-09, stage 8: 8.13: scripts/coverage-table.mjs floors percentages to two decimals (istanbul's convention) and coverage.json allFiles uses the same function, so table, evidence JSON and the vitest printout agree for the same counts; the table and coverage.json were regenerated from a fresh npm test at 3c81d9a and README/TESTING quote 99.18%. Alternative rejected: retyping 99.18 by hand while the script still printed 99.19..
- 2026-09-09, stage 8: 8.14: tests/e2e/dev-gallery.spec.ts asserts the body background with expect.poll at all four read sites (not only the two the verifier named), since every read follows a render whose data-theme is applied in a useEffect; alternatives rejected: a fixed delay, or asserting the data-theme attribute instead of the computed colour..
- 2026-09-09, stage 8: The alt text for patient-sheet.png was rewritten from the PNG itself (viewed) and cross-checked against the assertions in tests/e2e/screenshots.spec.ts (check 1 Done, check 2 Due, check 4 in viewport); board-light.png's alt text was also checked by eye and left unchanged..
- 2026-09-09, stage 8: TESTING.md's fix-round paragraph cites the worktree run by HEAD 3c81d9a and describes the hook-clone runs without a SHA, because the commit they cloned (b0f5713) was amended to add that paragraph and is not on the pushed branch..
- 2026-09-09, stage 8: Verifier notes that were not failures were left alone to keep the change set to the four findings: docs/evidence/axe.json still carries no commit field (adding one means editing the stage 7 a11y spec and re-running with WARDBELT_EVIDENCE); TESTING.md's reference to 'the stage 8 report in STATE.md' stands because the orchestrator writes that report; README line 105's 'anything on the board' wording for the export nudge was left as is..
- 2026-09-09, stage 8 (orchestrator): the second verification's single minor finding was fixed directly by the orchestrator per CLAUDE.md step 4, without a third verifier round, because the change was documentation plus a temp-directory cleanup and the full pre-push gate was re-run green, n.

## Open questions
Anything blocking or deferred, with the stage it affects.

## Next action
Orchestrator: confirm the CI run and Pages deploy for the stage 8 merge on main, check the deployed manifest and service worker, then set this line to none: deployed.

